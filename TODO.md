# TODO — build.one.web

Pending work, deferred decisions, known issues. Check off as done; prune anything stale.

## U-190 spawned follow-ups (2026-08-02, surfaced by /simplify Pass-2)

- [x] 🟡 **`ContractLaborView.tsx:113-128,264-268` raw float line math** — View recomputes amount/price with raw `Number(x) * ...` float arithmetic while ContractLaborEdit now routes through the exact-decimal `computeBillLine`/`shared/money`, so **View and Edit can display different money for the same row**. Route View through the same shared helpers. **DONE — U-198** (routed through `computeAmount`/`applyMarkup` with raw decimal strings; totals sum the per-line cent-rounded values; 7 mutation-proven specs).
- [ ] 🟡 **Move `pages/bills/lineMath.ts` → `src/shared/`** — it is now the shared line-math home consumed by bills, budgets (U-190), and (indirectly) contract-labor; living under `pages/bills/` makes every non-bill consumer cross-import from a sibling page. Relocate + update importers (BudgetEdit, lineMath.test.ts, any others). Behavior-neutral move. **Scope note (U-198):** the three contract-labor screens (`ContractLaborView`, `ContractLaborEdit`, `LaborReviewScreen`) deliberately adapt straight to `shared/money` and are **OUT of scope** for this move — do not try to route them through `computeBillLine`. Its string-in/string-out contract blanks zeros (`amount: rounded ? rounded.toFixed(2) : ""`) and carries the U-167 lump-sum-preservation branch, neither of which has meaning for contract labor; routing CL through it would turn a legitimate `$0.00` row into `—` and blank a malformed row instead of showing `NaN`.

## U-198 spawned follow-ups (2026-08-02, surfaced by /simplify Pass-2 + Codex Pass-1)

- [ ] 🟡 **`shared/money.ts` silently zeroes non-finite NUMERIC input — fix at the root and retire the U-198 shim.** `canonicalDecimalString` (`money.ts:46-51`) handles a malformed *string* correctly (fails the well-formed gate → float fallback → NaN propagates) but maps any non-finite *number* to `"0"`. So `applyMarkup(NaN, …)` returns exactly `0`, rendering a plausible `$0.00` over broken data. Found by Codex Pass-1 on U-198 and confirmed empirically. U-198 patched its own call site (`ContractLaborView.linePrice`); **`ContractLaborEdit.tsx:57-59` is the identical unguarded twin and still renders `$0.00`** — and it is worse there, because `:389` **persists** `price: computePrice(row)`, so a non-finite amount would store a price of `0`. (Reachability is narrow: cents must exceed `MAX_SAFE_INTEGER`, i.e. ~$90.07 trillion, or a pasted `1e400`.) **Fix:** delete the non-finite branch in `canonicalDecimalString` and let `String(v)` fall through to the existing float fallback — no new path. **Blast radius checked:** flips 2 assertions in `money.test.ts:41-45` (a deliberate shared-kernel contract change, hence its own unit); `percentToFraction`/`fractionToPercent` keep zeroing (they gate themselves); `bills`/`budgets` unaffected (NaN price is falsy → still `""`); `LaborReviewScreen` immune (`parseOrZero` clamps first). Retires the shim + comment in `ContractLaborView.linePrice`; its 3 malformed/overflow specs stay green.
- [ ] 🔴 **`ContractLaborLineItem.Price` has three authors, two rounding formulas, and zero client readers — and the stored value is what gets BILLED.** Web is **two-shot** (round amount to cents → apply markup → round again; the U-181 policy) at `ContractLaborEdit.tsx:389` + `LaborReviewScreen.tsx:507`; T-SQL is **single-shot** (`dbo.time_entry.sql:1520`, `@TotalHours * @HourlyRate * (1 + ISNULL(@Markup,0))` into DECIMAL(18,2)). **They disagree by 1¢ on realistic CL rows** — verified against the real helpers: `0.75h × $40.18 @ 25%` → web `$37.68`, SQL `$37.67`; `2.5h × $33.33 @ 15%` → web `$95.83`, SQL `$95.82`. `build.one.api/entities/contract_labor/business/bill_service.py` reads `li.price` verbatim (6 sites: `:388,:535,:838,:939,:993`) to build the BillLineItem, the totals and the PDF, while **no CL web surface displays the stored column at all** — View recomputes, Edit drops it in `fromServer`. So the number on screen can differ by a cent from the number billed to the client. Note U-198 changed which side View sits on: its old float `hours*rate*(1+markup)` was structurally *single-shot* (SQL-shaped); it is now two-shot (Edit-shaped, matching what a web save persists). **Cross-repo, needs a `/domain` + `/pm` call on the rounding policy**, then make both repos implement it and either drop `price` from the client contract or badge a recompute-vs-stored mismatch. NOT a web-only refactor.
- [ ] 🟢 **Test-fixture triplication** — `sampleLineItem`/`sampleEntry` are now byte-identical in **three** spec files (`ContractLaborEdit.test.tsx:83,146`, `ContractLaborView.test.tsx:59,84`, `LaborReviewScreen.test.tsx:66,104`) — 61 lines materialising 52 typed fields, so every new required field on `ContractLabor`/`ContractLaborLineItem` forces three identical edits. Extract to `src/__testutils__/contractLaborFixtures.ts` (the established home, alongside `flush.ts`/`domEvents.ts`). Deliberately NOT done in U-198: it would have meant editing two spec files outside that unit's scope.
- [ ] 🟢 **Make lump-sum amounts enterable in BudgetEdit** — U-190 preserves a stored lump-sum amount but there's no first-class way to TYPE one (the Amount cell is display-only; you can only reach a lump-sum row by clearing qty/rate on a unit line). Add an editable Amount affordance for schedule-of-values rows.

## Bills post-hoc — U-181 spawned follow-ups (2026-08-01)

- [x] **✅ BudgetEdit::compute money defects — DONE (U-189 `bca48c6`, roundMoney port)** · ~~carries both lineMath defects~~ — `src/pages/budgets/BudgetEdit.tsx` `compute()` still rounds via `toFixed(2)` on the un-rounded path (mis-rounds half-cents AND derives price from the un-rounded amount) — the exact pair U-181 fixed in `computeBillLine`. Port/share `roundMoney`.
- [x] **✅ Persisted-price rounding — DONE (U-185 `948dd41`, both CL pages → roundMoney)** · ~~toFixed rounding~~ — `LaborReviewScreen.tsx:515` + `ContractLaborEdit.tsx:381` round PERSISTED prices via `Number(x.toFixed(2))` (same mis-rounding). Route through `roundMoney`.
- [x] **✅ P1 — shared EXACT-DECIMAL line-math module — DONE (U-192, `src/shared/money.ts`)** · `roundMoney` MOVED there (re-export dropped — `bills/lineMath` is no longer a second home); exact BigInt scaled-integer `computeAmount` / `applyMarkup` / `percentToFraction`; all four compute sites migrated; `80.02 × 1.25` now persists `100.03`. ~~`roundMoney` now has 3 consumers (bills/lineMath, ContractLabor ×2, BudgetEdit) via interim cross-import → promote. **AND (Codex U-189 P1, pre-existing across ALL of them):** the `qty×rate` / `amount×(1+markup)` MULTIPLY is float — `80.02 × 1.25` persists `100.02` where exact is `100.03`; `roundMoney` only rounds the shortest-round-trip decimal of the already-lossy product, so it cannot recover the exact value. The module must own `roundMoney` + an exact-decimal multiply policy (scaled-integer cents or decimal-string) so bills/CL/budget all compute PERSISTED money correctly. Pairs with U-190. ~~Eventual shared money.ts~~ — `roundMoney` lives in `bills/lineMath.ts`; **the CL pages now consume it via cross-import (U-185)** — 2 consumers, RIPE; promote to `src/shared/money.ts` when BudgetEdit joins (or now). NB `budgets/revisionLedger.ts` exports a DIFFERENT `roundToCents` (returns integer CENTS) — keep names disjoint (silent 100× hazard).

## Money math — U-192 spawned follow-ups (2026-08-01)

Surfaced by U-192's Codex review + `/simplify` (4 agents). None are float-multiply
defects — that class is closed. Ranked by real user impact.

- [x] 🔴 **P1 — `ContractLaborEdit` renders a float-garbage markup into a visible input.** **RESOLVED (U-195, 2026-08-02):** `fractionToPercent` exported from `shared/money` (BigInt decimal shift +2 with trailing-zero normalization, inverse of `percentToFraction`), adopted at all three crossings; 6 mutation proofs. The fraction→percent direction is hand-rolled as `String(fraction * 100)` at `ContractLaborEdit.tsx:90`, `:222`, `:312`: markup `0.07` → `"7.000000000000001"`, `0.14` → `"14.000000000000002"`, `0.29` → `"28.999999999999996"` — ordinary markups, landing in the **Markup (%)** field the user reads. `LaborReviewScreen.tsx:114` already uses an immune spelling (`Math.round(n*10000)/100`), so the two CL screens disagree. Fix = export a `fractionToPercent` from `shared/money` (its private `formatDecimal` is already the machinery) and use it at all crossings. Pass-1 defect on lines U-192 did not touch.
- [ ] 🟡 **P2 — `LaborReviewScreen` display/save disagree for UNTOUCHED >2-decimal server values.** `shapeLine` (`:258`) feeds display math from `decimalToDisplayString`'s **2dp-rounded** string while the save path (`:480`) reads raw `li.hours`/`li.rate`: server `hours "1.234" × rate 10` shows **$12.30**, persists **12.34**. Pre-dates U-192 and is unchanged by it (a display-precision defect, not a float-multiply one). Note `decimalToDisplayString`'s own comment claims "Save path never reads this value, so the rounding is purely cosmetic" — false, the display *math* reads it. Decide what the reviewer should see, then make the two paths share one source.
- [ ] 🟡 **P2 — `revisionLedger.ts:9` uses `Math.round(n * 100)`** — the exact float-multiply `roundMoney`'s docblock forbids; `roundToCents("1.005")` → 100 vs exact 101. Latent today (BudgetEdit pre-rounds to 2dp) but live on the budget-ledger display path (`BudgetView.tsx:478`, `:489`). It also hand-rolls a cents→string formatter `shared/money`'s `formatDecimal` already implements. Right depth: `money.ts` owns `toCents()` + `centsToDecimalString()`, `revisionLedger` consumes them — which also retires the 100×-confusion warning that only exists because there are two modules.
- [ ] 🟢 **P3 — four percent→fraction crossings still float-divide** (`ContractLaborEdit.tsx:386` persists `markup` via `Number(pct)/100` one line above `:387` persisting `price` via `percentToFraction`; also `LaborReviewScreen.tsx:259`, `:317`, `:493`). **Verified NOT money loss:** all 401 realistic markups (0–100% in 0.25 steps) agree with the exact shift. Consistency/legibility only. **Re-flagged by U-195's altitude review (2026-08-02)** and held out of scope by explicit human call: U-195 converted the three INBOUND crossings exactly, so `ContractLaborEdit` now reads exact-in / float-out. Right-depth close is `Number(percentToFraction(row.markup_percent))` at `:386` — one line, helper already imported, keeps the wire type a JSON number.
- [ ] 🟢 **P3 — collapse `isWellFormedBoundedDecimal` + `parseDecimal` into one pass** returning `null` on malformed. Today each operand is trimmed twice and scanned for the exponent twice; it also removes the standing invariant "never call `parseDecimal` without validating first". Differentially verified equivalent over 20K random strings; deferred only because U-192 was at its review-round cap. (~14 lines, and `formatDecimal`'s dead `scale <= 0` branch goes with it.)
- [ ] 🟢 **P3 — `ContractLaborEdit` computes each row 4×** (`:245`, `:249`, `:655`, `:656`) where one `useMemo` of `{amount, price}[]` would do. Pre-dates U-192; realistic N is 1–6 lines (one worker-day, `MAX_DAILY_HOURS = 8`) so ~0.09 ms — take it for shape, not speed. **`ContractLaborView` now has the same shape** (U-198: `lineAmount` runs 4× per row — the Amount cell, inside `linePrice` for the Price cell, and again in each of the two total memos). Measured at 1.05 µs of wasted BigInt parse per row per pass — deliberately not fixed, since collapsing it forces the two independent total memos into one. Fix both together or neither.
- [ ] 🟢 **P3 — `fmtMoney` constructs a fresh `Intl.NumberFormat` on every call, 13 sites repo-wide.** `n.toLocaleString("en-US", { style: "currency", … })` with an **options object** defeats V8's format cache (only the no-options path is cached). Measured: **38,407 ns per call vs 829 ns** for a module-level `Intl.NumberFormat(...).format(n)` — ~46×. Hoisting the options object does NOT help (42,376 ns); the formatter itself must be hoisted. An 8-row ContractLaborView spends ~1 ms here versus ~8 µs in the entire money-math path. Pre-dates U-198 and unrelated to it — surfaced by that unit's `/simplify` efficiency pass while measuring something else. Shared helper in `shared/money.ts` would close all 13.
- [ ] 🟢 **P3 — unify field-level numeric input rejection across the four line-item editors.** `LaborReviewScreen` has `inputRejectsNegative`/`inputRejectsAboveMax`; bills and budgets have nothing (BillEdit's markup input is `type="number" step="any"` with **no `min`** — that is how the negative-markup P0 was reachable). NB even after this lands you cannot drop `money.ts`'s own guard: a shared kernel must stay total.
- [ ] 🟢 **P3 — `toMoneyField(n)`** — `x ? x.toFixed(2) : ""` is spelled 4× and encodes a subtle rule (BOTH `0` and `NaN` render `""`; the malformed-input specs depend on the NaN half).

## Money math — U-195 spawned follow-ups (2026-08-02)

Surfaced by U-195's Codex xhigh Pass-1 (PASS) + `/simplify` (4 agents). The
float-multiply display defect itself is closed; these are the reuse/altitude
residuals the unit deliberately did not widen into.

- [ ] 🟡 **P2 — nine hand-rolled markup-percent displays, drifted to three different precisions.** `(Number(v) * 100).toFixed(N)` is spelled at `vendors/VendorView.tsx:50`, `employee-labor/EmployeeLaborView.tsx:57`, `employees/EmployeeView.tsx:46`, `employees/EmployeeList.tsx:20` (all `toFixed(0)`), `invoices/InvoiceView.tsx:25`, `bills/BillView.tsx:45`, `expenses/ExpenseView.tsx:25` (all `toFixed(1)`), `contract-labor/ContractLaborView.tsx:32` (`toFixed(2)`), `labor/LaborReviewScreen.tsx:100`. **The same 7% markup renders as `7%`, `7.0%`, and `7.00%` depending on which page you open.** These are NOT `fractionToPercent` call sites — they want a `%` suffix and fixed decimals, and `toFixed` already masks the float noise — so the fix is a **second** helper (`formatPercent(fraction, digits, blankAs)`) next to it in `shared/money.ts`, not a reuse of the first. Six of the nine sites are in files U-195 was held out of.
- [ ] 🟢 **P3 — `LaborReviewScreen.markupToPctDisplayString` is now a near-duplicate of `fractionToPercent`.** `LaborReviewScreen.tsx:110-115` has the identical contract (fraction → bare percent string for an `<input value>`, `""` on null/blank) via the immune-but-different spelling `String(Math.round(n * 10000) / 100)`. **Not a mechanical swap:** that spelling quantizes to 2 decimal places of percent, so `0.00125` → `"0.13"` where `fractionToPercent` → `"0.125"` (pinned by U-195's own spec). Consolidating changes displayed values for sub-0.01% markups — needs a product call. The right shape is a nullable `fractionToPercentOrBlank` in `shared/money.ts` adopted at `ContractLaborEdit.tsx:91`, `:223` and `LaborReviewScreen.tsx:260`, `:406`, `:408`, `:1032`. Note the guard callers hand-write today is defending against `""` as much as `null`, and `""` is invisible to `Decimalish` (`canonicalDecimalString("")` → `"0"`) — the docstring's prose is doing type-system work.
- [ ] 🟢 **P3 — `setSelectValue` shipped shared but its four predecessors are still live.** U-195 added it to `src/__testutils__/domEvents.ts` (sibling of `setInputValue`). Not migrated: `bills/BillEdit.test.tsx:354` (a **byte-identical** local `setSelectValue`, call sites `:406`, `:516`), `bills/BillCreate.test.tsx:140` (`selectChange`, raw `.value =`), `bill-credits/billCreditInvalidate.test.tsx:198` and `sub-cost-codes/subCostCodeInvalidate.test.tsx:181` (inline raw `.value =` — these two do NOT reset React's `_valueTracker`). All four files already import from `__testutils__/domEvents`, so each is a one-token import change plus a delete. **Also stale:** `billCreditInvalidate.test.tsx:197` carries the comment `// <select> has no shared helper (setInputValue is typed for HTMLInputElement).` — U-195 made that false.
- [ ] 🟢 **P3 — three more test factories join the already-measured backlog.** `ContractLaborEdit.test.tsx` now has `sampleLineItem` (twin at `labor/LaborReviewScreen.test.tsx:104`), `sampleVendor` (twin at `vendors/vendorInvalidate.test.tsx:58`), `sampleProject` (twin at `project/projectListFilters.test.ts:6`). Adds to the `makeModule`/`makeUser`/`adminUser()` tally below (16 and 27 verbatim copies). Per this file's own standing note, extract as a **dedicated test-only unit** (`src/__testutils__/factories.ts`), not smuggled into an unrelated one.

## Bills post-hoc — U-176 spawned follow-ups (2026-07-30)

- [ ] **tsconfig type-check gap** — `tsconfig.app.json` EXCLUDES `ExpenseEdit.tsx`/`BillCreditEdit.tsx`/`InvoiceEdit.tsx` (lines 28/34/37), so neither `tsc -p tsconfig.app.json` nor `npm run build`'s `tsc -b` type-checks them — a change to those pages compiles 'clean' under the standing build while never being verified. U-176 proved them 0-errors via a one-off tsc, but that's not enforced. Either drop them from `exclude` (fix whatever made them excluded) or add a CI one-off. Real gap.
- [ ] **Mid-save refetch rekeys a new row (P3)** — in the InlineLineItems consumers, a refetch landing mid-save can rekey a just-added row. Root cause is the accumulate-then-commit saveAll in Expense/BillCredit/Invoice (same class BillEdit's U-170 `stampRow` fixed), NOT the uid mapper — the right fix is porting stampRow across the three pages, not a bandaid. Deliberately not patched in U-176.
- [x] **✅ BudgetEdit index-key — DONE (U-189 `bca48c6`, uid key)** · ~~:383 variant~~ — `key={li.public_id ?? \`new-${idx}\`}` still index-keys NEW rows; BudgetEdit does NOT use InlineLineItems, so U-176 didn't touch it. Its own small unit (mint a uid for new budget rows).
- [ ] **Invoice add-row autofocus (pre-existing)** — InvoiceEdit's add-row selects the first field; Codex refuted it as a U-176 regression (pre-existing). Minor UX polish if wanted.
- [ ] **Migrate remaining local uid minters onto `lineItemUid.ts`** — BillEdit.tsx:66, BillCreate.tsx (U-175's newLine), TimeEntryView.tsx:128 still mint locally. When migrating **BillEdit**, also flip its hydrate (BillEdit.tsx:216, currently `newLineUid()` per row) to `persistedLineItemUid(public_id)` + `existingUidsByPublicId` reuse — sharing the module alone leaves BillEdit's remount-on-refetch defect in place. Supersedes/narrows TODO:9.

## Bills post-hoc — U-175 spawned follow-ups (2026-07-30)

- [ ] **BillEdit load-effect dep + auto-save item-seed** (U-174 Option B, deferred) — the line-items load effect deps on `[item, fullProjects]` and mints fresh uids on every run, so any item-cache seed (U-174's saveAll seed, the shipped onComplete seed at :192) re-fires it (extra GET bill_line_items + row remount). Narrow the dep to `[item?.id, fullProjects]` (its only real input), THEN seed the item cache on the 300ms auto-save to close the Cancel-after-autosave stale-row_version gap (unsafe today: mid-type seeding would clobber in-progress line edits via the effect re-run). ⚠️ Behavior change to the shipped completion path — own unit, careful Complete-path test.

- [x] **✅ InlineLineItems index-key — DONE (U-176 `480c472`)** — Expense/BillCredit/Invoice now key on a shared-minted uid (`src/shared/lineItemUid.ts`). RESIDUAL: BudgetEdit:383 variant + BillEdit/BillCreate/TimeEntryView still mint locally (below).  ~~ONE shared-component unit~~ — `src/components/InlineLineItems.tsx:73` renders `<tr key={rowIdx}>`; Expense/BillCredit/Invoice Edit all route rows through it, so removing a non-last line reuses a DOM node across logically-different rows (focus/input on the wrong row) on all three at once. Fix once: key on a stable item id (`public_id ?? client-uid`). `BudgetEdit.tsx:383` (`key={li.public_id ?? \`new-${idx}\`}`) is a separate variant (new rows still index-key). RENDERING defect — distinct from the per-page saveAll retry-safety defect the U-170 board note tracks.
- [ ] **BillEdit due-date parity** — U-175 removed BillCreate's due-date auto-calc + editable field (now `due_date = bill_date`, read-only, per Bill.DueDate = BillDate). BillEdit still has its own due-date handling → Create and Edit now contradict. Bring BillEdit into parity or decide the intended model.
- [ ] **Shared uid minter** — BillCreate's `newLine()` and BillEdit's `newLineUid()` both mint `li-uid-${++n}` independently; extract one shared minter (do it when the InlineLineItems unit lands so all line-row pages share it).

---

## Bills redesign (f73a3fd) — post-hoc `/code-review` follow-ups (2026-07-29)

The Bill Create/Edit redesign shipped as concurrent-session WIP without the two-pass gate; a post-hoc `xhigh` `/code-review` found 14 issues. **The P0 (silent money loss — `computeBillLine` zeroing lump-sum/QBO account-based line amounts) is FIXED + DEPLOYED as U-167** (`7ec9b1d`, `lineMath.test.ts`). The remaining 13 are follow-up units, each needs its own Map→Hunt→Refactor→Test→Verify→deploy:

**Update 2026-07-29 — all 4 High SHIPPED (web deploy batched):** U-168 `395bf3b` (submit off the create path) · U-169+U-170 `fb06d7c` (out-of-scope project preserve + retry-safe line-item write-back) · U-171 `7fcaa95` (shared-attachment re-home before first-line delete). Each Gate-2 verified (tsc · 671 vitest · build; independent CONFIRMED-FIXED / CONFIRMED-CLEAN). U-171 residual edges (Pass-1 found — all CONSERVATIVE ABORTS, no orphan / no data-loss; own follow-ups if multi-attachment-per-line bills ever appear):
- [ ] **U-171 edge — rehome can't see an unsaved new-row survivor.** If the ONLY surviving line is a brand-new row (no `public_id` yet), `survivingIds` is empty → safe abort ("Could not preserve…"); the user must Save (creates the row) then remove+Save. Fix needs `saveAll` to create new rows before re-homing.
- [ ] **U-171 edge — multiple DISTINCT attachments across removed lines + too few link-free survivors** → partial rehome write then abort; the "nothing was saved" message is then inaccurate and a retry re-aborts. Needs per-line multi-attachment handling (unusual today — "one shared PDF per bill" is the model).

**High**
- [ ] **Submit-for-Review empty reviewer recipients** — `BillCreate.tsx:230`. Submit is gated on `populatedLines.some(hasProject)` but `create/bill` embeds `submit_for_review=true` carrying only `populatedLines[0]`'s project; project-bearing later lines POST afterward → server resolves no PMs/Owners → BCC-only notification. Mirror BillEdit: persist all lines, then call `/submit/review/bill/:id` (never submit on the create path). Reintroduces the K06988-class bug BillEdit was refactored to avoid.
- [ ] **Project silently stripped on save** — `BillEdit.tsx:122`. Load resolves `project_id`→`project_public_id` via `fullProjects.find`; a project absent from the loaded (inactive/RBAC-scoped) list → `""` → next `saveAll` writes `project_public_id: null`, wiping the association (breaks PM resolution + doc routing). Preserve the original project id when the lookup misses (e.g. keep a fallback public_id), don't coerce to null.
- [ ] **saveAll mid-loop failure → duplicate lines + 409 wedge** — `BillEdit.tsx:311`. New public_ids / refreshed row_versions are written back to state only after the whole loop (`setLineItems(savedItems)` at 311); a mid-loop throw discards them in the catch → retry re-POSTs already-created rows (duplicates) and re-PUTs with stale row_versions (permanent 409 until reload). Persist per-item progress incrementally (update state as each row succeeds) or make the sync idempotent.
- [ ] **Shared attachment orphaned when first line removed** — `BillEdit.tsx:109`/`281`. The bill's single PDF is fetched/linked via `res.data[0]` only; deleting the first line deletes its BLI → the bill loses its only source document. Re-link the shared attachment to a surviving line before deleting, or model the attachment at the bill level.

**Medium**
- [ ] **Partial line-POST failure → overstated header total** — `BillCreate.tsx:332`. `create/bill` sends `total_amount` = sum of ALL lines, but additional lines POST one-by-one afterward and a failure only toasts + navigates — the header is never corrected, so `Bill.TotalAmount` > Σ created BLIs. Recompute + PATCH the header after the line loop (mirror BillEdit's `saveAll` recompute), or create all lines transactionally.
- [ ] **Cache reconciliation (SCOPED DOWN 2026-07-30)** — the bill LIST uses `usePaginatedList` (module-level Map, NOT React Query), so `invalidateEntity(list)` is a **no-op** (refuted for the list on both BillCreate-create and BillEdit-delete/save); the list self-heals via `load(false)` on mount (brief stale flash). The REAL, valid part is the **item** React-Query cache (`useEntityItem`): BillEdit delete should `removeEntity(itemPath)` and saveAll should `setQueryData(entityItemKey)` (a stale cached item can 409 a downstream editor) — owned by **U-174**. Follow-up (low-pri): expose cache invalidation from `usePaginatedList` if the create/delete stale-flash becomes annoying. ~~Missing cache reconciliation~~ — `BillCreate` (no list invalidate after create), `BillEdit:535` (delete: no `removeEntity`), `BillEdit:275` (saveAll: no `setQueryData` on the item). With global `staleTime: 5min` + `refetchOnWindowFocus: false`, stale lists / ghost rows / a 404-on-read item persist for minutes. Apply the documented `invalidateEntity`/`removeEntity`/`setQueryData` pattern (CLAUDE.md checklist step 7).
- [x] ❌ **REFUTED (2026-07-30, U-173 Gate-1, em-verified)** — the API's Universal-PDF rule (`entities/bill/business/service.py:377-387`) creates a placeholder BillLineItem and binds the attachment to it on `if attachment_public_id:`, rolling back the bill if either insert fails. No orphan is reachable; a `>=1 line` gate would break the intended flow. ~~Save-For-Later with zero populated lines → orphan attachment~~ — `BillCreate.tsx:297`. Enabled on `!file` alone; with no populated lines `buildBody` creates no summary BLI, yet `attachment_public_id` is still sent → the uploaded PDF binds to no line (invisible on BillEdit's `res.data[0]` fetch, orphaned in blob). Require ≥1 populated line when a file is attached, or create a summary line to hold it.
- [ ] **`total_amount` raw float sum** — `BillCreate.tsx:243` (+ BillEdit `saveAll` via `sumLineAmounts`). `reduce` over `Number(amount)` sends e.g. `0.30000000000000004` to the money field; NEW vs the pre-rework single-line `Number(line.amount)`. ✅ **VERIFIED NON-ISSUE (2026-07-30):** `Bill.TotalAmount` is `DECIMAL(18,2)` and the sproc param is `@TotalAmount DECIMAL(18,2)`, so SQL Server quantizes the float on assignment (`0.30000000000000004`→`0.30`). No client fix needed — closed.
- [ ] **Double-submit → duplicate create + orphan upload** — `BillCreate.tsx:288`. All three action buttons are `type=submit`; `disabled=busy` lands only after re-render, so a fast double-click fires `handleSubmit` twice (two uploads, two `create/bill`). Only the `(vendor,bill_number,date)` uniqueness 409 backstops it, and the orphan attachment persists. Guard with an in-flight ref / disable-before-await.

**Low**
- [ ] **Index-as-key on editable rows** — `BillCreate.tsx:459`. `key={index}` on an add/remove list → removing a non-last row makes React reuse DOM nodes / transient input+focus across logically different rows. Key on a stable row id (BillEdit already keys on `li.public_id ?? idx`).
- [ ] **DueDate auto-calc DST off-by-one** — `BillCreate.tsx:129`. `new Date(bd.getTime() + due_days*86400000)` drifts one day across a DST fall-back boundary despite the comment. Use `dd.setDate(dd.getDate()+due_days)`. (Also reconcile against the API convention that `Bill.DueDate = BillDate`.)
- [x] **✅ `lineMath` penny-drift — DONE (U-181 `f8c7b06`)** — new `roundMoney` (half-away-from-zero, decimal-string scaling + 2^49 cutoff) + price from rounded amount. ~~toFixed penny-drift (pre-existing)~~ — `lineMath.ts:36`. `amount` is `toFixed(2)`-rounded while `price` derives from the un-rounded amount, and `toFixed` mis-rounds exact half-cents (`1.005`→`"1.00"`). Not introduced by f73a3fd but now the single money path for both pages. Round-half-up on a scaled integer, and derive price from the rounded amount.
- [ ] **`.detail-fields-form` fragile CSS coupling (altitude)** — `index.css:5574`. A per-page descendant override styles `FormField`/`SelectField`/… private `.form-group` markup ("without touching the components themselves"); a future internal change silently breaks the row layout with no type error. Add a `layout="row"` variant/prop (or a shared form-row primitive) so the shell is a contract, mirroring `DetailView`.

---

## /docs surface — keep current + extend

v1 (admin-only, iOS section) shipped 2026-06-20. **Standing rule:** every feature add/change refreshes the matching `/docs` section — the Docs step of the per-unit pipeline (see `CLAUDE.md` "Documentation surface" + umbrella memory `feedback_docs_keep_current.md`). Follow-ups:

- [ ] **API section (LIVE)** — highest value: a live OpenAPI explorer from `/openapi.json` + entity/agent registries, behind a new `require_system_admin` admin endpoint in `build.one.api`. The first section that's genuinely always-current; replaces the "coming soon" stub in `docsSections.ts`.
- [ ] **Web / MCP / Scheduler sections (DERIVED)** — build-time generators: web route table (`App.tsx` / `menuConfig.ts`), MCP tool manifest (`tools/*.py`), scheduler cron inventory (`function_app.py`). Each replaces a "coming soon" stub. **When the Web/API section lands, document the U-005 Expense Coding Cockpit** (`src/pages/expense-coding/`, `Modules.EXPENSES`): the 58999→SubCostCode queue, deterministic suggestions (reason+confidence), human confirm → surgical fail-closed QBO recode. Deferred here only because the per-repo `/docs` Web/API sections don't exist yet (stubs) — there's no matching section to add to today; documented in `build.one.api/CLAUDE.md` in the interim.
- [x] **Mobile reachability** — Delivered as a side effect of U-046: both surfaces now share `secondarySectionsForUser`, so the Reference → Docs section flows into `MoreDrawer` for admins. Pinned by a `BottomTabBar`/`MoreDrawer` regression spec (U-090).
- [x] **Freshness enforcement (hardening)** — **DONE (U-091, 2026-07-19):** report-only drift guard `npm run docs:check:ios` (`scripts/check-ios-docs-fresh.mjs`). Regenerates the iOS manifest via `gen_docs_manifest.py --out -` (writes nothing), strips the volatile `freshness` block (single owner: `VOLATILE_TOP_LEVEL_KEYS`) and diffs the substantive content vs committed `src/docs/ios/ios-manifest.json`, plus byte-compares the 3 narrative `.md`. **Skips (exit 0)** when `../build.one.ios` is absent OR `python3` is unavailable (present-but-not-checkable); **fails loud (exit 1)** on real drift or an unexpected error. Paths/`FILES` shared with the sync writer via `scripts/ios-docs-shared.mjs` (`BUILD_ONE_IOS_DIR` override for CI/testing). Two-tier, mirroring U-085: (a) hermetic `src/docs/ios/iosManifest.test.ts` internal-consistency tripwire runs everywhere incl. iOS-absent CI; (b) this script is the external cross-repo drift guard. **Deliberately NOT wired into `build`/`prebuild`** (would break iOS-absent CI + auto-fix would hide drift). Two-pass ✓ (Codex gpt-5.5/xhigh — 2 findings fixed [corrupt-manifest false-pass, argv guard], re-review PASS; `/simplify` 4-agent applied: redundant stringify gate + double freshness-strip collapsed).
  - **Follow-up (deferred):** nothing invokes `docs:check:ios` automatically — on a lane where the iOS repo IS present, the guard only fires if a human/hook runs it. If a future **iOS-aware CI lane** is stood up, add `docs:check:ios` there to actually gate; and/or a `settings.json` post-sync hook. Left out here as a behavior/infra change beyond this web-only unit.

---

## Contract Labor surface (U-134) — deferred items

- [ ] **Unpark the Generate-Bills surface only after the API guard lands (U-135).** `ContractLaborBills.tsx` / `ContractLaborImport.tsx` / `ContractLaborCreate.tsx` are deliberately tsconfig-parked + unrouted: `POST /generate-bills/{vendor_id}` cannibalizes already-billed feeders on re-run with NO server-side guard (api `bill_service.py:373-433`; memory `feedback_cl_generate_bills_editpath_hazard`). U-135 (board) adds the server guard; only then route the Bills page, gated hard.
- [ ] **[U-134-deferred] `can_delete`-without-`can_update` persona can't single-delete from Edit.** ContractLaborEdit early-returns without `can_update`, so its Delete button is unreachable for that persona (Codex P3, accepted: bulk-delete on the List covers it, and ExpenseEdit sets the same page-level-guard precedent). Revisit only if a delete-only role materializes.
- [ ] **Extract a shared `CurrentUser`/`CurrentUserModule` test fixture.** `contractLaborPermissions.test.ts` is the ~12th verbatim copy of `makeModule`/`makeUser` (flagged by the U-134 `/simplify` reuse pass). One-time extraction to a shared test util; codebase-wide, not per-unit work.
- [ ] **Parameterize the per-entity splat/redirect describe block in `routes.test.tsx`.** U-147 (roles) added the **12th** verbatim copy (was 5th at contract-labor); the sibling `xPaths` + "matches under AppLayout" blocks are at 13. The hand-pinned expected-paths + nav-reachable enumerations are deliberate tripwires and stay; these derived per-entity blocks are not. Collapse into a `describe.each` helper — scheduled work, not "next touch" (that trigger has failed 7 units running).

---

## Expense Coding cockpit (U-005) — enhancements

- [x] **[U-023] Sort + badge the coding queue by suggestion confidence.** Highest-confidence rows (ready to confirm) surface at the top; null/non-finite-confidence rows (no score → need cardholder follow-up) sink to a distinct bottom group; each row header carries a small confidence badge (High ≥0.90 / Medium 0.60–0.90 / Low <0.60 / No score). Web-only, reuses the existing `/api/v1/expense-coding/queue` data (no API change) — all sort/tier logic keyed STRICTLY on `suggestion_confidence` (number|null) and lives in the pure module `src/pages/expense-coding/expenseCodingLogic.ts` (`sortQueueByConfidence`, `confidenceTier`, `formatConfidencePct`, shared `toRatio`), so an in-flight `/expense-coding` endpoint reshape is a one-file re-point. Sort applied via `useMemo` on the query data (never mutates the RQ cache); badge is a self-contained pill styled to match the sibling status badge. Preserves Confirm/Flag/edit + the "selections entry == dirty" invariant + the multi-user cache contract. **DONE (U-023, 2026-07-15):** two-pass ✓ (Codex gpt-5.5/xhigh adversarial — PASS, 0 findings P0–P3; `/simplify` 4-agent applied: index-decoration removal on the sort — native stable `Array.sort`, es2023 target — and a `No suggestion`→`No score` label fix. A shared-`toRatio` dedup with `formatSuggestionHelper` was attempted then REVERTED after the Codex re-review flagged a P3: the /100→*100 round-trip flipped the rounding of half-integer percents there — `formatSuggestionHelper` (percent-native) and `confidenceRatio` (ratio-native) are now deliberately kept separate with a comment saying why; final re-review PASS). tsc 0 + build 0 + **119 vitest** green (incl. 5 cacheCleanup + 9 new sort/tier/badge specs). Deploy = combined web SWA ship, held per web-deploy-ask-first.
- [ ] **Extract a shared base-pill CSS class.** `.status-badge`, `.expense-coding-credit-chip`, and (as of U-023) `.expense-coding-confidence-badge` each repeat the same 5 base pill declarations (`display`/`padding`/`border-radius`/`font-size`/`font-weight`). Deferred from U-023 because `.status-badge` is used across ~10 pages — retrofitting it is out of scope for a web-only badge unit. Extract one base-pill class all three extend (flagged by the U-023 `/simplify` reuse pass).

---

## PWA — Tier 2 / Tier 3 decision gates

- [ ] **JWT-in-localStorage durability on installed PWAs.** Surfaced 2026-06-15 (Phase 1.9). iOS Safari may evict localStorage from standalone PWAs after ~7 days of non-use, silently logging the user out. Documented as a Tier 1 known limitation. Long-term fix is migrating refresh tokens to httpOnly cookies + access tokens to in-memory only; that's a separate auth-durability project with CORS + CSRF implications. Do NOT bundle into PWA Tier 2/3 work — flag it, then decide independently. Triggers: complaints from installed-PWA users about unexpected sign-outs, or once Tier 2 lands and the surface becomes more "app-like" in expectation.
- [x] **Tier 2 — offline reads** shipped 2026-06-15 same session as Tier 1. Persistent React Query cache via `@tanstack/react-query-persist-client` + `idb-keyval` with user-scoped persister key; multi-user safety contract enforced via `clearAllUserScopedStorage` + awaited logout + login reload (5-spec Vitest regression test at `src/auth/cacheCleanup.test.ts`). NetworkFirst SW routes for `/api/v1/get/*`, `/api/v1/list/*`, `/api/v1/lookups*`, `/api/v1/*/by-*`. OfflineBanner shows "Synced X ago". Storage readout in Profile → Appearance. See `docs/pwa-tier2.md`.
- [ ] **Tier 3 decision gate — local-first writes.** Conditional, not committed. Greenlight Tier 3 (~10 weeks server + client) only if (a) >20% of mutations are attempted while OfflineBanner is visible (instrument via telemetry after Tier 2 lands), (b) PMs ask for browser-side offline writes specifically, or (c) Android tablets / Chromebooks come on the roadmap. Otherwise mark "deferred indefinitely" — iOS already owns the offline-write surface for field users. Server-side prerequisite (`dbo.IdempotencyKey` table + middleware on every mutation route, ~3 weeks) has independent value for iOS hardening even if Tier 3 itself is deferred.
- [ ] **Theme-color + manifest colors.** ~~Was the deferred Phase 6 follow-up from the initial mobile-shell work.~~ Folded into PWA Tier 1 Phase 1.2 — `theme_color` is now `#1a1816` (charcoal) and `background_color` is `#f4f2ee` (warm cream) in both `index.html` and the manifest emitted from `vite.config.ts`. Close.

---

## TimeEntry view — auto-save / concurrency follow-ups (deferred)

Surfaced by the Codex Pass-1 review of the TimeEntryView concurrency hardening
(`f1f75c0`, 2026-07-11). The 4 P1 data-loss bugs from that review are **fixed +
shipped**; these are the lower-severity residuals left for later.

- [x] **`useAutoSave` drop-without-reschedule (P2, pre-existing, shared hook). FIXED
  2026-07-12.** `src/hooks/useAutoSave.ts` now serializes saves via a single-in-flight
  runner: a request made while a save is in flight sets a pending flag and a single
  coalesced follow-up runs the latest `saveFn` after the in-flight save resolves
  (loops until nothing pending); `flush()` resolves only after the latest state is
  persisted (awaits in-flight + follow-up); reentrancy-safe (in-flight promise
  published to `inFlightRef` synchronously before `saveFn` runs). Codex round-1 caught
  that the reschedule alone still sent a **stale `row_version`** for row_version-coupled
  callers (follow-up runs before React commits the prior save's `setForm`, and
  `UpdateExpenseById` rejects a stale token → silently-lost edit). Full fix: **Expense**
  (`autoSaveHeader` + `saveAll`) and **TimeEntry** (`autoSaveHeader`) now source
  `row_version` from a `rowVersionRef` updated synchronously on save success, with a
  `useEffect(() => { ref.current = null }, [form?.row_version])` clear so external
  hydrations stay authoritative. Codex PASS after 2 fix rounds (P1/P2/P3 closed;
  Expense Complete path + f1f75c0 guards intact). 6 vitest specs on the hook
  (reschedule / flush-await / no-overlap / mount-skip / reentrancy / ref-visibility).
  **Note:** `BillEdit.tsx` does NOT use `useAutoSave` (manual `saveAll` on Complete) —
  not a caller today; it becomes one when Bills unparks. Coverage boundary: the caller
  `rowVersionRef` edits have no component-level unit test (repo has no
  `@testing-library/react` harness) — covered by tsc (TimeEntry only; `pages/expenses`
  is excluded from `tsconfig.app.json`), build, Codex re-review, and the hook's
  ref-visibility spec.
- [x] **Extract `useSyncedToken(form?.row_version)` — DONE in U-097 (2026-07-20).** The
  `rowVersionRef` + clear-effect + `?? committed` read pattern was duplicated in
  ExpenseEdit + TimeEntryView; extracted to `src/hooks/useSyncedToken.ts` as the
  first step of wiring BillEdit onto `useAutoSave`. TimeEntryView migrated; BillEdit
  auto-save shipped in the same unit. U-132 (2026-07-23) migrated ExpenseEdit — the
  last inline copy. BillCreditEdit deliberately NOT migrated: no auto-save and no
  flush-then-save chain (one header PUT per user action, buttons disabled while
  saving), so there is no pre-commit window for the token to bridge; adopt the hook
  there if/when its Edit gains auto-save or a Complete-flush.
- [ ] **Entry-delete vs in-flight log-save race (P3).** `f1f75c0` disables Delete
  Entry + guards `handleDelete` while any log row is `saving`, closing the main
  window. Residual: the guard is a point-in-time snapshot, so a delete initiated in
  the same tick a log save starts is still theoretically racy. Low severity — revisit
  only if it manifests.

(BudgetLayout / BudgetSidebar nav-retirement is already tracked under **Phase 1B**
below.)

---

## Nav strategy — Phase 1+ (from 2026-06-17 navigation evaluation)

Pattern locked: **Bottom Tab Bar + Hamburger Drawer** with a curated per-role primary-slot resolver. Phase 0 shipped this session (chassis only — no user-visible nav change except the tablet sidebar flip). For the full evaluation see the workflow output at `/private/tmp/claude-501/-Users-chris-Applications-build-one/7b4ffd2a-28ac-4754-9941-d5889b92a8cd/tasks/w0sh0c392.output`.

Project-scoped vs entity-scoped doors decision (2026-06-17): **both, sharing one component.** Each entity (Bills, Expenses, Invoices, BillCredits, Budget, ContractLabor) gets a top-level nav entry (entity-centric flat list with filters) AND a Project detail page tab (project-scoped pre-filtered view). The list component takes `defaultFilter` + `hiddenFilters` + `showProjectColumn` props; top-level call passes nothing, project tab call locks project_id.

### Phase 1A — Project as hub (shipped 2026-06-17)

- [x] **New `src/pages/project/ProjectDetailScreen.tsx`** with tabs: Overview, Budget, Bills (placeholder), Expenses (placeholder), Invoices (placeholder), Labor (placeholder). Lives at `/project/:publicId`. The "project as hub" pattern.
- [x] **`BudgetView` refactor** — extracted `BudgetViewContent({ publicId })` so the same component renders as a route at `/budget/:publicId` AND embedded in the Project → Budget tab. The wrapper still renders at the page route.
- [x] **New `src/pages/project/ProjectList.tsx`** — minimal list (name + abbreviation + status, click to detail). Phase 1B will refurbish to a richer list with filters + customer column.
- [x] **menuConfig: Projects entry** under `section: "financials"`, module `Modules.PROJECTS`, priority 30, icon Briefcase.
- [x] **PM / Owner / AP / AR / Controller / Reviewer / Auditor / Tenant Admin curated slots** updated to `["time", "labor", "projects", "profile"]`. DEFAULT_PRIMARY_SLOTS also updated for unknown-role + system-admin fallback.
- [x] **App.tsx routes**: `/project/list` and `/project/:publicId` inside AppLayout, under ProtectedRoute.
- [x] **menuConfig tests** updated (24/24 specs pass) — covers Projects in curated mappings, in entriesInSection("financials"), fallback for unknown roles.
- [x] **CSS** for `.project-tabs` / `.project-tab` strip — horizontally scrollable, charcoal active state.
- [x] **New `fetchBudgetByProject` + `budgetKeys.byProject`** in `src/api/budget.ts` so the Budget tab can resolve a project's budget without a manual lookup.

### Phase 1B — Vendors + Customers unpark + BudgetLayout retirement (in progress)

- [x] **Unpark `pages/vendors/`** — remove from tsconfig exclude. Verify VendorList / VendorView / VendorEdit / VendorCreate against current API envelope + Tier 2 cache patterns (`/api/v1/get/*` is whitelisted; `/list/*` is whitelisted). Add menuConfig entry under `section: "contacts"`, module `Modules.VENDORS`.
- [x] **Unpark `pages/customers/`** — same pattern as Vendors. Add menuConfig entry under `section: "contacts"`, module `Modules.CUSTOMERS`. Reuse `entityListKey`/`entityItemKey` from `useEntity.ts` for post-mutation `invalidateQueries` (U-041 established this; don't re-hand-roll the key tuples).
- [ ] **(tech-debt) Generic `useEntity` mutation invalidation.** `createEntity`/`updateEntity`/`deleteEntity` are plain async fns that don't invalidate the RQ cache — every `useEntity`-based page (vendors + customers now — 6 hand-rolled call sites; the ~25 still-parked CRUD pages later) must hand-roll `invalidateQueries` after a write (5-min `staleTime` + persisted cache ⇒ stale reads / stale `row_version` otherwise). Consider a `useMutation`-shaped layer or an `invalidateEntity(qc, {listPath, itemPath})` helper so callers stop re-hand-rolling. Non-trivial: ~25 call sites + non-uniform mutation-path→get-path/key mapping (pluralization, `_` vs `-`), so keys can't be mechanically derived. Defer out of any single unpark unit.
- [x] **Retire `BudgetLayout` / `BudgetSidebar`.** Move `/budget/list`, `/budget/create`, `/budget/:publicId`, `/budget/:publicId/edit` into `AppLayout` (currently in `BudgetLayout` sibling chrome). `/budget/:publicId` can either stay as the page route OR redirect to `/project/{budget.project_public_id}` with the Budget tab pre-selected — pick when implementing. — **DONE (U-066, 2026-07-16):** all 4 routes + the `/budget/*` redirect moved into the `AppLayout` group; both layout files deleted. **Decision: `/budget/:publicId` stays the page route.** The redirect option was rejected as unbuildable-as-scoped — `ProjectDetailScreen`'s `activeTab` is `useState`, not URL-driven, so "Budget tab pre-selected" would first require adding URL-param tab state to a shipped screen; the target also needs `budget.project_public_id`, so it could not be a static `<Navigate>` (async resolve + loading flash on every budget link); and it would break the `BudgetView → /budget/{id}/edit?rev={co} → back` round-trip. The reuse it would have bought already exists — `ProjectDetailScreen` renders `BudgetViewContent` from `BudgetView.tsx`, one component, two mounts. **Load-bearing detail:** `budget.css` was imported *only* by `BudgetLayout`, so the retirement had to relocate that import to `BudgetList`/`BudgetView`/`BudgetEdit` (not `BudgetCreate` — it uses no budget selectors) or every budget style would have orphaned, including in the project Budget tab, which was styled only by BudgetLayout's static import.
- [x] **Wire the `more` slot to BottomTabBar.** (U-046, 2026-07-16) Bottom pill ends with a "More" slot that opens `MoreDrawer`, shown only when `secondarySectionsForUser(me)` returns ≥1 section. Pill is 4 primary + 1 More. Also fixed the same bug on desktop: `AppSidebar`'s local `SECONDARY_SECTIONS` omitted `contacts`, so Vendors/Customers were URL-only on **every** breakpoint, not just mobile. Both surfaces now share one `secondarySectionsForUser` helper (which also retired AppSidebar's latent Projects double-listing).
- [x] **Refurbish the live `pages/project/` ProjectList** — **DONE (U-067, 2026-07-17):** added client-side name/abbreviation search (pure, tested `projectListFilters.ts` with a mutation-verified no-mutation contract) to the routed singular `src/pages/project/ProjectList.tsx`, kept on the ios-primitive track (NavHeader/SectionCard/ListRow — NOT DataTable). Deleted the unrouted **plural** `src/pages/projects/ProjectList.tsx` (name-collision duplicate, zero importers). Scope corrected mid-unit: the assignment named the plural dead file; the live surface is the singular one. **Status filter dropped** (all 135 projects are `Status='active'`; free-text NVARCHAR(50), no enum). **Customer column deferred → U-071**: `Project` payload carries only `customer_id`; client-side resolution is `Modules.CUSTOMERS`-gated (16/21 humans — every PM/Field Crew/Intern — lack it), so the proper fix is a server-side `customer_name` join (api+web, low) done when api is free.
  - **Follow-up (rule-of-three, deferred):** `.project-search-input` (`index.css`) is a byte-for-byte clone of `.labor-filter-input` — the 2nd hand-rolled ios-track search field. Extract a shared `SearchField` primitive into `src/components/ui/` + one `.ios-search-input` class and migrate labor + projects onto it **when a 3rd ios-track list search field appears** (that unit can safely touch `LaborList.tsx`; this one deliberately did not). Do NOT take the cheap fix of putting `labor-filter-input` on the Projects page — that's invisible cross-page coupling, worse than a visible duplicate.
  - **Follow-up (deferred):** filters use local `useState`, not URL params. Correctly proportionate today — `ProjectDetailScreen`'s back button hard-navigates to bare `/project/list` (no `?q=`), so URL params would restore nothing on the primary back path. Only worth it as a coordinated pair with switching that back button to `navigate(-1)`, if bookmarkable filters are ever wanted.
  - **Remaining Phase-1B port:** the plural `pages/projects/{View,Edit,Create}.tsx` stay parked (tsc-excluded; no `/project/create` or `/project/:id/edit` route exists) — port them into `project/` or delete the folder as a separate unit.

#### Deferred out of U-046 (found during its review — none are regressions from it)

- [x] **Budgets is unreachable from nav — same bug class U-046 just fixed.** `Modules.BUDGETS` exists and `/budget/list|create|:publicId|:publicId/edit` are all routed, but there is **no `MENU_ENTRIES` entry for budgets**. It is linked only from `BudgetSidebar`, which renders only once you are already on `/budget/*` — so Budgets is reachable **only by typing the URL**. Verified 2026-07-16. Adding the entry is a behavior change + a per-role curation decision, so it was left out of U-046. Pairs with the BudgetLayout retirement item above. — **DONE (U-055, 2026-07-16):** entry added at `section: "financials"`, `priority: 35` (Projects 30 → Budgets 35 → Bills 40 → Expense 45), icon `Calculator`, `Modules.BUDGETS` / `can_read`, secondary-only (pill is at its ≤5 cap). Flows to both surfaces via `secondarySectionsForUser` with zero consumer changes.
- [x] **Nav has no shell-awareness — `MenuEntry` declares `route` but not which layout owns it.** U-046 newly exposes More → Financials → **Bills** on a phone, but `/bill/*` renders under `BillLayout`, whose `.sidebar` is a hard `width: 220px; flex-shrink: 0` with **no media query** — so a 220px sidebar eats a 430px column, with no bottom pill and no More drawer. Not a dead-end (the `← Field App` link escapes) and not a regression (AppSidebar already exposed Bills at ≥768px), but it is a real phone→desktop-chrome jump. Right-depth fixes: a `shell`/`surface` field on `MenuEntry`, or fold Bill/Budget into the responsive `AppLayout` (see the BudgetLayout retirement item). Do not paper over it per-entry. — **DONE (U-079, 2026-07-17):** took the second fix for **Bills** too, closing the item. All 5 bill routes (`/bill/list`, `/bill/create`, `/bill/:publicId`, `/bill/:publicId/edit` + the `/bill/*` redirect) moved into the `AppLayout` group; `BillLayout.tsx` + `BillSidebar.tsx` deleted, and `Header.tsx` (imported *only* by BillLayout) with them. **Decision: `/bill/:publicId` stays the BillView page route** — it *is* the AP completion page, nothing to redirect to (mirrors the U-066 BudgetView call). Gate-1 confirmed BillSidebar carried nothing AppLayout lacks: its "Bills" link is already a `menuConfig` entry, and the `← Field App` back-link + top-bar Sign Out are subsumed by AppLayout's always-present responsive nav (Time is a primary slot; sign-out via Profile). `routes.test.tsx` flipped `branchHasLayout(branch, BillLayout)` → `AppLayout` and gained the bill route-precedence suite. No shell still on sibling chrome. Orphaned legacy CSS (`.app-layout`/`.app-main`/`.app-content`, `.header*`) + the already-dead `Sidebar.tsx` left for a separate housekeeping sweep (they were deliberately-retained legacy per the index.css comment; deferring keeps this diff tight).
- [ ] **`Sheet.tsx` has no dialog semantics or focus management.** No `role="dialog"`, `aria-modal`, `aria-labelledby`, focus move-in, focus trap, or focus restore. Esc + backdrop dismiss already work. Deferred from U-046 by explicit decision: `Sheet` is shared by 7 consumers (MoreDrawer, TimeLogForm, EditEntryScreen, ChangePasswordSheet, UserPickerSheet, ProjectPickerSheet, SubCostCodePickerSheet), so this is its own reviewed unit with a regression pass over all of them. **Note the live overclaim:** `BottomTabBar`'s More button advertises `aria-haspopup="dialog"` while the popup is generic divs — fix them together. (Codex P3 on U-046.)
- [ ] **Two definitions of "active" now coexist in the bottom pill.** Primary tabs use `NavLink`'s default matching, which is *false* on detail pages (`/time-entry/123` does not start with `/time-entry/list`), while More uses `isEntryRouteActive` base matching, which *is* true on `/vendor/456`. Net: on a vendor detail page More is lit; on a time-entry detail page nothing is lit. More's rule is arguably the correct one and the primary tabs are the pre-existing gap. UX call, not a cleanup — decide before extending either.
- [ ] **`MAX_PRIMARY_SLOTS` vs the More slot (latent).** Widest role today is 4 primary + More = 5, exactly at the cap. A 5th curated primary slot would make the pill 6. Truncating is NOT the fix: `section: "primary"` entries (Time/Labor) are not in `SECONDARY_SECTION_SPECS`, so a truncated primary entry would vanish from every surface. Decide the overflow rule when a role actually needs a 5th slot. CSS degrades gracefully today (`overflow-x: auto`).
- [ ] **`BillSidebar` is a hand-maintained nav surface outside `menuConfig`.** _(Was `BillSidebar` / `BudgetSidebar`; `BudgetSidebar` is gone — U-066 deleted it. `BillSidebar` is the last one.)_ It hardcodes its own `<NavLink>` list — the same shape as the pre-U-046 `AppSidebar` that caused this bug. Fold into the menuConfig model when retiring those layouts.
- [ ] **(cosmetic) `menuConfig.test.ts` mixes conventions.** 14 pre-existing `makeModule("Time Tracking", …)` raw literals sit alongside the newer `Modules.*` constants. Note the tension before mechanically converting: in *nav code* the constant is strictly better (a typo fails compilation), but in a *test* a raw literal pins the real wire value, whereas `Modules.X` on both sides of the assertion is tautological. Decide intent rather than sweeping.

#### Deferred out of U-055 (found during its two-pass review — none are regressions from it)

- [ ] **No tripwire for "routed but not in nav" — the exact U-055 bug class, still unguarded for the NEXT entity.** U-046's tripwire asserts every non-primary `MENU_ENTRIES` section appears in `SECONDARY_SECTION_SPECS`; it would NOT have caught U-055 (budgets had no entry at all), and a route-coverage tripwire would NOT have caught U-046 (`/vendor` + `/customer` were covered while still unreachable). **Two failure modes, two guards — this is the missing second one.** Feasibility was measured, not assumed: parsing `src/App.tsx` and grouping by **first path segment** yields 14 namespaces, 10 covered by a `MENU_ENTRIES` route and **4 legitimately nav-less** (`/`, `*`, `/login`, `/user`) — a small, stable allowlist. Per-namespace is the right granularity; per-route (41 paths) would misfire on `/budget/create`, `/profile/details/:fieldKey`, etc. Pre-U-055, `/budget` would have shown as UNCOVERED. **Two design calls before building:** (1) it needs a **vacuous-pass defense** (assert ≥14 namespaces found) — if `App.tsx` ever moves to `path={ROUTE_CONST}` the regex silently matches nothing and the test goes green while guarding zero, which is worse than no test; (2) it reads another file's *source text*, unlike the in-module constant-vs-constant existing tripwire, so it likely belongs in its own file, and the allowlist is a **policy** call about which namespaces may be nav-less — deserves review, not smuggling.
  - **⚠️ RE-SCOPED by U-066 (2026-07-16) — do not build the regex design.** The route tree is now exported as a real data structure: `appRouteTree` in **`src/routes.tsx`** (a module that deliberately imports no shell/provider components, so consumers need no mocks). `createRoutesFromElements(appRouteTree)` + `matchRoutes` gives the routes directly — see `src/routes.test.tsx` for the working pattern. Measured off the real structure it reproduces this item's regex-derived numbers exactly: **41 paths / 14 namespaces**, nav-less set exactly `/`, `*`, `/login`, `/user`. **Both design calls above are now moot:** (1) the vacuous-pass hazard is *structural*ly gone — JSX props evaluate, so `path={ROUTE_CONST}` lands in `RouteObject.path` as a real string; a regex can silently match nothing, a data structure cannot; (2) there is no source-text reading — it's an ordinary typed import, so the own-file question dissolves. **What remains is only the allowlist policy call** (which namespaces may legitimately be nav-less) — still deserves review, still don't smuggle it.
- [x] **(rule-of-three) Per-entry RBAC test triad — DONE in U-094.** Bills was the 4th entity, the designated trigger; collapsed to `it.each(ENTRY_MODULE_ROWS)` (raw wire-name literals kept, inert `role:` fields dropped). Original rationale, still useful for the next table candidate: ~14 lines per entity asserting *data* ("entry X exists and is wired to module Y"), not logic — the three `canSeeEntry` branches they exercise are already covered generically by the `time`/`labor` tests. The `role` field in each is inert (`canSeeEntry` never reads `me.role`), and the admin-bypass leg is near-tautological (`canSeeEntry` returns at `if (me.is_admin) return true;` before reading `entry.module`, so it passes for any existing entry). An `it.each` over `[id, moduleName]` pairs collapses 9 tests → 3, making the next entity a one-line row. **Deliberately deferred from U-055:** the fix requires rewriting the two pre-existing vendors/customers blocks (~45 lines of churn on a 10-line declarative unit), and the explicit test names read well in output. Phase 1+ adds Invoices / Bill Credits / Employee Labor — take this on at the 4th entity. Keep the raw `makeModule("Budgets")` literals when collapsing (see the convention note above).

### Phase 2 — Tier 3 financial surfaces (3-4 weeks, web-only)

### Phase 2 — Tier 3 financial surfaces (3-4 weeks, web-only)

- [x] **Unpark Bills — DONE in U-094 (2026-07-20), premise was stale.** The Bills surface was never route/nav-parked: routes wired under AppLayout since U-079, `menuConfig` financials entry since `265695b` (2026-06-25), both live in prod; the list API 500 was fixed by U-089. U-094 added the missing piece — `bills` in the AP Specialist + Controller primary pill (kept at 4 curated slots: BottomTabBar appends More, so 5 curated would render a 6-item pill; a `PRIMARY_SLOTS_BY_ROLE` invariant tripwire now enforces ≤ MAX−1 + id resolution) — and source-verified LIST/VIEW against the current API contract (envelope `{data,count}`, `?is_draft=` filter composition, line-item/attachment routes). The full bills-first pill re-order (`["bills", "expenses", "invoices", "more", "profile"]`) waits for Expenses/Invoices to unpark. **Follow-up review units (live surfaces, never two-passed):** Bill CREATE (drop-zone/folder intake, DI prefill, `can_complete` gating) · Bill EDIT + auto-save (flush-on-Complete / guard-on-Delete race class) · Bill COMPLETE web path (vs U-065 durable CompletionJob) · ReviewTimeline interactive surface · `/accept` data check: prod `RoleModule` grants Bills `can_read` for AP Specialist/Controller · (P3) BillList sort is client-side over the current page only · (pre-existing, Codex U-097) BillEdit state (form/lineItems) is not reset on a /bill/:id/edit → /bill/:other/edit history navigation — page shows the prior bill until reload; writes are corruption-proof (RowVersion guard) but the staleness needs a param-change reset.
- [x] **Unpark Expenses — DONE in U-124 (2026-07-23).** Routed `/expense/list|create|:publicId|:publicId/edit` + `/expense/*` redirect mirroring Bills; nav = TWO financials entries (Vendors/Compliance precedent): **Expenses** → `/expense/list` (repointed from the old lone "Expense" entry) + **Expense Coding** → `/expense-coding` (cockpit keeps its own entry, zero outbound links so demoting it would bury a daily AP tool). Route tripwires (U-066 expected-paths + U-077 nav pins) updated in the same diff; `expensePermissions.ts` mirrors `billPermissions.ts` (Edit refuse-renders without `can_update` since auto-save PUTs; Complete = `can_update && can_complete`; Create page + List create-button gated `can_create`; no entity-level Delete/Submit buttons exist on the surface). Fixed the `setOrigLineItems([])` delete-tracking bug (tracking now resets to the SAVED id set, BillEdit U-097 class) + regression test. Pill slots deliberately untouched (5 curated + More would trip `MAX_PRIMARY_SLOTS`; pill re-order still waits for Invoices). **Remaining from the original item:** ~~migrate ExpenseEdit onto `useSyncedToken` (`src/hooks/`)~~ — DONE in U-132 (2026-07-23), with a chained-save stale-token regression test. Also (pre-existing, Codex U-124 P2, exact twin of the Codex U-097 BillEdit finding above): ExpenseEdit form state is not reset on a /expense/:id/edit → /expense/:other/edit history navigation — stale display until reload; writes corruption-proof (RowVersion → 409). Fix both pages' param-change reset in one unit.
- [x] **Unpark Invoices (browse) — DONE in U-128 (2026-07-23).** Routed `/invoice/list` + `/invoice/:publicId` + `/invoice/*` redirect; ONE financials nav entry **Invoices** → `/invoice/list` (priority 47, after Expense Coding). NO create/edit routes — invoices are QBO-first (created in QBO, pulled local). `InvoiceCreate.tsx` / `InvoiceEdit.tsx` stay tsconfig-parked; List has no create button; `InvoiceView` is browse-only (`DetailView` editPath omitted). U-066/U-077 tripwires + `ENTRY_MODULE_ROWS` + financials-order pins updated in the same diff. Pill re-order / AR primary slot still waits on the per-role mapping unit. **Do the two U-127 altitude extractions before wiring Create/Edit (if ever):** (1) collapse the per-entity route-test blocks in `routes.test.tsx` into a `describe.each` over base segments; (2) extract shared `syncLineItems` from Bill/Expense/BillCredit Edit pages; (3) consolidate the byte-identical `fmtMoney` clones in Bill/Expense/BillCredit/Invoice View pages onto `MoneyCell.tsx`'s `formatMoneyParts` (U-128 Pass-2 observation).
- [ ] **Invoice Create/Edit unpark (U-128 deferred).** BLOCKED on bidirectional QBO invoice sync being wired end-to-end in `build.one.api` (`sync_to_qbo_invoice` enqueue + `_sync_billed_status_to_qbo` — currently dormant/no-op; local complete would double-bill: `IsBilled` flips locally while QBO `BillableStatus` stays billable). **Also:** `InvoiceEdit.tsx` carries the U-124/U-127 delete-tracking twin bug (`setOrigLineItems([])` reset after save instead of refresh-by-`public_id`) — MUST be fixed + regression-tested before any Edit unpark.
- [x] **Unpark BillCredits — DONE in U-127 (2026-07-23).** Routed `/bill-credit/list|create|:publicId|:publicId/edit` + `/bill-credit/*` redirect; ONE financials nav entry **Bill Credits** → `/bill-credit/list` (priority 42, between Bills and Expenses; More-drawer only per this item's original note, no primary-slot change). U-066/U-077 tripwires + `ENTRY_MODULE_ROWS` + financials-order pins updated in the same diff; `billCreditPermissions.ts` mirrors `expensePermissions.ts` (Edit refuse-renders without `can_update`; Complete = `can_update && can_complete`; Create page + List create-button gated `can_create`; no auto-save on this Edit, no entity-level Delete/Submit buttons). Fixed the `setOrigLineItems([])` delete-tracking reset bug (U-097/U-124 class) + regression tests incl. component-level permission-gate wiring (beyond the Expense template). `/accept` data check owed: prod `RoleModule` grants Bill Credits `can_read` for AP Specialist/Controller (U-094/U-124 precedent).
- [x] **Unpark EmployeeLabor — DONE in U-136 (2026-07-23).** Routed `/employee-labor/list|:publicId|:publicId/edit` + `/employee-labor/*` redirect; ONE financials nav entry **Employee Labor** → `/employee-labor/list` (priority 49, after Contract Labor). **Create PARKED** (`/employee-labor/create` → list redirect; `EmployeeLaborCreate.tsx` stays tsconfig-parked): manual rows with arbitrary period/status are a billing footgun — EL rows are auto-aggregated from TimeEntry submit. Edit's manual "Invoiced" status option also parked (`employeeLaborStatus.ts` `EDITABLE_STATUS_OPTIONS`): `invoiced` is set only by API `complete_invoice`; a manual flip would orphan the row from invoicing. `employeeLaborPermissions.ts` mirrors the API gates (Edit refuse-renders + View edit link on `can_update`; View delete on `can_delete`); deliberately NO submit-for-review action (the api review router has no EL routes — TimeEntry submit is the upstream). U-066/U-077 tripwires + `ENTRY_MODULE_ROWS` + financials pins updated in the same diff; also fixed + regression-tested the param-change stale-form hazard (`formSeedId` guard — see the class item below). Unpark Create only behind a guarded API path (U-135 class). `/accept` data check owed: which prod roles hold Employee Labor grants.
- [ ] **Param-change form-reset class (U-136 Pass-2 altitude).** The stale-form-on-`:publicId`-change hazard (Codex U-097 BillEdit + Codex U-124 ExpenseEdit notes above) is a property of the shared render-seed idiom, not of two pages: ~24 Edit pages use `if (item && !form)` (Vendor/Customer/Project/Taxpayer/Invoice/BillCredit/Bill…). U-136 fixed `EmployeeLaborEdit` with a `formSeedId` guard keyed on `item.public_id` (+ a fails-without-fix regression test) — promote that to a shared seed-keyed form hook next to `useEntityItem` (U-132 `useSyncedToken` promotion precedent) and migrate the class in one unit, superseding the per-page "fix both pages" note in the U-094/U-124 items. Also from the U-136 reuse pass: `makeModule`/`makeUser` test fixtures are **16** verbatim copies (U-138 added `costCodePermissions.test.ts`; U-145 `organizationPermissions.test.ts` + `organizationRouteParams.test.tsx`; U-147 `rolePermissions.test.ts`) and the api-client mock + microtask-flush test harness now **9** copies (U-138 added `CostCodeEdit.test.tsx`, U-141 `SubCostCodeEdit.test.tsx`, U-143 `CompanyEdit.test.tsx`, U-145 `OrganizationEdit.test.tsx`, U-147 `RoleEdit.test.tsx`, U-149 `PaymentTermEdit.test.tsx`, U-151 `EmployeeEdit.test.tsx`) — one-time extraction to a shared `__testutils__` module. NB the "when next touched" trigger has now fired on **nine** consecutive unpark units (U-127/U-136/U-138/U-141/U-143/U-145/U-147/U-149/U-151) without action — schedule the extraction as its own test-only unit instead of waiting for the next touch. U-143 (`CompanyEdit`), U-145 (`OrganizationEdit`), U-147 (`RoleEdit`), U-149 (`PaymentTermEdit`), and U-151 (`EmployeeEdit`) added the 4th–8th verbatim `formSeedId` copies. **Harness divergence (U-147):** `RoleEdit.test.tsx` captures `NavCapture`'s navigate fn in a `useEffect` because render-time assignment errors under the enabled `react-hooks/globals` ESLint rule — the five older copies (`OrganizationEdit`/`CompanyEdit`/`CostCodeEdit`/`EmployeeLaborEdit`/`SubCostCodeEdit`) still assign during render and all fail lint; the roles copy is the canonical form for the extraction to adopt (U-149's `PaymentTermEdit.test.tsx` adopts it — 2nd lint-clean copy). **U-141 variant note (Pass-2 altitude):** `SubCostCodeEdit` is the 3rd `formSeedId` copy but NOT verbatim — its seed is additionally gated on a second async source (`!costCodesLoading`, to prefill `cost_code_public_id` via a client-side `cost_code_id`→`public_id` join) plus a late-prefill patch block for the warm-stale-cache leg. The shared seed-keyed hook must support seeds joined against a second async source, or it will silently flatten this page (regressing the FK prefill). True altitude for the FK-prefill class is cross-repo: the API sub-cost-code payload returning `cost_code_public_id` directly would delete the join, the seed gate, and the patch block (and `EmployeeLaborEdit`'s blank-prefill variant) — log as an api unit when the class migrates.
- [ ] **[U-138 Pass-2 follow-ups] Two more untracked copy axes in the unpark pattern.** (1) *Per-entity routeParams spec*: `costCodeRouteParams.test.tsx` is a 122-line token-rename copy of `vendorTypeRouteParams.test.tsx` (U-141 added the 3rd: `subCostCodeRouteParams.test.tsx`; U-143/U-145/U-147/U-149/U-151 brought it to **8** — company/organization/role/paymentTerm/employee) — the spec is already table-driven internally, so a shared `assertRouteParamFetch(Component, routePath, initialEntry, expectedGetPath)` helper (home: the now-real `src/__testutils__/`, created by U-152) would make each future unpark add one table row, not one file. (2) *Permission-refusal JSX*: the `page-error` + Back-button refuse-render block is now inline in 12+ pages (vendor-types, bill-credits, expenses, budgets, employee-labor, cost-codes ×2, sub-cost-codes ×2 via U-141, roles ×2 via U-147, payment-terms ×2 via U-149, employees ×2 via U-151) with real copy drift (EmployeeLaborEdit "You don't…" vs everywhere else "You do not…") baked into per-entity test assertions — extract a ~10-line `<PermissionRefusal message backTo backLabel>` in `src/components/` and migrate the class in one behavior-preserving unit. Also from the U-138 reuse pass: `adminUser()`/`sampleCostCode()` are duplicated across the two cost-codes test files (first folder with both a routeParams and an Edit spec; U-141's sub-cost-codes is the 2nd) — fold into the same extraction. (3) *FK-dropdown idiom (U-141 reuse pass)*: two competing idioms now coexist — `useLookups` + lookup-`id` join (EmployeeLaborEdit, the documented convention) vs `useEntityList` over the full entity list (SubCostCode Create/Edit, chosen because `useLookups` exposes no `error` and `LookupCostCode` lacks `id`). Converge one way in a small shared-infra unit: add `error` surfacing to `useLookups` + `id` to `LookupCostCode` (Phase-7b `LookupSubCostCode` precedent), then repoint the sub-cost-code pages — or bless the entity-list pattern and document it.
- [ ] **[U-145 Pass-2 follow-ups]** (1) *Org-companies payload enrichment (api unit)*: `GET /api/v1/get/organization_companies/organization/:id` returns no `company_name`, so the organization pages carry a best-effort COMPANIES-gated `/get/companies` fetch purely for names (raw-id fallback when denied) — centralized in `src/pages/organizations/useOrganizationCompanies.ts`. Returning `company_name` in the payload deletes that second fetch + fallback (same cross-repo class as the U-141 FK-prefill note above). (2) *Deferred Codex P2 (U-145 round 1)*: `OrganizationEdit` refuse-renders on ORGANIZATIONS `can_update`, so a hypothetical can_create/can_delete-only grant cannot reach the org↔company link UI the API allows (`organization_company` router create=:28 can_create, delete=:124 can_delete). Deliberate compound shape matching the CompanyEdit precedent; revisit only if such a grant materializes.
- [ ] **[U-147 Pass-2 follow-ups] Template-wide efficiency pattern, fix at the template not per-entity.** (1) Entity Edit pages `await invalidateEntity({ itemPath })` after save while the page still holds an active observer on that key — `refetchType: 'active'` fires and awaits an extra `GET` of the row between PUT and navigate, on data the PUT response already returned. Cheaper shape: `setQueryData(entityItemKey(itemPath), putResponse)` + invalidate the (unmounted, hence free) list only. Applies to RoleEdit/OrganizationEdit/CompanyEdit and the invalidating fleet — one behavior-preserving unit, verify PUT response shape matches the GET envelope per entity. (2) The refuse-render gate sits below the entity-fetch loading gate, so a denied user pays a wasted GET + full RTT before seeing the refusal; skipping the fetch wants an `{ enabled }` passthrough on `useEntityItem` (shared-hook change). (3) View-page delete calls `removeEntity` while the item observer is still mounted — React Query can rebuild + refetch the deleted row (404) before `navigate` unmounts; navigate-first (or reconcile after) is airtight. All three are U-145-template-identical, deliberately NOT fixed in U-147 to avoid template divergence. — same list component as top-level entries, with `defaultFilter={{ project_id }}` + `hiddenFilters={['project_id']}` + `showProjectColumn={false}`.
- [x] **[U-151] The `does not clobber in-progress edits` spec is FALSE-GREEN in three SHIPPED units — fix the copies.** — DONE (U-152, 2026-07-25). Root-caused by mutation at Gate 1, fixed test-only (zero source/runtime change). **Two independent defects, either one alone fatal:** (A) the spec typed via `input.value = X; dispatchEvent(new Event("input"))`, which also updates React's `_valueTracker` so React SUPPRESSES `onChange` — the edit reached the DOM but never component state; (B) it flushed with microtasks only, which never advances `vi` fake timers, so React Query's refetch never propagated and the component still rendered the STALE item at assertion time. The final assertion therefore read back the DOM value the test itself wrote = tautology. **Premise corrected at Gate 1: three copies were false-green, not four** — `EmployeeEdit.test.tsx` (U-151) already caught the mutant; it was converged onto the shared harness anyway (it passed *incidentally*, via `act()`'s flush, not by construction). Fixed by creating `src/__testutils__/` — `domEvents.ts` (`setInputValue`, native-value-setter typing), `flush.ts` (`flushUntil`, timer-advancing), `formSeedGuardHarness.ts` (`assertGuardSurvivesSameRowRefetch`). The harness is **anti-vacuous by construction**: a `RefetchWitness` sibling subscribes to the SAME `useEntityItem` query key and must render the refreshed `row_version` BEFORE the edit-survival assertion runs, and the fixture preconditions (refreshed values must differ from both the seeded and the edited value) are machine-checked rather than commented. **em-verified: all 4 specs go RED under the canonical mutant** (`useEffect(() => { if (item) setForm(...) }, [item])`) and under a `row_version`-keyed guard; tsc 0 + 564/564 vitest.
- [ ] **[U-152 follow-up] `ExpenseEdit.test.tsx:504` is the LAST copy of the broken typing idiom — and its spec is unproven.** `refInput.value = "EXP-EDITED"; refInput.dispatchEvent(...)` is now the only remaining raw-`.value` edit simulation in `src` (grep-verified at U-152). Deleting the entire typing block leaves the spec — *"saveAll after flushAutoSave sends the token returned by the flush PUT, not stale form state"* — **still GREEN (5/5)**, because `useAutoSave.flush()` deliberately ignores `enabled` (`src/hooks/useAutoSave.ts:88-91`), so the PUT chain fires with or without a user edit. Weaker than the 4 clobber specs (its rv-1→rv-2 token-chaining assertions do still discriminate), but its NAMED premise is unproven and it is the live copy-paste vector. Unit: repoint at `src/__testutils__/domEvents.setInputValue`, re-mutation-test, then strengthen it or rename it to what it actually proves. NB `src/pages/expenses/**` is tsconfig-excluded (parked) but DOES run under vitest.
- [ ] **Per-role primary slot mapping update** in `PRIMARY_SLOTS_BY_ROLE` for AP / AR / Controller / Reviewer / Auditor / Tenant Admin.

### Phase 3 — Admin section (2-3 weeks, web-only)

- [ ] **Unpark Users / UserList / UserCreate, Roles, Modules, Organizations, Companies, Integrations, Review Statuses.** Add admin section entries with `requiresAdmin: true` where appropriate. Most lives in sidebar Admin section (collapsed by default).
- [ ] **Reference Data sub-group inside Admin.** Cost Codes / Sub Cost Codes / Payment Terms / Review Statuses / Address Types / Vendor Types / Taxpayers. Collapsed by default; phone access only via deep link.
- [ ] **UserRole / RoleModule / UserModule / UserProject** as standalone Admin entries for bulk operations (inline editors on User/Role pages handle the common case).

### Phase 4 — Dynamic menuConfig + IsNavigable (1 week, requires API change)

- [ ] **API: `dbo.Module.IsNavigable BIT NOT NULL DEFAULT 1`.** Lets ops hide modules from nav without revoking permissions. ~2 hours server.
- [ ] **Refactor `menuConfig` to consume `me.modules` directly.** Render any module with `can_read && is_navigable && route !== null`. Hardcoded `MENU_ENTRIES` collapses to ~5 (Profile, sub-screens of Profile, anything without a Module). Per-role primary slot resolver becomes a priority map keyed on module name.

### Phase 5 — Deferred (do not attempt until earlier phases prove out)

- [ ] **Cross-entity Search.** Multi-week — needs API + indexing decision. When built, lands as a header bar input (cmd-K) — NOT a menuConfig entry.
- [ ] **Dashboard.** Defer until Phase 3 telemetry shows what roles open first. Too many audience-specific KPIs to spec prematurely.
- [ ] **Inbox / Email Messages.** Gated on the larger inbox rebuild per umbrella MEMORY.md — no timeline.
- [ ] **BuildOneTray revival.** Separate UX project.

### Anti-patterns to avoid (codebase-specific)

- Don't ship a 5+ entry flat bottom tab bar. Stay at 5 slots max. Capacity grows via the More drawer.
- Don't put financials as Profile sub-screens just because they fit alphabetically under "Account."
- Don't spin up another `BudgetLayout`-style sibling chrome.
- Don't gate menuConfig entries on hardcoded role name strings. Use module read perms or `is_admin`.
- Don't add Reports / Inbox menuConfig entries as placeholders — both need API support that doesn't exist.
- Don't touch `NavHeader.tsx` per-page back/title conventions — orthogonal to top-level nav.
- Don't revive `BuildOneTray` or `src/agents/` tree as part of nav work — separate UX project.

---

## Auth / authz hardening — API repo follow-ups (from 2026-06-16 evaluation)

The full auth+authz evaluation lives in the workflow transcript at
`/private/tmp/claude-501/-Users-chris-Applications-build-one/7b4ffd2a-28ac-4754-9941-d5889b92a8cd/tasks/wuqbrmpaw.output`
(audit + risk pass across web/api/ios). Web-side Phase 0 (redirectToLogin
cleanup, Sign out of all devices, agent transcript scoping) shipped this
session. The items below are API-repo work, in priority order per the
user's answers to the open questions.

### Phase 0.5 — refresh cookie never reaches the API (SameSite=Lax + cross-site) — ✅ RESOLVED via Path B (2026-07-12)

> **UPDATE 2026-07-10 — Path A REVERTED; Path B underway; deploy HELD at Gate 2.** Path A (SameSite=None) was built then reverted: Codex Pass-1 confirmed a **P1** — even once the cookies transmit cross-site, the CSRF double-submit still 403s because the web reads `token.csrf` from `document.cookie` (`src/api/client.ts:68`) but that cookie is host-only on the API host and unreadable from `app.bld-one.com`. Path A just trades a 401 for a 403; the hourly logout persists (plus SameSite=None is third-party-cookie auth → Safari ITP / Chrome phase-out fragility). Pivoted to **Path B (custom API domain `api.bld-one.com`)** → app+api share eTLD+1 `bld-one.com` → same-site → Lax works AND the CSRF cookie is readable (scoped `Domain=bld-one.com`). **✅ DONE 2026-07-12:** API deployed (`master` — `api.bld-one.com` + managed cert + CSRF `Domain=bld-one.com` cookie), then the web flipped `VITE_API_BASE_URL` → `https://api.bld-one.com` (`main` `9654743`) + SWA-deployed to `app.bld-one.com`. Verified: the live bundle references only `api.bld-one.com`; CORS preflight from `app.bld-one.com` → `api.bld-one.com/auth/refresh` returns 200 with Allow-Credentials + Allow-Origin `https://app.bld-one.com` + Allow-Headers `x-csrf-token`. Same-site → SameSite=Lax + CSRF double-submit both work. **Human check pending:** confirm a live browser session stays logged in past ~60 min. Full runbook + API detail: `build.one.api/TODO.md` "Auth cross-site 'hourly re-login' fix → Path B". The original Path A/B write-up below is retained for context — Path A is now moot.

- [ ] **Fix the refresh-cookie cross-site drop.** Surfaced 2026-06-16. User reports being forced to log in roughly every hour. Root cause confirmed by reading `entities/auth/api/router.py:95-120` + `.env:18`: the three auth cookies (`access_token`, `refresh_token`, `token.csrf`) are all set with `samesite="lax"`. The web is hosted at `app.bld-one.com`; the API is at `buildone-esgaducjg4d3eucf.eastus-01.azurewebsites.net`. These are different sites (different eTLD+1). `SameSite=Lax` allows cookies on top-level navigation but **NOT on `fetch()` cross-site requests**. So every time the access token expires (60 min per `access_token_expire_seconds=3600`), the web POSTs to `/api/v1/auth/refresh` → browser refuses to send the refresh cookie → server returns 401 → user bounces to /login. The 30-day refresh token TTL is irrelevant because the cookie never reaches the server.

  **Two paths:**
  - **Path A — change SameSite to None (immediate).** Edit `entities/auth/api/router.py:95-120` to `samesite="none"` (keep `secure=True` — required when SameSite=None). Verify the CORS config has `allow_credentials=True` AND `allow_origins=["https://app.bld-one.com"]` (or a list including it — wildcard `*` is incompatible with credentials). Test: log in, wait 60+ min or manually expire the access token, confirm a background API call refreshes instead of bouncing to /login. ~1 hour incl. verification.
  - **Path B — Custom API domain (proper long-term fix).** Move the API to `api.bld-one.com`. Then both surfaces share eTLD+1 = `bld-one.com` → same-site → `SameSite=Lax` works as-is. Already on this TODO as the deferred "Custom API domain (api.bld-one.com)" item. Resolves this issue as a side effect AND avoids the wider `SameSite=None` cookie surface.

  **Recommendation**: Path A now for immediate relief, then Path B when the custom-domain work happens (which moots Path A's wider cookie permission).

  **Verification once Path A lands**: open DevTools → Application → Cookies on `buildone-…azurewebsites.net` after a fresh login. The three auth cookies should show `SameSite=None`. Then on the Network tab, trigger a 401 + refresh; the refresh request's Cookie header should include the refresh cookie. Today that header is empty.

### Phase 1 — Active Sessions surface + agent audit-trail (yes/strong appetite)

- [ ] **Active Sessions API endpoints.** Build out:
  - `GET /api/v1/mobile/auth/sessions` — list active `AuthRefreshToken` rows for the caller. Fields: `(jti, issued_datetime, last_used_datetime, user_agent, ip, is_current)`. The "current" flag marks the row whose hash matches the caller's refresh-cookie.
  - `DELETE /api/v1/mobile/auth/sessions/{jti}` — revoke a specific session (reuses `revoke_by_jti`).
  - Capture `User-Agent` + remote IP at refresh-token-mint time and persist on the row (column add to `dbo.AuthRefreshToken`, sproc update, service threading from the request via FastAPI `Request` object).
  - Effort: ~3 days. Web-side `Profile → Active sessions` screen + per-row revoke is then a ~2-day follow-up.
- [ ] **Agent audit-trail separation — shape A (`acting_on_behalf_of_user_id` column).** Decision locked 2026-06-16: column shape, NOT JWT actor claim, NOT header pattern. Rationale: smallest schema delta, captures the audit need without touching the JWT/RBAC layer; if RBAC-level on-behalf-of becomes needed later, shape C can be layered on top without rework. Work:
  - Add nullable FK `ActingOnBehalfOfUserId BIGINT NULL FK User.Id` to: `Workflow`, `AgentSession`, optionally `Bill` / `Expense` / `Invoice` create rows.
  - New ContextVar `current_acting_on_behalf_of_user_id` populated when an orchestrator's delegation tool fires. ToolContext threads it through every sub-agent call.
  - Service-layer writes to `Workflow.ActingOnBehalfOfUserId` when the ContextVar is set.
  - Surface in an admin audit view (separate web task once data flows).
  - Effort: ~1 week. Server-only; no web-side dependency.

### Phase 2 — Server-side hardening (1-2 weeks, defer 4-6 weeks)

- [ ] **Cross-worker RBAC cache invalidation.** `shared/rbac.py` keys cache per (user_sub, company_id) with 5-min TTL. `invalidate_all_caches()` only clears the dict in the worker that handled the mutation — other gunicorn workers serve stale grants for up to 5 min. Implement via a small Redis pubsub (preferred — App Service can add Azure Cache for Redis Basic at ~$15/mo) OR a polling watermark in `dbo.RbacInvalidation` table that each worker checks every 30s. Manifests today as "I changed your role and you still see the old menu." Effort: ~3 days for the Redis path, ~5 days for the DB-polling path.
- [ ] **401 vs 403 discrimination.** Web's `redirectToLogin` fires on every 401 — including 401s emitted by a backend bug, misconfigured `require_module_api`, or 502→401 proxy translation. Server should return 403 for permission denials, reserve 401 for "authentication invalid" (missing/expired/forged token). Web client checks the body for `error="invalid_token"` per RFC 6750 §3.1. Trivial server change (FastAPI dependencies + custom 401 exception handler); ~1 day web change to discriminate. Effort: ~2 days total.
- [ ] **JWT `kid` claim + JWKS endpoint.** Manual secret rotation today means a rotation-day code redeploy. Adding `kid` to the JWT header + serving a JWKS endpoint (`GET /.well-known/jwks.json`) lets the secret rotate independently of code deploys. Can stay on HS256 (uglier but lower-risk) or switch to RS256 simultaneously. Defer until next rotation actually happens. Effort: ~3 days HS256+kid, ~5 days for RS256 migration.

### Phase 3 — DEFERRED, conditional

- [ ] **React Query persister cid-keying.** Today the persister key is `bo.rq.v1.<uid>` — switching Companies (cid changes) keeps the same key, leaking Company A's cached entity data into Company B context. Latent today (single Company in prod); user confirmed Phase 5b multi-Company is >6 months out. Add `// FIXME(Phase-5b)` comment in `src/main.tsx` so it's visible when the time comes. Effort: ~2 hours.
- [ ] **httpOnly cookie for access token.** Defeats XSS exfiltration of the JWT. Big project — CORS rework, CSRF rework, potentially breaks the agent-as-user pattern. Standalone effort. Trigger: real XSS incident, security audit, or regulatory ask. Effort: ~6-8 weeks if greenlit.

---

## Profile — Self-read carve-out on Contacts endpoint (API change)

- [ ] **`GET /api/v1/get/contacts/user/{user_id}` is gated on `Modules.VENDORS`.** Surfaced 2026-06-15 while wiring the Profile Contact rows. The endpoint reads ANY user's contacts as long as the caller holds the Vendors module (legacy "manage a vendor's contact list" use case). Side effect: a non-admin user who lacks Vendors read can't see their OWN Contact data on `/profile` or `/profile/details` — the row falls to "—". Christopher (system admin) sees it fine because IsSystemAdmin bypasses module gating; field users with role e.g. Field Crew / Intern would not. **Fix lives in `build.one.api`**, two options:
  1. **New endpoint `GET /api/v1/get/contacts/user/me`** — auth-only (no module gate), reads `current_user_id` from the ContextVar, returns just the caller's contacts. Same shape as `read_by_user_id`. Web's ProfileView + UserDetailScreen switch to this endpoint when reading the caller's own contacts (no behavior change for admin Vendors-side use of the existing endpoint).
  2. **Self-read carve-out on the existing endpoint** — at the top of `get_contacts_by_user_id_router`, if `user_id == current_user_id.get()`, skip the Vendors check; otherwise require it. Minimal surface change but couples auth-vs-RBAC in a way that's easy to miss when someone adds a new gate.
  Recommend option 1 — single-purpose endpoint, single-purpose docstring, easier to RBAC-audit. Web change is a one-line URL swap. Defer until a non-admin user actually hits this on prod (Christopher is the only active human signing into web today; field users land on iOS).

---

## Budget UI — future work

- [x] **Change-order timeline on the budget view (future version).** Requested 2026-06-15. Render the revision history as a visible amendment ledger/timeline on `BudgetView`, not just the flat revisions table that ships today. Each entry = a revision in order (Original, then each Change Order) showing: title, status badge, effective date, **net dollar impact** (sum of that revision's line `price` deltas — contract value; optionally `amount` for cost), approver + approved date, and a **running contract total after each approved revision**. Expandable to that revision's delta line items. **No backend work needed** — the data already exists: `GET /api/v1/get/budget-revisions/by-budget/{budget_public_id}` (carries `approved_by_user_id`/`approved_datetime`/`effective_date`/`type`/`status`/`revision_number`) + `GET /api/v1/get/budget-line-items/by-revision/{revision_public_id}` for each revision's delta lines. Pure frontend addition to `src/pages/budgets/BudgetView.tsx` (+ `budget.css`). Only approved revisions count toward the running total; draft COs shown as pending. Context: umbrella memory `project_budget_entity.md`. **DONE (U-017, 2026-07-14):** `BudgetView` revisions table replaced with an expandable amendment ledger (Rev/Type/Title/Status/Effective/**Net Impact**/**Running Total**/Approver/Actions). Ledger math extracted to a pure, unit-tested `src/pages/budgets/revisionLedger.ts` (`buildLedger` — cent-exact integer-cents sums, approved-only running total in `revision_number` order, draft/pending marked "Pending" + excluded, unknown-vs-known-zero distinction so a still-loading/errored revision shows "…" not a fabricated $0.00). Per-revision delta lines fetched in parallel via `useQueries({combine})`; approver resolved via `useIdNameMap`. Two-pass ✓ (Codex gpt-5.5/xhigh: P1 unknown-vs-zero under-report fixed → re-review PASS; `/simplify` 4-agent applied combine-stability + useIdNameMap reuse + dedup, behavior-preserving Codex PASS). tsc + build + **110 vitest** green (incl. 5 cacheCleanup + 10 revisionLedger specs). Preserves the Edit/View-lines link + "+ Change Order" affordances. Deploy = combined web SWA ship, held.

---

## Audit 2026-05-20 — Web findings

Full multi-repo audit memory: `~/.claude/projects/-Users-chris-Applications-build-one/memory/project_audit_2026_05_20.md`. Web-specific findings below; pair with that memory for context, cross-repo patterns, and Tier-1 fix list.

### Critical / High
- [x] **`AuthContext.isAuthenticated` and `ProtectedRoute` read `localStorage` during render — non-reactive.** [src/auth/AuthContext.tsx:44](src/auth/AuthContext.tsx); [src/auth/ProtectedRoute.tsx](src/auth/ProtectedRoute.tsx). Login/logout doesn't update derived state until the component remounts. Users can land in redirect loops or blank pages. Fix: derive `isAuthenticated` from `username` state (already reactive via `setUsername`); refactor `ProtectedRoute` to use `useAuth().isAuthenticated`. **DONE (U-014, 2026-07-14):** `isAuthenticated = username !== null`; `ProtectedRoute` consumes `useAuth().isAuthenticated`. Same-tab login/logout/session-expiry now update the gate without a remount. Follow-up filed below (cross-tab gate sync).
- [x] **Cross-tab auth-gate sync (follow-up from U-014).** [src/auth/AuthContext.tsx:44](src/auth/AuthContext.tsx). `isAuthenticated` derives from same-tab `username` state; there was no `storage` listener, so a logout/login in another tab didn't flip THIS tab's gate until its next server touch (a data fetch's 401 → `client.ts` `redirectToLogin` self-heals it — no data exposed, gate just briefly stale). Correct fix is NOT a naive `storage`→`setUsername` (a cross-tab *login-as-other* would swap identity without re-keying the per-user persister → the multi-user bleed contract). **DONE (U-026, 2026-07-14):** `AuthProvider` now registers a `window` `storage` listener (empty-deps `useEffect`, removed on unmount) that calls `window.location.reload()` on a REAL cross-tab change to `access_token`/`username` — guarded by `e.storageArea === window.localStorage` (excludes sessionStorage), `e.key ∈ {access_token, username}`, and `e.oldValue !== e.newValue` (no-op writes ignored). Covers login (null→value), logout (value→null), and login-as-other (valueA→valueB); the reload re-runs the boot-time per-user persister keying in `main.tsx` (`bo.rq.v1.<uid>`), never a `setUsername` identity-swap-without-rekey. No self-loop: `storage` fires only in OTHER tabs, and current-tab login/signup/logout already hard-reload. Two-pass ✓ (Codex gpt-5.5/xhigh PASS, 0 findings P0–P3; Pass-2 skipped-trivial — one `useEffect` in the shape of the sibling profile-events effect). tsc 0 + build 0 + **110 vitest** green (incl. 6 cacheCleanup contract specs). **Coverage boundary:** no `@testing-library` harness in-repo → listener behavior covered by tsc + build + Codex + cacheCleanup specs (consistent with U-014, same file). Deploy = combined web SWA ship, held. **U-032 refinement (2026-07-15):** the U-026 listener hard-reloaded on ANY cross-tab `access_token` change — but `client.ts` rewrites `access_token` on every token refresh (~hourly / on 401), so OTHER tabs got a spurious `window.location.reload()` that discarded unsaved work on non-auto-save surfaces (e.g. Bills forms). Extracted the reload decision into an exported pure predicate `shouldReloadOnStorage(e)` (`src/auth/authStorageSync.ts`); the listener is now a thin wrapper. Reloads ONLY on a real identity transition: `username` change, `access_token` removal (logout), OR the token's decoded **`uid`** claim changing — keyed on `uid` via the existing `decodeJwtPayload` (`src/api/jwt.ts`), the SAME value `main.tsx` keys the persister on (`bo.rq.v1.<uid>`), NOT the raw token string or the username string. A same-user refresh keeps the same uid → no reload. Pass 1 (workflow xhigh, refute-verified: 6 candidates → 4 refuted → 1 PLAUSIBLE-not-live) surfaced that keying identity on the username STRING misses a same-username/new-uid swap → the `uid`-keying closes it (implemented as a review-driven Refactor; deviates from the literal Gate-1 removed-only spec, flagged for approval). Pass 2 `/simplify` (reuse/efficiency clean) retired the duplicated rationale comment. tsc 0 + build 0 + **129 vitest** green (10 predicate specs incl. refresh→no-reload + uid-swap→reload; 6 cacheCleanup contract specs still green). Gate-2 held (unstaged).
- [ ] **Access token in localStorage is XSS-exfiltratable (audit 2026-05-20 residual — split out of the cross-tab bullet above at U-026, distinct concern, NOT done).** The CSRF cookie is correctly httpOnly; the access token isn't. Long-term fix: move JWT to httpOnly cookie (requires API changes — CORS + CSRF rework; see the "httpOnly cookie for access token" Phase 3 item under Auth hardening below). Interim: tight CSP headers + remove every `dangerouslySetInnerHTML` usage. [src/auth/AuthContext.tsx:37,62](src/auth/AuthContext.tsx); [src/api/client.ts:49](src/api/client.ts).
- [x] ~~**Logout clears only `["me"]` query from React Query cache.**~~ **DONE (verified 2026-07-14 recon; `b4c0792`).** `AuthContext.logout` calls `queryClient.clear()` (whole cache) + awaits `clearAllUserScopedStorage()` before redirect — no cross-user cache bleed.
- [ ] **Complete/Delete handlers missing `finally { setX(false) }`.** [src/pages/bills/BillEdit.tsx:221-233](src/pages/bills/BillEdit.tsx); [src/pages/expenses/ExpenseEdit.tsx:204-217](src/pages/expenses/ExpenseEdit.tsx); [src/pages/invoices/InvoiceEdit.tsx:198-210](src/pages/invoices/InvoiceEdit.tsx). Success path leaves the button stuck disabled; potential setState-on-unmounted-component warning when navigation fires. Same pattern on `BillCreate.tsx:46-80` (saving). Fix: wrap each in `try { ... } finally { setX(false); }` (and bail with a mounted ref check if needed).
- [x] ~~**Bill complete missing `await flushAutoSave()` before `saveAll()`.**~~ **MOOT (verified 2026-07-14 recon).** BillEdit + InvoiceEdit do NOT use `useAutoSave` — there's no auto-save to race; `saveAll()` on Complete IS the save. Only ExpenseEdit has auto-save and it already flushes. Re-open only if Bills/Invoices adopt auto-save.
- [x] ~~**`BillEdit.tsx` formRef captured null before initialization**~~ **MOOT (verified 2026-07-14 recon).** `if (!form) return null` gates the entire form + Save-button render, so `saveAll()`'s `if (!latestForm) return false` is unreachable from a user action.

### Medium
- [x] **`useCompletionPolling` ignores all errors — 401/500 spins for 3 minutes silently.** [src/hooks/useCompletionPolling.ts](src/hooks/useCompletionPolling.ts). ✅ FIXED 2026-07-12 (U-005): catch now branches on the structured `ApiError.status` — 404 keeps polling, any other HTTP status stops with a clear error surfacing `err.detail`, network/no-status stays transient. Also added a per-start generation guard so overlapping/stale polls can't clobber a terminal state (found in review). 10 vitest specs (`useCompletionPolling.test.ts`) cover 404/401/500/200/transient/timeout/overlap×2/stale-restart.
- [x] ~~**ESLint flagged: `setState` synchronously inside `useEffect`.**~~ **BENIGN-LINT (verified 2026-07-14 recon).** `usePaginatedList:190` sets `loading` before fetch (standard; `load` memo deps exclude `loading` → no loop); `useViewAttachmentObjectUrl` resets to initial values (idempotent). No cascade — suppress the lint, not a bug.
- [x] ~~**`FolderPicker.tsx:76` `useEffect` missing dep `browseFolderItems`**~~ **BENIGN-LINT (verified 2026-07-14 recon).** `browseFolderItems` is `useCallback([mode])` and `mode` IS in the effect's dep array, so the closure is never stale in practice. Add the dep or eslint-disable; lint-only, no bug.
- [ ] **File upload validates MIME type only, not size.** [src/pages/bills/BillCreate.tsx:34-44](src/pages/bills/BillCreate.tsx). Big uploads time out with a vague network error. Add a `file.size` check (e.g. 50 MB) with a clear inline error.
- [x] ~~**`Sidebar.tsx:27` route fallback mismatches actual routes.**~~ **MOOT (verified 2026-07-14 recon).** `Sidebar.tsx` is dead code — no import anywhere; live nav is `AppSidebar` rendering static `to={entry.route}` from `menuConfig.ts` (no `toLowerCase()` fallback). Optionally delete `Sidebar.tsx`.
- [x] **No `<ErrorBoundary>` wrapping any page.** Render error = blank screen + console log + no recovery. Wrap routes (or the entire `<Outlet />`) in a class-component ErrorBoundary that shows an inline fallback + reload button. — DONE (U-009, 2026-07-13): `src/components/ErrorBoundary.tsx` (class boundary, `console.error` logs, theme-aware `SectionCard`/`ios-page` fallback + Reload; `RouteErrorBoundary` resets on `location.pathname` change without remounting healthy pages). Wraps the `<Outlet/>` in all three shells (AppLayout + BillLayout + BudgetLayout) + a top-level backstop `<ErrorBoundary>` in `App.tsx` covering `/login` + providers. Vitest at `src/components/ErrorBoundary.test.tsx`. Codex PASS.
- [ ] **`computeLineItem` uses JS `Number()` arithmetic for currency.** [src/pages/bills/BillEdit.tsx:39-50](src/pages/bills/BillEdit.tsx). UI totals drift from server on edge cases. Use a small decimal library or carry values as strings until display.
- [x] ~~**`useAutoSave.flush()` swallows save errors.**~~ **DONE (verified 2026-07-14 recon; `5382fac`).** The single-in-flight rewrite made `runSave` reject its chain on `saveFn` error (`rejectChain(err)`), and `flush()` returns that chain — so an awaiting caller now sees the rejection. (Residual, NOT this item: the *debounce* path discards `runSave()`'s promise, so a failed background auto-save is an unhandled rejection with no user toast — track separately if it bites.)
- [x] **InlineContacts delete has no `saving` guard / inline error persistence** — rapid clicks trigger duplicate deletes; transient error message can be lost. — DONE (U-010, 2026-07-13): `handleDelete` now mirrors the sibling `handleAdd`/`handleUpdate` pattern — keeps the `confirm()`, then `if (saving) return` + `setError("")` + `setSaving(true)` at the top, `del()` + optimistic `setContacts` filter on success, `setError(err.message)` in `catch`, `setSaving(false)` in `finally`. Both Edit + Del buttons now carry `disabled={saving}` (matches Save/Add), so no delete/edit/add interleave. Error surface = existing `{error && <div className="form-error">}` under the Contacts header; `setError("")` on delete-start fixes stale-message persistence. tsc + build + 92/92 vitest green (incl. 5 cacheCleanup specs). Codex (gpt-5.5, xhigh) PASS, zero findings.

### Low
- [x] **Extract a shared `renderHook` test harness.** — DONE (U-036, 2026-07-15): hoisted the duplicated `createRoot`+`TestComponent` hook-render harness (+ `deferred`/`drain`) into `src/hooks/__testutils__/renderHook.ts` — generic `renderHook<T>(useHook)`, `deferred<T = void>()` (the `void` default keeps autosave's `deferred()`/`Deferred[]`/no-arg `resolve()` byte-identical while the completion spec uses `deferred<PollResult>()`), and `drain()`. Both `useCompletionPolling.test.ts` and `useAutoSave.test.ts` now import it; every `it()` body stays byte-identical (completion keeps its 11 `renderHook()` call sites via a one-line local wrapper + import alias; autosave keeps a thin `renderAutoSave` wrapper for its editable saveFn/deps + object-shaped rerender). Local copies deleted. **Test-only — zero runtime `src/` change.** tsc -b + build + **129/129** vitest green (incl. 5 cacheCleanup specs). Composer wrote (composer-2.5-fast); Codex (gpt-5.5, xhigh) Pass-1 PASS, zero findings; Pass-2 `/simplify` clean (reuse/efficiency/altitude no findings; one stale-comment nit applied).
- [ ] **[U-036 follow-up] Single-source `IS_REACT_ACT_ENVIRONMENT` in `vitest.setup.ts`.** The flag is suite-global and `vitest.config.ts` already wires `setupFiles: ["./vitest.setup.ts"]`, yet it's still set inline in **5** places (was 3; `src/layout/BottomTabBar.test.tsx` was missed in the original count and U-047 added `src/components/ui/Sheet.test.tsx` — it keeps getting copy-pasted precisely because the "inherit it by importing the harness" rationale doesn't cover specs that don't import it): `src/hooks/__testutils__/renderHook.ts`, `src/components/ErrorBoundary.test.tsx`, `src/pages/expense-coding/ExpenseCodingCockpit.test.tsx`, `src/layout/BottomTabBar.test.tsx`, `src/components/ui/Sheet.test.tsx`. Hoist the one line to `vitest.setup.ts` and delete all inline copies. Out of U-036's 3-file scope (touches non-diff files; changes the flag for suites that don't currently set it). Surfaced by Pass-2 altitude review. Related trajectory note: if hook-test reuse widens, `deferred`/`drain` are general async-test primitives that would belong in a top-level `src/test/` util rather than `renderHook.ts` (leaving that file the render harness proper).
- [x] InlineContacts edit row: no Escape-key handler to cancel edit. Minor UX. — DONE (U-021, 2026-07-14): added `handleEditKeyDown(e, contact)` — **Escape** → `cancelEdit()` (always, regardless of `saving`); **Enter** → `handleUpdate(contact)` only when `!saving` (mirrors the Save button's `disabled={saving}` guard, so no double-save; Enter is a no-op mid-save). Wired `onKeyDown` onto all 5 edit-row inputs; `preventDefault()` on both keys. Add-new-contact row, delete-guard (U-010), and all `saving` guards untouched; normal typing unaffected (only Enter/Escape intercepted). tsc + build + 110/110 vitest green (incl. 5 cacheCleanup specs). Codex (gpt-5.5, xhigh) PASS, zero findings; Pass-2 skipped-trivial.
- [ ] **215 `@typescript-eslint/no-explicit-any` errors** across `src/pages/*` — not bugs themselves but a risk amplifier; real type-mismatches survive type-check. Sweep over time.
- [ ] **[U-047 follow-up] `ActionModal` in `ReviewTimeline.tsx` is now a worse duplicate of `Sheet`.** [src/components/ReviewTimeline.tsx:337-365](src/components/ReviewTimeline.tsx). Hand-rolls `role="dialog"` + `aria-modal` + backdrop + `stopPropagation` with inline styles, and has **no focus trap, no Escape, no focus restore, no `aria-labelledby`**. U-047 made `Sheet` strictly better on every one of those axes, so this should either adopt `Sheet` or be deleted in favour of it. Pre-existing (predates U-047), surfaced by its Pass-2 reuse review. Blast radius = the review-timeline action flow, so it wants its own unit + regression pass.
- [ ] **[U-047 follow-up] Escape closes EVERY open sheet, not just the topmost.** [src/components/ui/Sheet.tsx](src/components/ui/Sheet.tsx). Each open `Sheet` registers its own `document` keydown listener, so if two are ever open simultaneously one Escape fires both `onDismiss` handlers. **Pre-existing** (the listener is unchanged by U-047, whose spec required preserving Esc as-is) and **currently unreachable** — no Sheet nests inside another, and all five consumers mount closed and toggle `open`; a picker's backdrop + focus trap block reaching a second trigger. Confirmed by Codex Pass-1 (P3) and consciously deferred. Fix when the first nested/simultaneous sheet lands: track a module-level open-sheet stack and only let the topmost handle Escape.
- [ ] **[U-047 follow-up] `SheetHeader` title should probably be an `<h2>`, not a `<div>`.** [src/components/ui/SheetHeader.tsx:26](src/components/ui/SheetHeader.tsx). Held out of U-047 because it changes the accessibility tree (= behavior) and Pass 2 is behavior-preserving. **The obvious objection is not real:** `index.css:1-8` has a global `*, *::before, *::after { margin: 0; padding: 0 }` reset, and `.sheet-header-title` already pins font-size/weight with a class selector, so an `h2` renders **pixel-identical** — verified by Pass-2 altitude review. It's an improvement for the two NON-sheet consumers too (`TimeLogForm`/`EditEntryScreen` currently render zero headings). Real question is house consistency: sibling `NavHeader` uses the same div-title pattern while older `PageHeader`/`DetailView` use real headings — decide the convention once and apply it to all of them.
- [ ] **[U-047 follow-up] `aria-labelledby` is claimed unconditionally but delivery isn't guaranteed.** [src/components/ui/Sheet.tsx](src/components/ui/Sheet.tsx). `Sheet` always renders `aria-labelledby={titleId}`, but the context only *offers* the id — a `Sheet` rendered without a `SheetHeader` would get a dangling IDREF and therefore **no accessible name**, the same shape of false claim U-046/U-047 exist to kill. Harmless today (all five consumers render `SheetHeader`). Proportionate fix is a tripwire test in the `menuConfig.test.ts` spirit, NOT `hasTitle` state or folding `title` into `Sheet`'s props — both cost more than the risk.
- [ ] **[U-047 follow-up] Evaluate native `<dialog>` + `showModal()` to replace the hand-rolled focus machinery.** [src/components/ui/Sheet.tsx](src/components/ui/Sheet.tsx). The platform gives trigger-restore-on-close (the entire `triggerRef` mechanism), the focus trap, Escape, `aria-modal`, and `::backdrop` for free — both of U-047's most heavily-commented mechanisms (render-phase trigger capture, StrictMode replay guard) exist *only* because we re-implement the platform. Bonus: top-layer rendering is immune to transformed ancestors, which would **retire the "MoreDrawer must stay outside `.app-tabbar`" landmine** documented in CLAUDE.md and pinned by a DOM-tree test in `BottomTabBar.test.tsx`. Baseline is fine (Safari 15.4+, below this PWA's floor). Migration touches the rise/fade animation, backdrop dismissal, and every pinning test — its own unit. Surfaced by U-047 Pass-2 altitude review.
- [ ] **[U-047 follow-up] Extract a shared component-mount test harness (sibling to U-036's `renderHook`).** The `container = createElement("div")` → `appendChild(document.body)` → `createRoot` → `afterEach` unmount+remove block is now triplicated verbatim: [src/layout/BottomTabBar.test.tsx](src/layout/BottomTabBar.test.tsx), [src/pages/expense-coding/ExpenseCodingCockpit.test.tsx](src/pages/expense-coding/ExpenseCodingCockpit.test.tsx), and the new `src/components/ui/Sheet.test.tsx`. Note U-036's `src/hooks/__testutils__/renderHook.ts` is **hook-only and deliberately does not append to `document.body`** — focus assertions need a connected tree, so component specs correctly can't reuse it. Wants a `mount()` returning `{container, root}` in a new `src/__testutils__/` (the `hooks/` dir is the wrong home for a component harness). Same shape as U-036 → its own test-only unit.
- [ ] **[U-088 follow-up] Extract shared `makeUser`/`makeModule` nav test fixtures.** U-088 added `src/layout/AppSidebar.test.tsx`, which carries a **3rd byte-for-byte copy** of the `makeUser`/`makeModule` `CurrentUser`/`CurrentUserModule` builders — the other two are [src/layout/BottomTabBar.test.tsx](src/layout/BottomTabBar.test.tsx) and [src/layout/menuConfig.test.ts](src/layout/menuConfig.test.ts) (verified identical). Any change to the `CurrentUser` shape means editing all three. No shared fixture module exists yet (`src/hooks/__testutils__/renderHook.ts` is hook-render only). Concrete fix: a `src/layout/__testutils__/navFixtures.ts` (or the broader `src/__testutils__/` proposed by the U-047 mount-harness follow-up) exporting `makeUser`/`makeModule`; migrate all three layout specs. Deferred: test-only, tangential to U-088's active-state unification, and best folded in with the sibling U-047 mount-harness + U-036 `IS_REACT_ACT_ENVIRONMENT` extractions (this new spec adds a copy to **both** of those lists too — `IS_REACT_ACT_ENVIRONMENT` now 6 sites; mount-harness block now quadruplicated). Surfaced by U-088 Pass-2 reuse+simplification review.
- [ ] **[U-005] Expense Coding Cockpit — per-keystroke full-list re-render (only if typing lag appears).** Editor `selections` state lives in the parent, so each keystroke in an expanded row re-runs `rows.map`, re-rendering all N `QueueRowCard`s (unpaginated queue → ~441 at the backlog). Imperceptible at ~1–2/day + a one-time backlog (collapsed cards are trivial, no DOM mutations), so NOT actioned. If typing lag is ever observed, the fix is to memo `QueueRowCard` + stabilize its `selection`/callback props, or localize the editor's transient state to the expanded card and lift only on Confirm (mind the "selections entry == dirty" invariant). Also unpaginated `/expense-coding/queue` — add server paging if the queue grows large.

## /docs — Web section (U-085) follow-ups

Shipped U-085 (2026-07-18): DERIVED build-time route table — `scripts/gen-web-docs-manifest.mjs` AST-introspects `routes.tsx` + `menuConfig.ts` → `src/docs/web/web-manifest.json` → `WebDocs.tsx`; `prebuild` hook regenerates on every build; `webManifest.test.ts` freshness tripwire. Deferred from its Pass-2 `/simplify` (all behavior-preserving, out of the web-routes-only unit scope):

- [ ] **[U-085 follow-up] Shared docs-freshness helper + type.** `relativeAge`/`isStale`/`STALE_AFTER_DAYS` are copied verbatim in both `src/pages/docs/sections/WebDocs.tsx` and `IOSDocs.tsx`; `types.ts` aliases `WebFreshness = IosFreshness`. Extract a shared `src/pages/docs/freshness.ts` (the date helpers) + a shared `DocsFreshness` type that both `IosFreshness`/`WebFreshness` alias, and migrate BOTH sections. Deferred because extracting for a single consumer isn't a win and migrating the working iOS section is outside U-085's web-only scope — do it when the API/MCP/Scheduler sections land (3rd+ consumer makes the shared home pay off). Same call applies to the `?raw`+`JSON.parse`+fallback loader duplicated across `webManifest.ts`/`iosManifest.ts` (a generic `parseRawManifest<T>(raw, fallback)`).
- [ ] **[U-085 follow-up] Rename the `nav_reachable` manifest key.** The count is honest ("page routes carrying a nav_group" = 34) but the KEY name overstates (reads like "nav landing targets", which is ~10). The WebDocs *label* was already corrected to "In nav group" (presentation-only, no output change). Renaming the JSON key (+ `WebCounts` type + the generator's stdout string + a truer companion metric = routes whose full path exactly equals a `MENU_ENTRIES.route`) changes manifest bytes, so it's a follow-up that regenerates. Deferred to keep the Pass-2 cleanup output byte-identical.
- [ ] **[U-085 follow-up] Share the route-tree walker between tests.** `routePaths()` in `src/docs/web/webManifest.test.ts` duplicates the file-local one in `src/routes.test.tsx` (identical minus a `.sort()`). Lift into a shared test util both import when a third caller appears. Test-only, low value.

---

## Bills — completion follow-ups (U-101)

Shipped U-101 (2026-07-21): honest async Complete on BillEdit — confirm → flush(saveAll) → POST `/complete/bill` 202 → poll the bill itself via `useCompletionPolling(billItemPath, { isDone: b => !b.is_draft, onComplete, onError })` → shared `CompletionStatusBar`. Deferred residuals:

- [ ] **[U-101 follow-up] Migrate ExpenseEdit off the stale-hazard completion-result poll + onto `CompletionStatusBar`.** Two paired fixes, one unit: (a) ExpenseEdit polls `GET /get/expense/{id}/completion-result` — a **permanent upsert never cleared on re-complete** (`build.one.api` `bill/persistence/repo.py::set_completion_result` pattern is shared by expense), and a failed non-finalizing run stores a record while the expense stays draft, so a retry's first poll tick can read the STALE record and report done/failed instantly. Switch it to the U-101 pattern (poll the expense itself with `isDone: e => !e.is_draft` + `onComplete` cache reconcile via `entityItemKey`); (b) replace its three inline-styled status divs (`ExpenseEdit.tsx:310-325`) with `CompletionStatusBar` (retires the drifting copy + the inline hexes that bypass the design tokens). Both were confirmed by the U-101 Pass-1/Pass-2 reviews; left out only because ExpenseEdit was outside the approved diff scope.
- [ ] **[U-101 follow-up] BillEdit component-level regression coverage.** The two Codex P2 fixes (React Query detail-cache reconcile in `onComplete`; `cancelAutoSave()` at the completion boundary killing an armed debounce timer) are verified by review + hook-level tests but have **no component-level regression test** — BillEdit has no mount harness, and standing one up (mock client + 6 data hooks) is an `/sdet`-sized unit. Fold into the existing shared-mount-harness follow-up (U-047/U-088 items above): once `src/__testutils__/mount()` exists, add a BillEdit completion spec exercising flush-before-POST order, poll-success cache write, and the armed-timer cancel.
- [ ] **[U-101 follow-up — ⚠️ 2nd consumer now EXISTS, schedule it] `useAutoSave` cancel-on-disable.** An armed debounce timer survives `enabled` flipping false (effect deps don't include `enabled`). BillEdit compensates with an explicit `cancelAutoSave()` at the completion boundary. The safe deeper fix is a dedicated cancel-only effect (`useEffect(() => { if (!enabled) cancel(); }, [enabled, cancel])`) — do NOT add `enabled` to the arming effect's deps (false→true would arm a spurious auto-save of unchanged data in TimeEntryView/ExpenseEdit). Schedule only when a second consumer gates `enabled` on a transient flag (completing/submitting) rather than load state. Surfaced by U-101 Pass-2 altitude review. **Update (U-105, 2026-07-21): the trigger condition is met** — BillEdit now ALSO gates `enabled` on `actions.canEdit` (permission) and carries a *second* hand-rolled compensating cancel effect. Two copies of the workaround in one file is the signal to fold it into the hook.

## Bills — permission gating follow-ups (U-105)

Shipped U-105 (2026-07-21): BillEdit action buttons gated on real Bills RBAC (`can_update` / `can_delete` / `can_complete`), plus the first shared `hasModulePermission` primitive (`src/shared/permissions.ts`). Deferred residuals, all surfaced by the U-105 Pass-1 (Codex xhigh) / Pass-2 reviews:

- [x] **DONE (U-108, 2026-07-21) — all 9 hand-rolled module-permission lookups migrated onto `hasModulePermission`.** No inline `me.modules?.find(...)` permission check survives in `src/`. ⚠️ **This item's original premise was WRONG and was refuted during U-108's Gate-1 map — recorded here so it isn't "rediscovered".** It claimed `TodayScreen`/`PastDayScreen`/`TimeEntryView` omitting the `is_admin` bypass meant "a system admin is denied a UI action the API would allow." **They were not denying anyone.** `/auth/me` synthesizes an all-true permission row for EVERY module when `User.IsSystemAdmin = 1` (`build.one.api/entities/auth/api/router.py:713-719`, the `if is_system_admin: perms = {p: True …}` branch over the unfiltered `ModuleService().read_all()`), so an admin's `can_view_team`/`can_approve` is already `true` — "admin without the explicit grant" is a state the API cannot emit. Adding the bypass was therefore a semantic no-op, kept only so the client mirror stays faithful to `require_module_api` if `/me` is ever narrowed to real grants. **The real bug at those coordinates was a different one, in the opposite direction:** `TimeEntryView` gated the Approve/Reject block on `can_update` while `POST /time-entries/{id}/approve` + `/reject` enforce `can_approve` (`time_entry/api/router.py:449,471`) — a false affordance that 403'd for a `can_update`-only user. U-108 fixed that and pinned it with a named regression spec.
- [x] **DONE (U-115, 2026-07-22) — `BudgetEdit.tsx:81` compound-action gap fixed.** New `src/pages/budgets/budgetPermissions.ts::resolveBudgetEditActions` (canActivate/canApproveRevision = `can_update` AND `can_approve`; canEdit = `can_update` gates the whole grid read-only, mirroring BillEdit); `BudgetCreate` gated on `can_create`; both pages wait for `meLoading` before permission-derived render (BillEdit precedent). Line-item `can_create`/`can_delete` legs deliberately NOT folded into the gate (documented in the resolver; API is the backstop). Resolver table-tested incl. the approve-without-update regression. Original finding: **🔴 `BudgetEdit.tsx:81` had the same compound-action gap U-105 fixed on BillEdit.** `canApprove` (Budgets `can_approve`) gates "Save & Activate Budget" (:474) and "Save & Approve Change Order" (:479), but both handlers call `saveAll()` FIRST (:259, :279), which POSTs `/create/budget-line-item` + PUTs `/update/budget-line-item` — `can_create`/`can_update` on Budgets (`build.one.api/entities/budget_line_item/api/router.py:25,77`), while activate/approve are `can_approve` (`budget/api/router.py:148`, `budget_revision/api/router.py:127`). So a `can_approve`-without-`can_update` user is offered both buttons, clicks, and `saveAll()` 403s into a dead end. Fix is `budgetPermissions.ts` shaped exactly like `billPermissions.ts::resolveBillEditActions` (which already encodes the "Complete pre-saves, so it needs `can_update` IN ADDITION" rule). Found by U-108's Pass-2 altitude review; deferred as a behavior change outside that unit's approved scope.
- [ ] **Extract `makeModule`/`makeUser` test fixtures — now 5 copies (U-115's `src/pages/budgets/budgetPermissions.test.ts` added the fifth; its `/simplify` reuse pass counted 7 including the `AppSidebar`/`BottomTabBar` layout-test variants).** Byte-identical in `src/shared/permissions.test.ts:6-43`, `src/pages/bills/billPermissions.test.ts:6-43`, `src/pages/time-entry/timeEntryPermissions.test.ts:6-43`, `src/pages/budgets/budgetPermissions.test.ts:6-43`, plus a drifted near-copy in `src/layout/menuConfig.test.ts:24-62` (adds `role`, and an `as CurrentUserModule` cast that would compile silently past a new required `can_*` field instead of failing). `CurrentUserModule` has 8 boolean flags and has grown at least twice, so the next one breaks 3 files loudly and 1 silently. Lands naturally with the now-real `src/__testutils__/` directory (created by U-152).
- [ ] **The "UI offers an action the API 403s" bug class is ~4× wider than Bills.** Confirmed identical ungated buttons + identical API gates: ~~`ExpenseEdit.tsx` Complete (`expense/router.py:276` `can_complete`)~~ — DONE in U-124 (`expensePermissions.ts`, incl. the `ExpenseCreate`/`ExpenseList` `can_create` gates), `InvoiceEdit.tsx:264` Complete (`invoice/router.py:1163`) plus 3 more `can_complete`-gated sync routes (`invoice/router.py:1186,1209,1296`), `BillCreditEdit.tsx:229` Complete (`bill_credit/router.py:172`), `ContractLaborEdit.tsx:808` Delete. Their Save buttons hit the same `can_update` PUTs with no gate either. **28 of 30 `*Edit.tsx` pages have zero permission awareness** (only `BudgetEdit` and now `BillEdit`). One unit per entity, each shaped exactly like U-105 — now cheap, since the primitive exists. `ExpenseCreate`/`InvoiceCreate`/`BillCreditCreate` also lack the `canCompleteBills`-style gate `BillCreate.tsx:63` has.
- [ ] **The Edit entry point is still ungated — it now dead-ends.** `DetailView.tsx:36` renders Edit unconditionally for **29 consumer pages**, and `routes.tsx` has no per-route permission guard (only `ProtectedRoute` = authenticated). A user without Bills `can_update` clicks Edit on BillView and lands on U-105's "You don't have permission to edit this bill" panel. Correct and safe, but the affordance shouldn't have been offered. Right fix is `DetailView` taking `canEdit`/`canDelete` (or a `module` prop + `hasModulePermission`), NOT a route guard — a route guard can express only one static permission and cannot express U-105's action-conditional rule (`canComplete = can_update AND can_complete`, because Complete pre-saves) or the `is_draft` state condition. Touching a 29-consumer shared component needs its own unit.
- [ ] **No drift detection between web permission strings and the API's `require_module_api`.** `billPermissions.ts` and `shared/modules.ts` both claim "keep in lockstep" with `build.one.api/shared/rbac_constants.py`; nothing enforces either, and no test anywhere references `rbac_constants`. If the API flips `POST /submit/review/bill` from `can_update` to `can_submit` — realistic, since `can_submit` already exists in `CurrentUserModule`, is enforced by **zero** API routes today, and was the permission U-105's original brief assumed — every web test stays green while the UI silently mis-gates. Wants a cross-repo parity test (parse `rbac_constants.py` → assert the `Modules` map; grep routers for `require_module_api(Modules.X, "perm")` → assert a web-side route→permission table). Real infrastructure; its own unit.
- [ ] **[folds into the U-101 mount-harness item above] BillEdit permission-gating component coverage.** U-105's pure resolver (`resolveBillEditActions`) is table-tested, including the regression case Codex found (`can_complete` without `can_update` → `canComplete` false). What is NOT covered by a test is the JSX wiring: that Save/form-submit/Enter, Complete, Submit-for-Review and the auto-save are all unreachable when `can_update` is false. Verified by review only. Add the spec once `src/__testutils__/mount()` exists.

## Companies unpark (U-143) — deferred items

Shipped U-143 (2026-07-24): unparked `src/pages/companies/*` full CRUD (routes + Reference-section nav + `companyPermissions.ts` gated `Modules.COMPANIES`, mirroring `entities/company/api/router.py`). Deferred residuals, surfaced by the U-143 Pass-1 (Codex xhigh) / Pass-2 reviews:

- [ ] **Cache-invalidation convention fork across the entity fleet.** Companies followed the customers/vendors convention (mutation → `invalidateEntity`/`removeEntity` from `src/hooks/useEntity.ts`), but **cost-codes, sub-cost-codes, vendor-types, and payment-terms don't invalidate at all** — with the persisted React Query cache (5-min `staleTime`), their Edit pages can re-seed a stale `row_version` (guaranteed 409 on next save) and their lists can show deleted rows. Two fixes at the right altitude, one unit: (a) fold the pairing into shared mutation-helper variants in `useEntity.ts` (take `{queryClient, listPath, itemPath}`; create→invalidate list, update→invalidate list+item, delete→remove item+invalidate list) so pages state their paths once; (b) converge the four non-invalidating entity folders on it. Surfaced as the U-143 Pass-1 P2 (companies fixed in-unit; siblings out of scope) + Pass-2 altitude finding. **U-157 update (2026-07-27):** `payment-terms` (U-149, SHIPPED + DEPLOYED 2026-07-25) is the 4th member and was not on this list — its Create/Edit/View never reconcile, so a deployed user creating a payment term does not see it in the list for up to 5 min (persisted to IndexedDB, so a reload doesn't clear it). Addresses shipped WITH the calls (caught by the U-157 Pass-2 reuse angle, not by Pass-1 — Codex reviews the diff, not the sibling convention, so a *missing* conventional call is structurally invisible to it). **Root cause of the recurrence: nothing tests the call site.** `invalidateEntity`/`removeEntity` are unit-tested in `src/hooks/useEntity.test.ts`, but no page-level spec asserts a page *wires* them, so an unpark can drop them silently — twice now (U-149, and U-157 until Pass 2). Fold a `assertReconcilesCacheOnMutation` helper into the same `src/__testutils__/` extraction, or make it structurally impossible via (a). **RESOLVED for (b) by U-159 (2026-07-28):** all four named entities plus employee-labor and bill-credits now carry the calls (17 sites), and the "nothing tests the call site" root cause is closed — 6 new `*Invalidate.test.tsx` specs assert the exact `{listPath, itemPath}` per page, each mutation-proven (reverting the page calls turns all 17 specs RED). **(a) remains open** and is now better specified — see the U-159 deferred section at the end of this file (lookups namespace, required-`listPath`, non-`useEntityList` lists).
- [ ] **Shared entity-Edit test harness — U-143 added copy #6.** `flushMicrotasks`/`waitForCondition`/`adminUser()`/`NavCapture`/the `createRoot` + `MemoryRouter`(+`QueryClientProvider`) render helper are now duplicated across six `*Edit.test.tsx` + the `*RouteParams.test.tsx` files (bill-credits, companies, cost-codes, employee-labor, expenses, sub-cost-codes), ~100 lines each, already drifting (loop constants, optional `queryClient` param). Hoist into the EXISTING `src/__testutils__/` root (created by U-152 — do NOT start a second `src/pages/__testutils__/` root) and converge. First job: a shared `renderEntityEdit()` consumed by BOTH each file's `renderXEdit` AND `assertGuardSurvivesSameRowRefetch`, which today build the same MemoryRouter/QueryClientProvider tree twice. Folds naturally into the existing `src/__testutils__/` items above (U-101 mount-harness + the `makeModule`/`makeUser` 5-copy extraction) — one testutils unit covers all three.
  - **U-159 update (2026-07-28) — this is the 10th consecutive unit to defer it, and it is now measured, not estimated.** The U-159 Pass-2 reuse + altitude agents independently hashed the duplication: **`adminUser()` is byte-identical in 27 test files** (md5-matched, 10 lines each — U-159 added 6 of them) and the **`MemoryRouter > QueryClientProvider > Routes > Route` render tree appears 29×**. U-159's own diff is the proof of cost: the 4 repaired `*RouteParams.test.tsx` files each needed the *same* hand-written `QueryClientProvider` nest inserted (11 diff lines × 4 = 44 lines of pure churn) precisely because that tree was copy-pasted rather than shared — 6 of the 10 `*RouteParams` files already had it, 4 didn't, and nothing could catch the drift. Concrete target signatures from that pass: `src/__testutils__/currentUser.ts` → `adminUser(overrides?: Partial<CurrentUser>)` (plain data, safe with vitest hoisting), and `src/__testutils__/renderPage.ts` → `makeRenderPage(getRoot, getQueryClient?)`, which leaves all call sites character-for-character unchanged and is directly consumable by the 10 `*RouteParams` files too. Add a third export, `assertReconcilesCacheOnMutation`, to own the invariant U-159's 6 specs now hand-copy (`removeEntity` on delete, never `invalidateEntity`-with-itemPath — the `useEntity.ts:31-35` contract). **Do NOT extract the `vi.mock` blocks** (~110 duplicated lines): vitest hoists `vi.mock` per test file, and neither existing `__testutils__` dir contains a single one — that is the house style, and centralizing them would introduce import-order flakiness. Deliberately still deferred at U-159 because this file's own instruction is to schedule it as its own test-only unit rather than smuggle a 27-file convergence into an unrelated unit; doing 6 of 27 would just create a half-migrated state. **Per the U-152 rule, mutation-prove the harness on extraction** — remove one page's `invalidateEntity` call and confirm the spec goes RED — since a shared harness that silently no-ops is strictly worse than six honest copies.

## Addresses unpark (U-157) — deferred items

Shipped U-157 (2026-07-27): unparked `src/pages/addresses/*` full CRUD (routes + Reference-section nav + `addressPermissions.ts` gated `Modules.VENDORS`, mirroring `entities/address/api/router.py` — piggyback, no dedicated Address module). Deferred residuals surfaced by the U-157 Gate-1 map / Pass-2 reviews:

- [ ] **`DELETE /api/v1/delete/address/{public_id}` 500s on any in-use address (api unit).** `dbo.Address` has three inbound FKs with **no CASCADE** — `FK_VendorAddress_Address`, `FK_ProjectAddress_Address` (`entities/{vendor,project}_address/sql/`) and `FK_PhysicalAddressAddress_Address` (`integrations/intuit/qbo/physical_address/`). The delete route (`entities/address/api/router.py:65-72`) has no FK handling, so deleting an address that any vendor or project points at raises a bare integrity error → opaque 500 with no actionable message. Since most rows in the table ARE referenced, this is the common path, not the edge. Web ships the Delete affordance (full CRUD per the board, human decision at U-157 Gate 1) and surfaces whatever the API returns. Fix at the API: catch the FK violation and return **409 with the blocking references named** ("in use by 2 vendors, 1 project"), matching the delete-guard shape used elsewhere; web then renders it as-is via the existing toast path, no web change needed.
- [ ] **`Modules.VENDORS` permission wrapper is now duplicated — extract at the Vendors unpark (rule of three).** `src/pages/addresses/addressPermissions.ts` and `src/pages/vendor-types/vendorTypePermissions.ts` hold byte-identical bodies (`hasModulePermission(me, Modules.VENDORS, permission)`). The `paymentTermPermissions.ts` precedent says the right shape is a one-line re-export alias off a canonical owner — but `src/pages/vendors/*` has **no permission gating at all yet** (zero `Modules.VENDORS` references in that folder), so there is no owner to alias and inventing one now would be speculative. When vendors gets its gating pass, create `vendorPermissions.ts` as the owner and collapse both wrappers to aliases. Third copy = do it then, not later.
- [ ] **Edit-page refusal renders only after the entity GET (repo-wide, template-identical).** `AddressEdit` (like `PaymentTermEdit`, `SubCostCodeEdit`, and the rest of the unparked fleet) puts `if (!canEdit)` below the `loading` / `!form` early returns, so a user without `can_update` pays a full `GET /api/v1/get/address/:publicId` round trip (~200-800 ms against prod) before being told no — even though `me` is already warm in cache. Cheaper ordering is `meLoading → !canEdit → loading`; skipping the request entirely wants an `{ enabled }` passthrough on `useEntityItem`. Deliberately NOT fixed in U-157 to avoid template divergence — this is the same item already logged under the U-147 Pass-2 follow-ups (2), recorded here only as another instance.

## Cache invalidation class-fix (U-159) — deferred items

Shipped U-159 (2026-07-28): added the missing `invalidateEntity`/`removeEntity` reconciliation to the four config unparks (vendor-types U-130, cost-codes U-138, sub-cost-codes U-141, payment-terms U-149) plus employee-labor (U-136) and bill-credits (U-127) — 17 call sites, 6 new mutation-proven specs. This closes half (b) of the U-143 item above. **Half (a) — the shared mutation-helper layer in `useEntity.ts` — remains open and is now better specified (see below).** Residuals surfaced by the U-159 Pass-1 (Codex xhigh, PASS/0) and Pass-2 (4-agent) reviews:

- [ ] **Scope correction to the "14 entities still missing invalidation" framing.** Verified against `tsconfig.app.json`: **12 of those folders are tsconfig-parked** (`address-types`, `integrations`, `modules`, `project-addresses`, `projects`, `role-modules`, `taxpayers`, `user-modules`, `user-projects`, `user-roles`, `vendor-addresses`, `users/UserCreate.tsx`) — not deployed, and each picks up the calls at its own unpark per the checklist. The real **live** gap after U-159 is exactly two files: `src/pages/users/UserProfile.tsx`, and `src/pages/bills/BillEdit.tsx` — the latter **intentionally**, since it reconciles with `queryClient.setQueryData(entityItemKey(...), bill)`, the cheaper shape this file already prefers (no extra GET between PUT and navigate). Do NOT "fix" BillEdit.
- [x] **🔴 The `["lookups", …]` cache is never reconciled by ANY mutation in the repo.** `useLookups` registers under `["lookups", include]` (`src/hooks/useLookups.ts:12`) — a namespace `invalidateEntity` never touches, and zero pages in `src/` invalidate it. With the global 5-min `staleTime`, creating or renaming a vendor type / payment term / sub-cost-code leaves **every dropdown containing it stale for exactly the 5 minutes U-159 just fixed for lists** (`sub_cost_codes` has 11 consumers, `payment_terms` 4, `vendor_types` 3). Same severity class as the bug U-159 fixed, one cache namespace over. This is also the strongest argument for (a): the per-page ritual reconciles two of the three caches an entity write dirties, and nothing at the call site prompts an author to think about the third. **RESOLVED (U-161, 2026-07-28):** shared `invalidateLookups` + `lookupsKey` in `useEntity.ts`, wired to the 8 routed lookup-backing entities across 24 call sites; prefix-key invalidation covers every include-combination.
- [ ] **Lookup-backing entities still tsconfig-parked and unrouted.** `projects`, `address-types`, and `modules` back `/api/v1/lookups` keys but were deliberately skipped in U-161. Their unpark must add **both** `invalidateEntity`/`removeEntity` **and** `invalidateLookups`. Scoping rationale (human call at U-161 Gate 1): they are unreachable (no route, in `tsconfig.app.json` `exclude`), so pre-wiring would be dead, untypechecked code — and wiring `invalidateLookups` *without* their missing `invalidateEntity` would leave them half-reconciled, which reads worse than zero. The unpark is the correct place to wire both at once; `CLAUDE.md` checklist step 7 is the reminder.
- [ ] **Lookups tripwire — the defensible shape (U-161 Pass-2 altitude).** Prose (checklist step 7) is a reminder, not a mechanism, and the recurrence it must survive is already scheduled: three parked unparks. Shape, mirroring the list-cache tripwire above but keyed on the lookups namespace: derive the key set from the **fields of the `Lookups` interface** (`src/types/api.ts`) rather than a hand-copied list; map snake_case key → kebab-case page folder (exact for 11/12; `contract_labor_vendors → vendors` is the single alias); drop folders matched by `tsconfig.app.json`'s `exclude`; assert every `*Create/*Edit/*View.tsx` in a mapped folder that imports a `useEntity` mutator also imports `invalidateLookups`. **More** defensible than the list-cache variant: zero intentional exceptions today (BillEdit's `setQueryData` carve-out doesn't apply — bills back no lookup key), no alternate list mechanism to exempt, self-maintaining skip list, and it fires automatically when `projects/` unparks. Known blind spot, stated honestly: it keys on `useEntity` mutator imports, so it cannot see raw `put()` sites — i.e. exactly the item below.
- [ ] **A live lookup-backing mutation sits OUTSIDE the entity's Create/Edit/View triple.** `src/pages/vendor-compliance/VendorComplianceDashboard.tsx:84-92` (`setTrackCompliance`) does a full-object `put("/api/v1/update/vendor/{publicId}")` from a **routed, unparked** page (`routes.tsx:173`) and reconciles nothing but its own `DASHBOARD_QUERY_KEY`. **Not a bug today and deliberately NOT wired in U-161** (verified at Gate 1: it re-reads and round-trips `name`/`abbreviation`, and `VendorUpdate.is_contract_labor` defaults to `None` so the sproc's `CASE WHEN @IsContractLabor IS NULL` guard preserves it — no field in the `vendors`/`contract_labor_vendors` payload can change). Two real residuals: (1) the PUT bumps `RowVersion` while leaving `["item","/api/v1/get/vendor/…"]` stale — the 409-on-next-save class U-159 fixed, one page over; (2) it is the counterexample to the model U-161 encodes ("a lookup-backing entity's mutations live in its Create/Edit/View triple"), so the moment that PUT gains a lookup-visible field or another compliance action edits a vendor name, every vendor dropdown goes stale for 5 minutes with nothing prompting the author.
- [ ] **`invalidateEntity`'s `listPath` is required, so "invalidate the item only" is inexpressible — and `entityListKey` is not parameter-aware.** This is why employee-labor has to pass a known-dead `/api/v1/get/employee-labors` with an explanatory comment: `EmployeeLaborList` fetches per-period (`?billing_period_start=`) via `rawRequest`. Note a plain `useEntityList` migration would **not** revive that key — React Query partial-matches array *elements*, not string prefixes, so the key must also become parameter-aware. Fold into (a): make `listPath` optional (or add `invalidateEntityItem`), and give `entityListKey` a `(basePath, params?)` shape. ~3-5 files.
- [ ] **`listPath` silently no-ops for any list not backed by `useEntityList`.** Two other list mechanisms exist and neither is reachable by `invalidateEntity`: `usePaginatedList` (`src/hooks/usePaginatedList.ts`) uses a **module-level `Map`, not React Query at all** (consumed by `BillList`, `EmailList`), and raw `rawRequest`+`useEffect` (`EmployeeLaborList`). Nothing at the call site tells an author which case they're in, so a `listPath` that reads correctly can be inert. Cheapest honest mitigation short of (a): one sentence in the `invalidateEntity` docblock naming the precondition ("only reaches lists registered via `useEntityList` under the identical path string").
- [ ] **Migrate `EmployeeLaborList` to `useEntityList`** (prerequisite: the parameter-aware key above). Until then its `listPath` stays inert-by-design and the two employee-labor call sites carry a comment saying so.
- [ ] **`BillCreditEdit` Complete path pays one dead round-trip.** `handleComplete` calls `saveAll()` (which invalidates and, because the Edit page is actively observing that item key, **awaits a real GET**) and only then fires the completion POST, whose result is invalidated and refetched again. The first GET is superseded microseconds later. Fix is a `saveAll({ invalidate = true })` flag with `handleComplete` passing `false` — **but it MUST also add `await invalidateEntity(...)` to `handleComplete`'s catch path**, because today's redundant call is what keeps the cache correct when the completion POST fails after a successful save. Deliberately NOT done in U-159: it adds branching to a financial completion path (kicks the SharePoint/Excel/QBO outbox) and changes user-visible timing, which is out of bounds for a behavior-safe unit. Also requires tightening `billCreditInvalidate.test.tsx`'s "complete" spec from `>= 2` invalidations to exactly one.
- [ ] **Optional tripwire — and NOT the naive form.** "Any page importing `createEntity|updateEntity|deleteEntity` must also import `invalidateEntity|removeEntity`" is a false-positive generator: it trips `BillEdit` (correct by design), needs a ~37-entry allowlist, is satisfied by a single token anywhere in a multi-entity file like `UserProfile`, and misses the ~28 live files that mutate via raw `post/put/del`. The defensible shape keys on **evidence that an RQ-backed list exists**: for each `src/pages/X/`, if `X*List.tsx` calls `useEntityList(P)` **and** a sibling Create/Edit/View imports a `useEntity` mutator, then that sibling must reference `P`. That auto-exempts `employee-labor` (rawRequest) and `bills` (usePaginatedList) with no hand-written exceptions, and reading `tsconfig.app.json`'s `exclude` keeps the parked-folder skip list self-maintaining. Verified green repo-wide as of U-159. Value is capped to the exact recurrence it would have caught (U-149, U-157 — both shipped whole page-sets with zero calls); rank it below the shared-spec-harness item, which catches the same class *and* checks path correctness.
