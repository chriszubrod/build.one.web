# TODO — build.one.web

Pending work, deferred decisions, known issues. Check off as done; prune anything stale.

---

## /docs surface — keep current + extend

v1 (admin-only, iOS section) shipped 2026-06-20. **Standing rule:** every feature add/change refreshes the matching `/docs` section — the Docs step of the per-unit pipeline (see `CLAUDE.md` "Documentation surface" + umbrella memory `feedback_docs_keep_current.md`). Follow-ups:

- [ ] **API section (LIVE)** — highest value: a live OpenAPI explorer from `/openapi.json` + entity/agent registries, behind a new `require_system_admin` admin endpoint in `build.one.api`. The first section that's genuinely always-current; replaces the "coming soon" stub in `docsSections.ts`.
- [ ] **Web / MCP / Scheduler sections (DERIVED)** — build-time generators: web route table (`App.tsx` / `menuConfig.ts`), MCP tool manifest (`tools/*.py`), scheduler cron inventory (`function_app.py`). Each replaces a "coming soon" stub. **When the Web/API section lands, document the U-005 Expense Coding Cockpit** (`src/pages/expense-coding/`, `Modules.EXPENSES`): the 58999→SubCostCode queue, deterministic suggestions (reason+confidence), human confirm → surgical fail-closed QBO recode. Deferred here only because the per-repo `/docs` Web/API sections don't exist yet (stubs) — there's no matching section to add to today; documented in `build.one.api/CLAUDE.md` in the interim.
- [ ] **Mobile reachability** — `/docs` is desktop-sidebar-only today (Reference group). Wire `MoreDrawer` into `BottomTabBar` so admins reach it on phones, or consciously accept desktop-only.
- [ ] **Freshness enforcement (hardening)** — no CI exists; the iOS snapshot can silently lag if `gen_docs_manifest.py` + `docs:sync:ios` aren't re-run. Consider a `build`-time freshness check (fail if `src/docs/ios` differs from the iOS source when both repos are checked out) and/or a `settings.json` hook so refresh isn't purely manual.

---

## Expense Coding cockpit (U-005) — enhancements

- [x] **[U-023] Sort + badge the coding queue by suggestion confidence.** Highest-confidence rows (ready to confirm) surface at the top; null/non-finite-confidence rows (no score → need cardholder follow-up) sink to a distinct bottom group; each row header carries a small confidence badge (High ≥0.90 / Medium 0.60–0.90 / Low <0.60 / No score). Web-only, reuses the existing `/api/v1/expense-coding/queue` data (no API change) — all sort/tier logic keyed STRICTLY on `suggestion_confidence` (number|null) and lives in the pure module `src/pages/expense-coding/expenseCodingLogic.ts` (`sortQueueByConfidence`, `confidenceTier`, `formatConfidencePct`, shared `toRatio`), so an in-flight `/expense-coding` endpoint reshape is a one-file re-point. Sort applied via `useMemo` on the query data (never mutates the RQ cache); badge is a self-contained pill styled to match the sibling status badge. Preserves Confirm/Flag/edit + the "selections entry == dirty" invariant + the multi-user cache contract. **DONE (U-023, 2026-07-15):** two-pass ✓ (Codex gpt-5.5/xhigh adversarial — PASS, 0 findings P0–P3; `/simplify` 4-agent applied: index-decoration removal on the sort — native stable `Array.sort`, es2023 target — and a `No suggestion`→`No score` label fix. A shared-`toRatio` dedup with `formatSuggestionHelper` was attempted then REVERTED after the Codex re-review flagged a P3: the /100→*100 round-trip flipped the rounding of half-integer percents there — `formatSuggestionHelper` (percent-native) and `confidenceRatio` (ratio-native) are now deliberately kept separate with a comment saying why; final re-review PASS). tsc 0 + build 0 + **119 vitest** green (incl. 5 cacheCleanup + 9 new sort/tier/badge specs). Deploy = combined web SWA ship, held per web-deploy-ask-first.
- [ ] **Extract a shared base-pill CSS class.** `.status-badge`, `.expense-coding-credit-chip`, and (as of U-023) `.expense-coding-confidence-badge` each repeat the same 5 base pill declarations (`display`/`padding`/`border-radius`/`font-size`/`font-weight`). Deferred from U-023 because `.status-badge` is used across ~10 pages — retrofitting it is out of scope for a web-only badge unit. Extract one base-pill class all three extend (flagged by the U-023 `/simplify` reuse pass).

---

## PWA — Tier 2 / Tier 3 decision gates

- [ ] **JWT-in-localStorage durability on installed PWAs.** Surfaced 2026-06-15 (Phase 1.9). iOS Safari may evict localStorage from standalone PWAs after ~7 days of non-use, silently logging the user out. Documented as a Tier 1 known limitation. Long-term fix is migrating refresh tokens to httpOnly cookies + access tokens to in-memory only; that's a separate auth-durability project with CORS + CSRF implications. Do NOT bundle into PWA Tier 2/3 work — flag it, then decide independently. Triggers: complaints from installed-PWA users about unexpected sign-outs, or once Tier 2 lands and the surface becomes more "app-like" in expectation.
- [x] **Tier 2 — offline reads** shipped 2026-06-15 same session as Tier 1. Persistent React Query cache via `@tanstack/react-query-persist-client` + `idb-keyval` with user-scoped persister key; multi-user safety contract enforced via `clearAllUserScopedStorage` + awaited logout + login reload (5-spec Vitest regression test at `src/auth/cacheCleanup.test.ts`). NetworkFirst SW routes for `/api/v1/get/*`, `/api/v1/list/*`, `/api/v1/lookups*`, `/api/v1/*/by-*`. OfflineBanner shows "Synced X ago". Storage readout in Profile → Appearance. See `docs/pwa-tier2.md`.
- [ ] **Tier 3 decision gate — local-first writes.** Conditional, not committed. Greenlight Tier 3 (~10 weeks server + client) only if (a) >20% of mutations are attempted while OfflineBanner is visible (instrument via telemetry after Tier 2 lands), (b) PMs ask for browser-side offline writes specifically, or (c) Android tablets / Chromebooks come on the roadmap. Otherwise mark "deferred indefinitely" — iOS already owns the offline-write surface for field users. Server-side prerequisite (`dbo.IdempotencyKey` table + middleware on every mutation route, ~3 weeks) has independent value for iOS hardening even if Tier 3 itself is deferred.
- [ ] **Theme-color + manifest colors.** ~~Was the deferred Phase 6 follow-up from the initial mobile-shell work.~~ Folded into PWA Tier 1 Phase 1.2 — `theme_color` is now `#1a1816` (charcoal) and `background_color` is `#f4f2ee` (warm cream) in both `index.html` and the manifest emitted from `vite.config.ts`. Close.

---

## TimeEntry view — auto-save / concurrency follow-ups (deferred)

Surfaced by the Codex Pass-1 review of the TimeEntryView concurrency hardening
(`f1f75c0`, 2026-07-11). The 4 P1 data-loss bugs from that review are **fixed +
shipped**; these are the lower-severity residuals left for later.

- [x] **`useAutoSave` drop-without-reschedule (P2, pre-existing, shared hook). FIXED
  2026-07-12.** `src/hooks/useAutoSave.ts` now serializes saves via a single-in-flight
  runner: a request made while a save is in flight sets a pending flag and a single
  coalesced follow-up runs the latest `saveFn` after the in-flight save resolves
  (loops until nothing pending); `flush()` resolves only after the latest state is
  persisted (awaits in-flight + follow-up); reentrancy-safe (in-flight promise
  published to `inFlightRef` synchronously before `saveFn` runs). Codex round-1 caught
  that the reschedule alone still sent a **stale `row_version`** for row_version-coupled
  callers (follow-up runs before React commits the prior save's `setForm`, and
  `UpdateExpenseById` rejects a stale token → silently-lost edit). Full fix: **Expense**
  (`autoSaveHeader` + `saveAll`) and **TimeEntry** (`autoSaveHeader`) now source
  `row_version` from a `rowVersionRef` updated synchronously on save success, with a
  `useEffect(() => { ref.current = null }, [form?.row_version])` clear so external
  hydrations stay authoritative. Codex PASS after 2 fix rounds (P1/P2/P3 closed;
  Expense Complete path + f1f75c0 guards intact). 6 vitest specs on the hook
  (reschedule / flush-await / no-overlap / mount-skip / reentrancy / ref-visibility).
  **Note:** `BillEdit.tsx` does NOT use `useAutoSave` (manual `saveAll` on Complete) —
  not a caller today; it becomes one when Bills unparks. Coverage boundary: the caller
  `rowVersionRef` edits have no component-level unit test (repo has no
  `@testing-library/react` harness) — covered by tsc (TimeEntry only; `pages/expenses`
  is excluded from `tsconfig.app.json`), build, Codex re-review, and the hook's
  ref-visibility spec.
- [ ] **Extract `useSyncedToken(form?.row_version)` when Bills gets auto-save (Pass-2
  altitude follow-up, 2026-07-12).** The `rowVersionRef` + clear-effect + `?? committed`
  read pattern is duplicated in ExpenseEdit + TimeEntryView. At 2 callers it's left
  inline (rule-of-three); extract a tiny `useSyncedToken` hook (`{ read, set }` +
  the clear-effect) as the **first step** of wiring Bills onto `useAutoSave`, so the
  silent-failure-critical clear-effect is never hand-copied into a 3rd caller.
- [ ] **Entry-delete vs in-flight log-save race (P3).** `f1f75c0` disables Delete
  Entry + guards `handleDelete` while any log row is `saving`, closing the main
  window. Residual: the guard is a point-in-time snapshot, so a delete initiated in
  the same tick a log save starts is still theoretically racy. Low severity — revisit
  only if it manifests.

(BudgetLayout / BudgetSidebar nav-retirement is already tracked under **Phase 1B**
below.)

---

## Nav strategy — Phase 1+ (from 2026-06-17 navigation evaluation)

Pattern locked: **Bottom Tab Bar + Hamburger Drawer** with a curated per-role primary-slot resolver. Phase 0 shipped this session (chassis only — no user-visible nav change except the tablet sidebar flip). For the full evaluation see the workflow output at `/private/tmp/claude-501/-Users-chris-Applications-build-one/7b4ffd2a-28ac-4754-9941-d5889b92a8cd/tasks/w0sh0c392.output`.

Project-scoped vs entity-scoped doors decision (2026-06-17): **both, sharing one component.** Each entity (Bills, Expenses, Invoices, BillCredits, Budget, ContractLabor) gets a top-level nav entry (entity-centric flat list with filters) AND a Project detail page tab (project-scoped pre-filtered view). The list component takes `defaultFilter` + `hiddenFilters` + `showProjectColumn` props; top-level call passes nothing, project tab call locks project_id.

### Phase 1A — Project as hub (shipped 2026-06-17)

- [x] **New `src/pages/project/ProjectDetailScreen.tsx`** with tabs: Overview, Budget, Bills (placeholder), Expenses (placeholder), Invoices (placeholder), Labor (placeholder). Lives at `/project/:publicId`. The "project as hub" pattern.
- [x] **`BudgetView` refactor** — extracted `BudgetViewContent({ publicId })` so the same component renders as a route at `/budget/:publicId` AND embedded in the Project → Budget tab. The wrapper still renders at the page route.
- [x] **New `src/pages/project/ProjectList.tsx`** — minimal list (name + abbreviation + status, click to detail). Phase 1B will refurbish to a richer list with filters + customer column.
- [x] **menuConfig: Projects entry** under `section: "financials"`, module `Modules.PROJECTS`, priority 30, icon Briefcase.
- [x] **PM / Owner / AP / AR / Controller / Reviewer / Auditor / Tenant Admin curated slots** updated to `["time", "labor", "projects", "profile"]`. DEFAULT_PRIMARY_SLOTS also updated for unknown-role + system-admin fallback.
- [x] **App.tsx routes**: `/project/list` and `/project/:publicId` inside AppLayout, under ProtectedRoute.
- [x] **menuConfig tests** updated (24/24 specs pass) — covers Projects in curated mappings, in entriesInSection("financials"), fallback for unknown roles.
- [x] **CSS** for `.project-tabs` / `.project-tab` strip — horizontally scrollable, charcoal active state.
- [x] **New `fetchBudgetByProject` + `budgetKeys.byProject`** in `src/api/budget.ts` so the Budget tab can resolve a project's budget without a manual lookup.

### Phase 1B — Vendors + Customers unpark + BudgetLayout retirement (in progress)

- [x] **Unpark `pages/vendors/`** — remove from tsconfig exclude. Verify VendorList / VendorView / VendorEdit / VendorCreate against current API envelope + Tier 2 cache patterns (`/api/v1/get/*` is whitelisted; `/list/*` is whitelisted). Add menuConfig entry under `section: "contacts"`, module `Modules.VENDORS`.
- [x] **Unpark `pages/customers/`** — same pattern as Vendors. Add menuConfig entry under `section: "contacts"`, module `Modules.CUSTOMERS`. Reuse `entityListKey`/`entityItemKey` from `useEntity.ts` for post-mutation `invalidateQueries` (U-041 established this; don't re-hand-roll the key tuples).
- [ ] **(tech-debt) Generic `useEntity` mutation invalidation.** `createEntity`/`updateEntity`/`deleteEntity` are plain async fns that don't invalidate the RQ cache — every `useEntity`-based page (vendors + customers now — 6 hand-rolled call sites; the ~25 still-parked CRUD pages later) must hand-roll `invalidateQueries` after a write (5-min `staleTime` + persisted cache ⇒ stale reads / stale `row_version` otherwise). Consider a `useMutation`-shaped layer or an `invalidateEntity(qc, {listPath, itemPath})` helper so callers stop re-hand-rolling. Non-trivial: ~25 call sites + non-uniform mutation-path→get-path/key mapping (pluralization, `_` vs `-`), so keys can't be mechanically derived. Defer out of any single unpark unit.
- [x] **Retire `BudgetLayout` / `BudgetSidebar`.** Move `/budget/list`, `/budget/create`, `/budget/:publicId`, `/budget/:publicId/edit` into `AppLayout` (currently in `BudgetLayout` sibling chrome). `/budget/:publicId` can either stay as the page route OR redirect to `/project/{budget.project_public_id}` with the Budget tab pre-selected — pick when implementing. — **DONE (U-066, 2026-07-16):** all 4 routes + the `/budget/*` redirect moved into the `AppLayout` group; both layout files deleted. **Decision: `/budget/:publicId` stays the page route.** The redirect option was rejected as unbuildable-as-scoped — `ProjectDetailScreen`'s `activeTab` is `useState`, not URL-driven, so "Budget tab pre-selected" would first require adding URL-param tab state to a shipped screen; the target also needs `budget.project_public_id`, so it could not be a static `<Navigate>` (async resolve + loading flash on every budget link); and it would break the `BudgetView → /budget/{id}/edit?rev={co} → back` round-trip. The reuse it would have bought already exists — `ProjectDetailScreen` renders `BudgetViewContent` from `BudgetView.tsx`, one component, two mounts. **Load-bearing detail:** `budget.css` was imported *only* by `BudgetLayout`, so the retirement had to relocate that import to `BudgetList`/`BudgetView`/`BudgetEdit` (not `BudgetCreate` — it uses no budget selectors) or every budget style would have orphaned, including in the project Budget tab, which was styled only by BudgetLayout's static import.
- [x] **Wire the `more` slot to BottomTabBar.** (U-046, 2026-07-16) Bottom pill ends with a "More" slot that opens `MoreDrawer`, shown only when `secondarySectionsForUser(me)` returns ≥1 section. Pill is 4 primary + 1 More. Also fixed the same bug on desktop: `AppSidebar`'s local `SECONDARY_SECTIONS` omitted `contacts`, so Vendors/Customers were URL-only on **every** breakpoint, not just mobile. Both surfaces now share one `secondarySectionsForUser` helper (which also retired AppSidebar's latent Projects double-listing).
- [x] **Refurbish the live `pages/project/` ProjectList** — **DONE (U-067, 2026-07-17):** added client-side name/abbreviation search (pure, tested `projectListFilters.ts` with a mutation-verified no-mutation contract) to the routed singular `src/pages/project/ProjectList.tsx`, kept on the ios-primitive track (NavHeader/SectionCard/ListRow — NOT DataTable). Deleted the unrouted **plural** `src/pages/projects/ProjectList.tsx` (name-collision duplicate, zero importers). Scope corrected mid-unit: the assignment named the plural dead file; the live surface is the singular one. **Status filter dropped** (all 135 projects are `Status='active'`; free-text NVARCHAR(50), no enum). **Customer column deferred → U-071**: `Project` payload carries only `customer_id`; client-side resolution is `Modules.CUSTOMERS`-gated (16/21 humans — every PM/Field Crew/Intern — lack it), so the proper fix is a server-side `customer_name` join (api+web, low) done when api is free.
  - **Follow-up (rule-of-three, deferred):** `.project-search-input` (`index.css`) is a byte-for-byte clone of `.labor-filter-input` — the 2nd hand-rolled ios-track search field. Extract a shared `SearchField` primitive into `src/components/ui/` + one `.ios-search-input` class and migrate labor + projects onto it **when a 3rd ios-track list search field appears** (that unit can safely touch `LaborList.tsx`; this one deliberately did not). Do NOT take the cheap fix of putting `labor-filter-input` on the Projects page — that's invisible cross-page coupling, worse than a visible duplicate.
  - **Follow-up (deferred):** filters use local `useState`, not URL params. Correctly proportionate today — `ProjectDetailScreen`'s back button hard-navigates to bare `/project/list` (no `?q=`), so URL params would restore nothing on the primary back path. Only worth it as a coordinated pair with switching that back button to `navigate(-1)`, if bookmarkable filters are ever wanted.
  - **Remaining Phase-1B port:** the plural `pages/projects/{View,Edit,Create}.tsx` stay parked (tsc-excluded; no `/project/create` or `/project/:id/edit` route exists) — port them into `project/` or delete the folder as a separate unit.

#### Deferred out of U-046 (found during its review — none are regressions from it)

- [x] **Budgets is unreachable from nav — same bug class U-046 just fixed.** `Modules.BUDGETS` exists and `/budget/list|create|:publicId|:publicId/edit` are all routed, but there is **no `MENU_ENTRIES` entry for budgets**. It is linked only from `BudgetSidebar`, which renders only once you are already on `/budget/*` — so Budgets is reachable **only by typing the URL**. Verified 2026-07-16. Adding the entry is a behavior change + a per-role curation decision, so it was left out of U-046. Pairs with the BudgetLayout retirement item above. — **DONE (U-055, 2026-07-16):** entry added at `section: "financials"`, `priority: 35` (Projects 30 → Budgets 35 → Bills 40 → Expense 45), icon `Calculator`, `Modules.BUDGETS` / `can_read`, secondary-only (pill is at its ≤5 cap). Flows to both surfaces via `secondarySectionsForUser` with zero consumer changes.
- [x] **Nav has no shell-awareness — `MenuEntry` declares `route` but not which layout owns it.** U-046 newly exposes More → Financials → **Bills** on a phone, but `/bill/*` renders under `BillLayout`, whose `.sidebar` is a hard `width: 220px; flex-shrink: 0` with **no media query** — so a 220px sidebar eats a 430px column, with no bottom pill and no More drawer. Not a dead-end (the `← Field App` link escapes) and not a regression (AppSidebar already exposed Bills at ≥768px), but it is a real phone→desktop-chrome jump. Right-depth fixes: a `shell`/`surface` field on `MenuEntry`, or fold Bill/Budget into the responsive `AppLayout` (see the BudgetLayout retirement item). Do not paper over it per-entry. — **DONE (U-079, 2026-07-17):** took the second fix for **Bills** too, closing the item. All 5 bill routes (`/bill/list`, `/bill/create`, `/bill/:publicId`, `/bill/:publicId/edit` + the `/bill/*` redirect) moved into the `AppLayout` group; `BillLayout.tsx` + `BillSidebar.tsx` deleted, and `Header.tsx` (imported *only* by BillLayout) with them. **Decision: `/bill/:publicId` stays the BillView page route** — it *is* the AP completion page, nothing to redirect to (mirrors the U-066 BudgetView call). Gate-1 confirmed BillSidebar carried nothing AppLayout lacks: its "Bills" link is already a `menuConfig` entry, and the `← Field App` back-link + top-bar Sign Out are subsumed by AppLayout's always-present responsive nav (Time is a primary slot; sign-out via Profile). `routes.test.tsx` flipped `branchHasLayout(branch, BillLayout)` → `AppLayout` and gained the bill route-precedence suite. No shell still on sibling chrome. Orphaned legacy CSS (`.app-layout`/`.app-main`/`.app-content`, `.header*`) + the already-dead `Sidebar.tsx` left for a separate housekeeping sweep (they were deliberately-retained legacy per the index.css comment; deferring keeps this diff tight).
- [ ] **`Sheet.tsx` has no dialog semantics or focus management.** No `role="dialog"`, `aria-modal`, `aria-labelledby`, focus move-in, focus trap, or focus restore. Esc + backdrop dismiss already work. Deferred from U-046 by explicit decision: `Sheet` is shared by 7 consumers (MoreDrawer, TimeLogForm, EditEntryScreen, ChangePasswordSheet, UserPickerSheet, ProjectPickerSheet, SubCostCodePickerSheet), so this is its own reviewed unit with a regression pass over all of them. **Note the live overclaim:** `BottomTabBar`'s More button advertises `aria-haspopup="dialog"` while the popup is generic divs — fix them together. (Codex P3 on U-046.)
- [ ] **Two definitions of "active" now coexist in the bottom pill.** Primary tabs use `NavLink`'s default matching, which is *false* on detail pages (`/time-entry/123` does not start with `/time-entry/list`), while More uses `isEntryRouteActive` base matching, which *is* true on `/vendor/456`. Net: on a vendor detail page More is lit; on a time-entry detail page nothing is lit. More's rule is arguably the correct one and the primary tabs are the pre-existing gap. UX call, not a cleanup — decide before extending either.
- [ ] **`MAX_PRIMARY_SLOTS` vs the More slot (latent).** Widest role today is 4 primary + More = 5, exactly at the cap. A 5th curated primary slot would make the pill 6. Truncating is NOT the fix: `section: "primary"` entries (Time/Labor) are not in `SECONDARY_SECTION_SPECS`, so a truncated primary entry would vanish from every surface. Decide the overflow rule when a role actually needs a 5th slot. CSS degrades gracefully today (`overflow-x: auto`).
- [ ] **`BillSidebar` is a hand-maintained nav surface outside `menuConfig`.** _(Was `BillSidebar` / `BudgetSidebar`; `BudgetSidebar` is gone — U-066 deleted it. `BillSidebar` is the last one.)_ It hardcodes its own `<NavLink>` list — the same shape as the pre-U-046 `AppSidebar` that caused this bug. Fold into the menuConfig model when retiring those layouts.
- [ ] **(cosmetic) `menuConfig.test.ts` mixes conventions.** 14 pre-existing `makeModule("Time Tracking", …)` raw literals sit alongside the newer `Modules.*` constants. Note the tension before mechanically converting: in *nav code* the constant is strictly better (a typo fails compilation), but in a *test* a raw literal pins the real wire value, whereas `Modules.X` on both sides of the assertion is tautological. Decide intent rather than sweeping.

#### Deferred out of U-055 (found during its two-pass review — none are regressions from it)

- [ ] **No tripwire for "routed but not in nav" — the exact U-055 bug class, still unguarded for the NEXT entity.** U-046's tripwire asserts every non-primary `MENU_ENTRIES` section appears in `SECONDARY_SECTION_SPECS`; it would NOT have caught U-055 (budgets had no entry at all), and a route-coverage tripwire would NOT have caught U-046 (`/vendor` + `/customer` were covered while still unreachable). **Two failure modes, two guards — this is the missing second one.** Feasibility was measured, not assumed: parsing `src/App.tsx` and grouping by **first path segment** yields 14 namespaces, 10 covered by a `MENU_ENTRIES` route and **4 legitimately nav-less** (`/`, `*`, `/login`, `/user`) — a small, stable allowlist. Per-namespace is the right granularity; per-route (41 paths) would misfire on `/budget/create`, `/profile/details/:fieldKey`, etc. Pre-U-055, `/budget` would have shown as UNCOVERED. **Two design calls before building:** (1) it needs a **vacuous-pass defense** (assert ≥14 namespaces found) — if `App.tsx` ever moves to `path={ROUTE_CONST}` the regex silently matches nothing and the test goes green while guarding zero, which is worse than no test; (2) it reads another file's *source text*, unlike the in-module constant-vs-constant existing tripwire, so it likely belongs in its own file, and the allowlist is a **policy** call about which namespaces may be nav-less — deserves review, not smuggling.
  - **⚠️ RE-SCOPED by U-066 (2026-07-16) — do not build the regex design.** The route tree is now exported as a real data structure: `appRouteTree` in **`src/routes.tsx`** (a module that deliberately imports no shell/provider components, so consumers need no mocks). `createRoutesFromElements(appRouteTree)` + `matchRoutes` gives the routes directly — see `src/routes.test.tsx` for the working pattern. Measured off the real structure it reproduces this item's regex-derived numbers exactly: **41 paths / 14 namespaces**, nav-less set exactly `/`, `*`, `/login`, `/user`. **Both design calls above are now moot:** (1) the vacuous-pass hazard is *structural*ly gone — JSX props evaluate, so `path={ROUTE_CONST}` lands in `RouteObject.path` as a real string; a regex can silently match nothing, a data structure cannot; (2) there is no source-text reading — it's an ordinary typed import, so the own-file question dissolves. **What remains is only the allowlist policy call** (which namespaces may legitimately be nav-less) — still deserves review, still don't smuggle it.
- [ ] **(rule-of-three) Per-entry RBAC test triad is now copy-pasted 3× (vendors / customers / budgets).** ~14 lines per entity asserting *data* ("entry X exists and is wired to module Y"), not logic — the three `canSeeEntry` branches they exercise are already covered generically by the `time`/`labor` tests. The `role` field in each is inert (`canSeeEntry` never reads `me.role`), and the admin-bypass leg is near-tautological (`canSeeEntry` returns at `if (me.is_admin) return true;` before reading `entry.module`, so it passes for any existing entry). An `it.each` over `[id, moduleName]` pairs collapses 9 tests → 3, making the next entity a one-line row. **Deliberately deferred from U-055:** the fix requires rewriting the two pre-existing vendors/customers blocks (~45 lines of churn on a 10-line declarative unit), and the explicit test names read well in output. Phase 1+ adds Invoices / Bill Credits / Employee Labor — take this on at the 4th entity. Keep the raw `makeModule("Budgets")` literals when collapsing (see the convention note above).

### Phase 2 — Tier 3 financial surfaces (3-4 weeks, web-only)

### Phase 2 — Tier 3 financial surfaces (3-4 weeks, web-only)

- [ ] **Unpark Bills.** Verify scaffolds against API envelope + auto-save (300ms debounce per pattern) + completion-outbox + ReviewTimeline patterns. Add to `menuConfig.ts` under `section: "financials"`, module `Modules.BILLS`. AP / Controller pill swaps to `["bills", "expenses", "invoices", "more", "profile"]`.
- [ ] **Unpark Expenses.** Same scaffolding verification. AP / field-receipt-capture get Expenses in their pill.
- [ ] **Unpark Invoices.** Same. AR / Controller pill gains Invoices.
- [ ] **Unpark BillCredits.** Lower-frequency surface; lives in More drawer Financials section, not a primary slot.
- [ ] **Project detail page Bills/Expenses/Invoices/ContractLabor tabs** become fully functional — same list component as top-level entries, with `defaultFilter={{ project_id }}` + `hiddenFilters={['project_id']}` + `showProjectColumn={false}`.
- [ ] **Per-role primary slot mapping update** in `PRIMARY_SLOTS_BY_ROLE` for AP / AR / Controller / Reviewer / Auditor / Tenant Admin.

### Phase 3 — Admin section (2-3 weeks, web-only)

- [ ] **Unpark Users / UserList / UserCreate, Roles, Modules, Organizations, Companies, Integrations, Review Statuses.** Add admin section entries with `requiresAdmin: true` where appropriate. Most lives in sidebar Admin section (collapsed by default).
- [ ] **Reference Data sub-group inside Admin.** Cost Codes / Sub Cost Codes / Payment Terms / Review Statuses / Address Types / Vendor Types / Taxpayers. Collapsed by default; phone access only via deep link.
- [ ] **UserRole / RoleModule / UserModule / UserProject** as standalone Admin entries for bulk operations (inline editors on User/Role pages handle the common case).

### Phase 4 — Dynamic menuConfig + IsNavigable (1 week, requires API change)

- [ ] **API: `dbo.Module.IsNavigable BIT NOT NULL DEFAULT 1`.** Lets ops hide modules from nav without revoking permissions. ~2 hours server.
- [ ] **Refactor `menuConfig` to consume `me.modules` directly.** Render any module with `can_read && is_navigable && route !== null`. Hardcoded `MENU_ENTRIES` collapses to ~5 (Profile, sub-screens of Profile, anything without a Module). Per-role primary slot resolver becomes a priority map keyed on module name.

### Phase 5 — Deferred (do not attempt until earlier phases prove out)

- [ ] **Cross-entity Search.** Multi-week — needs API + indexing decision. When built, lands as a header bar input (cmd-K) — NOT a menuConfig entry.
- [ ] **Dashboard.** Defer until Phase 3 telemetry shows what roles open first. Too many audience-specific KPIs to spec prematurely.
- [ ] **Inbox / Email Messages.** Gated on the larger inbox rebuild per umbrella MEMORY.md — no timeline.
- [ ] **BuildOneTray revival.** Separate UX project.

### Anti-patterns to avoid (codebase-specific)

- Don't ship a 5+ entry flat bottom tab bar. Stay at 5 slots max. Capacity grows via the More drawer.
- Don't put financials as Profile sub-screens just because they fit alphabetically under "Account."
- Don't spin up another `BudgetLayout`-style sibling chrome.
- Don't gate menuConfig entries on hardcoded role name strings. Use module read perms or `is_admin`.
- Don't add Reports / Inbox menuConfig entries as placeholders — both need API support that doesn't exist.
- Don't touch `NavHeader.tsx` per-page back/title conventions — orthogonal to top-level nav.
- Don't revive `BuildOneTray` or `src/agents/` tree as part of nav work — separate UX project.

---

## Auth / authz hardening — API repo follow-ups (from 2026-06-16 evaluation)

The full auth+authz evaluation lives in the workflow transcript at
`/private/tmp/claude-501/-Users-chris-Applications-build-one/7b4ffd2a-28ac-4754-9941-d5889b92a8cd/tasks/wuqbrmpaw.output`
(audit + risk pass across web/api/ios). Web-side Phase 0 (redirectToLogin
cleanup, Sign out of all devices, agent transcript scoping) shipped this
session. The items below are API-repo work, in priority order per the
user's answers to the open questions.

### Phase 0.5 — refresh cookie never reaches the API (SameSite=Lax + cross-site) — ✅ RESOLVED via Path B (2026-07-12)

> **UPDATE 2026-07-10 — Path A REVERTED; Path B underway; deploy HELD at Gate 2.** Path A (SameSite=None) was built then reverted: Codex Pass-1 confirmed a **P1** — even once the cookies transmit cross-site, the CSRF double-submit still 403s because the web reads `token.csrf` from `document.cookie` (`src/api/client.ts:68`) but that cookie is host-only on the API host and unreadable from `app.bld-one.com`. Path A just trades a 401 for a 403; the hourly logout persists (plus SameSite=None is third-party-cookie auth → Safari ITP / Chrome phase-out fragility). Pivoted to **Path B (custom API domain `api.bld-one.com`)** → app+api share eTLD+1 `bld-one.com` → same-site → Lax works AND the CSRF cookie is readable (scoped `Domain=bld-one.com`). **✅ DONE 2026-07-12:** API deployed (`master` — `api.bld-one.com` + managed cert + CSRF `Domain=bld-one.com` cookie), then the web flipped `VITE_API_BASE_URL` → `https://api.bld-one.com` (`main` `9654743`) + SWA-deployed to `app.bld-one.com`. Verified: the live bundle references only `api.bld-one.com`; CORS preflight from `app.bld-one.com` → `api.bld-one.com/auth/refresh` returns 200 with Allow-Credentials + Allow-Origin `https://app.bld-one.com` + Allow-Headers `x-csrf-token`. Same-site → SameSite=Lax + CSRF double-submit both work. **Human check pending:** confirm a live browser session stays logged in past ~60 min. Full runbook + API detail: `build.one.api/TODO.md` "Auth cross-site 'hourly re-login' fix → Path B". The original Path A/B write-up below is retained for context — Path A is now moot.

- [ ] **Fix the refresh-cookie cross-site drop.** Surfaced 2026-06-16. User reports being forced to log in roughly every hour. Root cause confirmed by reading `entities/auth/api/router.py:95-120` + `.env:18`: the three auth cookies (`access_token`, `refresh_token`, `token.csrf`) are all set with `samesite="lax"`. The web is hosted at `app.bld-one.com`; the API is at `buildone-esgaducjg4d3eucf.eastus-01.azurewebsites.net`. These are different sites (different eTLD+1). `SameSite=Lax` allows cookies on top-level navigation but **NOT on `fetch()` cross-site requests**. So every time the access token expires (60 min per `access_token_expire_seconds=3600`), the web POSTs to `/api/v1/auth/refresh` → browser refuses to send the refresh cookie → server returns 401 → user bounces to /login. The 30-day refresh token TTL is irrelevant because the cookie never reaches the server.

  **Two paths:**
  - **Path A — change SameSite to None (immediate).** Edit `entities/auth/api/router.py:95-120` to `samesite="none"` (keep `secure=True` — required when SameSite=None). Verify the CORS config has `allow_credentials=True` AND `allow_origins=["https://app.bld-one.com"]` (or a list including it — wildcard `*` is incompatible with credentials). Test: log in, wait 60+ min or manually expire the access token, confirm a background API call refreshes instead of bouncing to /login. ~1 hour incl. verification.
  - **Path B — Custom API domain (proper long-term fix).** Move the API to `api.bld-one.com`. Then both surfaces share eTLD+1 = `bld-one.com` → same-site → `SameSite=Lax` works as-is. Already on this TODO as the deferred "Custom API domain (api.bld-one.com)" item. Resolves this issue as a side effect AND avoids the wider `SameSite=None` cookie surface.

  **Recommendation**: Path A now for immediate relief, then Path B when the custom-domain work happens (which moots Path A's wider cookie permission).

  **Verification once Path A lands**: open DevTools → Application → Cookies on `buildone-…azurewebsites.net` after a fresh login. The three auth cookies should show `SameSite=None`. Then on the Network tab, trigger a 401 + refresh; the refresh request's Cookie header should include the refresh cookie. Today that header is empty.

### Phase 1 — Active Sessions surface + agent audit-trail (yes/strong appetite)

- [ ] **Active Sessions API endpoints.** Build out:
  - `GET /api/v1/mobile/auth/sessions` — list active `AuthRefreshToken` rows for the caller. Fields: `(jti, issued_datetime, last_used_datetime, user_agent, ip, is_current)`. The "current" flag marks the row whose hash matches the caller's refresh-cookie.
  - `DELETE /api/v1/mobile/auth/sessions/{jti}` — revoke a specific session (reuses `revoke_by_jti`).
  - Capture `User-Agent` + remote IP at refresh-token-mint time and persist on the row (column add to `dbo.AuthRefreshToken`, sproc update, service threading from the request via FastAPI `Request` object).
  - Effort: ~3 days. Web-side `Profile → Active sessions` screen + per-row revoke is then a ~2-day follow-up.
- [ ] **Agent audit-trail separation — shape A (`acting_on_behalf_of_user_id` column).** Decision locked 2026-06-16: column shape, NOT JWT actor claim, NOT header pattern. Rationale: smallest schema delta, captures the audit need without touching the JWT/RBAC layer; if RBAC-level on-behalf-of becomes needed later, shape C can be layered on top without rework. Work:
  - Add nullable FK `ActingOnBehalfOfUserId BIGINT NULL FK User.Id` to: `Workflow`, `AgentSession`, optionally `Bill` / `Expense` / `Invoice` create rows.
  - New ContextVar `current_acting_on_behalf_of_user_id` populated when an orchestrator's delegation tool fires. ToolContext threads it through every sub-agent call.
  - Service-layer writes to `Workflow.ActingOnBehalfOfUserId` when the ContextVar is set.
  - Surface in an admin audit view (separate web task once data flows).
  - Effort: ~1 week. Server-only; no web-side dependency.

### Phase 2 — Server-side hardening (1-2 weeks, defer 4-6 weeks)

- [ ] **Cross-worker RBAC cache invalidation.** `shared/rbac.py` keys cache per (user_sub, company_id) with 5-min TTL. `invalidate_all_caches()` only clears the dict in the worker that handled the mutation — other gunicorn workers serve stale grants for up to 5 min. Implement via a small Redis pubsub (preferred — App Service can add Azure Cache for Redis Basic at ~$15/mo) OR a polling watermark in `dbo.RbacInvalidation` table that each worker checks every 30s. Manifests today as "I changed your role and you still see the old menu." Effort: ~3 days for the Redis path, ~5 days for the DB-polling path.
- [ ] **401 vs 403 discrimination.** Web's `redirectToLogin` fires on every 401 — including 401s emitted by a backend bug, misconfigured `require_module_api`, or 502→401 proxy translation. Server should return 403 for permission denials, reserve 401 for "authentication invalid" (missing/expired/forged token). Web client checks the body for `error="invalid_token"` per RFC 6750 §3.1. Trivial server change (FastAPI dependencies + custom 401 exception handler); ~1 day web change to discriminate. Effort: ~2 days total.
- [ ] **JWT `kid` claim + JWKS endpoint.** Manual secret rotation today means a rotation-day code redeploy. Adding `kid` to the JWT header + serving a JWKS endpoint (`GET /.well-known/jwks.json`) lets the secret rotate independently of code deploys. Can stay on HS256 (uglier but lower-risk) or switch to RS256 simultaneously. Defer until next rotation actually happens. Effort: ~3 days HS256+kid, ~5 days for RS256 migration.

### Phase 3 — DEFERRED, conditional

- [ ] **React Query persister cid-keying.** Today the persister key is `bo.rq.v1.<uid>` — switching Companies (cid changes) keeps the same key, leaking Company A's cached entity data into Company B context. Latent today (single Company in prod); user confirmed Phase 5b multi-Company is >6 months out. Add `// FIXME(Phase-5b)` comment in `src/main.tsx` so it's visible when the time comes. Effort: ~2 hours.
- [ ] **httpOnly cookie for access token.** Defeats XSS exfiltration of the JWT. Big project — CORS rework, CSRF rework, potentially breaks the agent-as-user pattern. Standalone effort. Trigger: real XSS incident, security audit, or regulatory ask. Effort: ~6-8 weeks if greenlit.

---

## Profile — Self-read carve-out on Contacts endpoint (API change)

- [ ] **`GET /api/v1/get/contacts/user/{user_id}` is gated on `Modules.VENDORS`.** Surfaced 2026-06-15 while wiring the Profile Contact rows. The endpoint reads ANY user's contacts as long as the caller holds the Vendors module (legacy "manage a vendor's contact list" use case). Side effect: a non-admin user who lacks Vendors read can't see their OWN Contact data on `/profile` or `/profile/details` — the row falls to "—". Christopher (system admin) sees it fine because IsSystemAdmin bypasses module gating; field users with role e.g. Field Crew / Intern would not. **Fix lives in `build.one.api`**, two options:
  1. **New endpoint `GET /api/v1/get/contacts/user/me`** — auth-only (no module gate), reads `current_user_id` from the ContextVar, returns just the caller's contacts. Same shape as `read_by_user_id`. Web's ProfileView + UserDetailScreen switch to this endpoint when reading the caller's own contacts (no behavior change for admin Vendors-side use of the existing endpoint).
  2. **Self-read carve-out on the existing endpoint** — at the top of `get_contacts_by_user_id_router`, if `user_id == current_user_id.get()`, skip the Vendors check; otherwise require it. Minimal surface change but couples auth-vs-RBAC in a way that's easy to miss when someone adds a new gate.
  Recommend option 1 — single-purpose endpoint, single-purpose docstring, easier to RBAC-audit. Web change is a one-line URL swap. Defer until a non-admin user actually hits this on prod (Christopher is the only active human signing into web today; field users land on iOS).

---

## Budget UI — future work

- [x] **Change-order timeline on the budget view (future version).** Requested 2026-06-15. Render the revision history as a visible amendment ledger/timeline on `BudgetView`, not just the flat revisions table that ships today. Each entry = a revision in order (Original, then each Change Order) showing: title, status badge, effective date, **net dollar impact** (sum of that revision's line `price` deltas — contract value; optionally `amount` for cost), approver + approved date, and a **running contract total after each approved revision**. Expandable to that revision's delta line items. **No backend work needed** — the data already exists: `GET /api/v1/get/budget-revisions/by-budget/{budget_public_id}` (carries `approved_by_user_id`/`approved_datetime`/`effective_date`/`type`/`status`/`revision_number`) + `GET /api/v1/get/budget-line-items/by-revision/{revision_public_id}` for each revision's delta lines. Pure frontend addition to `src/pages/budgets/BudgetView.tsx` (+ `budget.css`). Only approved revisions count toward the running total; draft COs shown as pending. Context: umbrella memory `project_budget_entity.md`. **DONE (U-017, 2026-07-14):** `BudgetView` revisions table replaced with an expandable amendment ledger (Rev/Type/Title/Status/Effective/**Net Impact**/**Running Total**/Approver/Actions). Ledger math extracted to a pure, unit-tested `src/pages/budgets/revisionLedger.ts` (`buildLedger` — cent-exact integer-cents sums, approved-only running total in `revision_number` order, draft/pending marked "Pending" + excluded, unknown-vs-known-zero distinction so a still-loading/errored revision shows "…" not a fabricated $0.00). Per-revision delta lines fetched in parallel via `useQueries({combine})`; approver resolved via `useIdNameMap`. Two-pass ✓ (Codex gpt-5.5/xhigh: P1 unknown-vs-zero under-report fixed → re-review PASS; `/simplify` 4-agent applied combine-stability + useIdNameMap reuse + dedup, behavior-preserving Codex PASS). tsc + build + **110 vitest** green (incl. 5 cacheCleanup + 10 revisionLedger specs). Preserves the Edit/View-lines link + "+ Change Order" affordances. Deploy = combined web SWA ship, held.

---

## Audit 2026-05-20 — Web findings

Full multi-repo audit memory: `~/.claude/projects/-Users-chris-Applications-build-one/memory/project_audit_2026_05_20.md`. Web-specific findings below; pair with that memory for context, cross-repo patterns, and Tier-1 fix list.

### Critical / High
- [x] **`AuthContext.isAuthenticated` and `ProtectedRoute` read `localStorage` during render — non-reactive.** [src/auth/AuthContext.tsx:44](src/auth/AuthContext.tsx); [src/auth/ProtectedRoute.tsx](src/auth/ProtectedRoute.tsx). Login/logout doesn't update derived state until the component remounts. Users can land in redirect loops or blank pages. Fix: derive `isAuthenticated` from `username` state (already reactive via `setUsername`); refactor `ProtectedRoute` to use `useAuth().isAuthenticated`. **DONE (U-014, 2026-07-14):** `isAuthenticated = username !== null`; `ProtectedRoute` consumes `useAuth().isAuthenticated`. Same-tab login/logout/session-expiry now update the gate without a remount. Follow-up filed below (cross-tab gate sync).
- [x] **Cross-tab auth-gate sync (follow-up from U-014).** [src/auth/AuthContext.tsx:44](src/auth/AuthContext.tsx). `isAuthenticated` derives from same-tab `username` state; there was no `storage` listener, so a logout/login in another tab didn't flip THIS tab's gate until its next server touch (a data fetch's 401 → `client.ts` `redirectToLogin` self-heals it — no data exposed, gate just briefly stale). Correct fix is NOT a naive `storage`→`setUsername` (a cross-tab *login-as-other* would swap identity without re-keying the per-user persister → the multi-user bleed contract). **DONE (U-026, 2026-07-14):** `AuthProvider` now registers a `window` `storage` listener (empty-deps `useEffect`, removed on unmount) that calls `window.location.reload()` on a REAL cross-tab change to `access_token`/`username` — guarded by `e.storageArea === window.localStorage` (excludes sessionStorage), `e.key ∈ {access_token, username}`, and `e.oldValue !== e.newValue` (no-op writes ignored). Covers login (null→value), logout (value→null), and login-as-other (valueA→valueB); the reload re-runs the boot-time per-user persister keying in `main.tsx` (`bo.rq.v1.<uid>`), never a `setUsername` identity-swap-without-rekey. No self-loop: `storage` fires only in OTHER tabs, and current-tab login/signup/logout already hard-reload. Two-pass ✓ (Codex gpt-5.5/xhigh PASS, 0 findings P0–P3; Pass-2 skipped-trivial — one `useEffect` in the shape of the sibling profile-events effect). tsc 0 + build 0 + **110 vitest** green (incl. 6 cacheCleanup contract specs). **Coverage boundary:** no `@testing-library` harness in-repo → listener behavior covered by tsc + build + Codex + cacheCleanup specs (consistent with U-014, same file). Deploy = combined web SWA ship, held. **U-032 refinement (2026-07-15):** the U-026 listener hard-reloaded on ANY cross-tab `access_token` change — but `client.ts` rewrites `access_token` on every token refresh (~hourly / on 401), so OTHER tabs got a spurious `window.location.reload()` that discarded unsaved work on non-auto-save surfaces (e.g. Bills forms). Extracted the reload decision into an exported pure predicate `shouldReloadOnStorage(e)` (`src/auth/authStorageSync.ts`); the listener is now a thin wrapper. Reloads ONLY on a real identity transition: `username` change, `access_token` removal (logout), OR the token's decoded **`uid`** claim changing — keyed on `uid` via the existing `decodeJwtPayload` (`src/api/jwt.ts`), the SAME value `main.tsx` keys the persister on (`bo.rq.v1.<uid>`), NOT the raw token string or the username string. A same-user refresh keeps the same uid → no reload. Pass 1 (workflow xhigh, refute-verified: 6 candidates → 4 refuted → 1 PLAUSIBLE-not-live) surfaced that keying identity on the username STRING misses a same-username/new-uid swap → the `uid`-keying closes it (implemented as a review-driven Refactor; deviates from the literal Gate-1 removed-only spec, flagged for approval). Pass 2 `/simplify` (reuse/efficiency clean) retired the duplicated rationale comment. tsc 0 + build 0 + **129 vitest** green (10 predicate specs incl. refresh→no-reload + uid-swap→reload; 6 cacheCleanup contract specs still green). Gate-2 held (unstaged).
- [ ] **Access token in localStorage is XSS-exfiltratable (audit 2026-05-20 residual — split out of the cross-tab bullet above at U-026, distinct concern, NOT done).** The CSRF cookie is correctly httpOnly; the access token isn't. Long-term fix: move JWT to httpOnly cookie (requires API changes — CORS + CSRF rework; see the "httpOnly cookie for access token" Phase 3 item under Auth hardening below). Interim: tight CSP headers + remove every `dangerouslySetInnerHTML` usage. [src/auth/AuthContext.tsx:37,62](src/auth/AuthContext.tsx); [src/api/client.ts:49](src/api/client.ts).
- [x] ~~**Logout clears only `["me"]` query from React Query cache.**~~ **DONE (verified 2026-07-14 recon; `b4c0792`).** `AuthContext.logout` calls `queryClient.clear()` (whole cache) + awaits `clearAllUserScopedStorage()` before redirect — no cross-user cache bleed.
- [ ] **Complete/Delete handlers missing `finally { setX(false) }`.** [src/pages/bills/BillEdit.tsx:221-233](src/pages/bills/BillEdit.tsx); [src/pages/expenses/ExpenseEdit.tsx:204-217](src/pages/expenses/ExpenseEdit.tsx); [src/pages/invoices/InvoiceEdit.tsx:198-210](src/pages/invoices/InvoiceEdit.tsx). Success path leaves the button stuck disabled; potential setState-on-unmounted-component warning when navigation fires. Same pattern on `BillCreate.tsx:46-80` (saving). Fix: wrap each in `try { ... } finally { setX(false); }` (and bail with a mounted ref check if needed).
- [x] ~~**Bill complete missing `await flushAutoSave()` before `saveAll()`.**~~ **MOOT (verified 2026-07-14 recon).** BillEdit + InvoiceEdit do NOT use `useAutoSave` — there's no auto-save to race; `saveAll()` on Complete IS the save. Only ExpenseEdit has auto-save and it already flushes. Re-open only if Bills/Invoices adopt auto-save.
- [x] ~~**`BillEdit.tsx` formRef captured null before initialization**~~ **MOOT (verified 2026-07-14 recon).** `if (!form) return null` gates the entire form + Save-button render, so `saveAll()`'s `if (!latestForm) return false` is unreachable from a user action.

### Medium
- [x] **`useCompletionPolling` ignores all errors — 401/500 spins for 3 minutes silently.** [src/hooks/useCompletionPolling.ts](src/hooks/useCompletionPolling.ts). ✅ FIXED 2026-07-12 (U-005): catch now branches on the structured `ApiError.status` — 404 keeps polling, any other HTTP status stops with a clear error surfacing `err.detail`, network/no-status stays transient. Also added a per-start generation guard so overlapping/stale polls can't clobber a terminal state (found in review). 10 vitest specs (`useCompletionPolling.test.ts`) cover 404/401/500/200/transient/timeout/overlap×2/stale-restart.
- [x] ~~**ESLint flagged: `setState` synchronously inside `useEffect`.**~~ **BENIGN-LINT (verified 2026-07-14 recon).** `usePaginatedList:190` sets `loading` before fetch (standard; `load` memo deps exclude `loading` → no loop); `useViewAttachmentObjectUrl` resets to initial values (idempotent). No cascade — suppress the lint, not a bug.
- [x] ~~**`FolderPicker.tsx:76` `useEffect` missing dep `browseFolderItems`**~~ **BENIGN-LINT (verified 2026-07-14 recon).** `browseFolderItems` is `useCallback([mode])` and `mode` IS in the effect's dep array, so the closure is never stale in practice. Add the dep or eslint-disable; lint-only, no bug.
- [ ] **File upload validates MIME type only, not size.** [src/pages/bills/BillCreate.tsx:34-44](src/pages/bills/BillCreate.tsx). Big uploads time out with a vague network error. Add a `file.size` check (e.g. 50 MB) with a clear inline error.
- [x] ~~**`Sidebar.tsx:27` route fallback mismatches actual routes.**~~ **MOOT (verified 2026-07-14 recon).** `Sidebar.tsx` is dead code — no import anywhere; live nav is `AppSidebar` rendering static `to={entry.route}` from `menuConfig.ts` (no `toLowerCase()` fallback). Optionally delete `Sidebar.tsx`.
- [x] **No `<ErrorBoundary>` wrapping any page.** Render error = blank screen + console log + no recovery. Wrap routes (or the entire `<Outlet />`) in a class-component ErrorBoundary that shows an inline fallback + reload button. — DONE (U-009, 2026-07-13): `src/components/ErrorBoundary.tsx` (class boundary, `console.error` logs, theme-aware `SectionCard`/`ios-page` fallback + Reload; `RouteErrorBoundary` resets on `location.pathname` change without remounting healthy pages). Wraps the `<Outlet/>` in all three shells (AppLayout + BillLayout + BudgetLayout) + a top-level backstop `<ErrorBoundary>` in `App.tsx` covering `/login` + providers. Vitest at `src/components/ErrorBoundary.test.tsx`. Codex PASS.
- [ ] **`computeLineItem` uses JS `Number()` arithmetic for currency.** [src/pages/bills/BillEdit.tsx:39-50](src/pages/bills/BillEdit.tsx). UI totals drift from server on edge cases. Use a small decimal library or carry values as strings until display.
- [x] ~~**`useAutoSave.flush()` swallows save errors.**~~ **DONE (verified 2026-07-14 recon; `5382fac`).** The single-in-flight rewrite made `runSave` reject its chain on `saveFn` error (`rejectChain(err)`), and `flush()` returns that chain — so an awaiting caller now sees the rejection. (Residual, NOT this item: the *debounce* path discards `runSave()`'s promise, so a failed background auto-save is an unhandled rejection with no user toast — track separately if it bites.)
- [x] **InlineContacts delete has no `saving` guard / inline error persistence** — rapid clicks trigger duplicate deletes; transient error message can be lost. — DONE (U-010, 2026-07-13): `handleDelete` now mirrors the sibling `handleAdd`/`handleUpdate` pattern — keeps the `confirm()`, then `if (saving) return` + `setError("")` + `setSaving(true)` at the top, `del()` + optimistic `setContacts` filter on success, `setError(err.message)` in `catch`, `setSaving(false)` in `finally`. Both Edit + Del buttons now carry `disabled={saving}` (matches Save/Add), so no delete/edit/add interleave. Error surface = existing `{error && <div className="form-error">}` under the Contacts header; `setError("")` on delete-start fixes stale-message persistence. tsc + build + 92/92 vitest green (incl. 5 cacheCleanup specs). Codex (gpt-5.5, xhigh) PASS, zero findings.

### Low
- [x] **Extract a shared `renderHook` test harness.** — DONE (U-036, 2026-07-15): hoisted the duplicated `createRoot`+`TestComponent` hook-render harness (+ `deferred`/`drain`) into `src/hooks/__testutils__/renderHook.ts` — generic `renderHook<T>(useHook)`, `deferred<T = void>()` (the `void` default keeps autosave's `deferred()`/`Deferred[]`/no-arg `resolve()` byte-identical while the completion spec uses `deferred<PollResult>()`), and `drain()`. Both `useCompletionPolling.test.ts` and `useAutoSave.test.ts` now import it; every `it()` body stays byte-identical (completion keeps its 11 `renderHook()` call sites via a one-line local wrapper + import alias; autosave keeps a thin `renderAutoSave` wrapper for its editable saveFn/deps + object-shaped rerender). Local copies deleted. **Test-only — zero runtime `src/` change.** tsc -b + build + **129/129** vitest green (incl. 5 cacheCleanup specs). Composer wrote (composer-2.5-fast); Codex (gpt-5.5, xhigh) Pass-1 PASS, zero findings; Pass-2 `/simplify` clean (reuse/efficiency/altitude no findings; one stale-comment nit applied).
- [ ] **[U-036 follow-up] Single-source `IS_REACT_ACT_ENVIRONMENT` in `vitest.setup.ts`.** The flag is suite-global and `vitest.config.ts` already wires `setupFiles: ["./vitest.setup.ts"]`, yet it's still set inline in **5** places (was 3; `src/layout/BottomTabBar.test.tsx` was missed in the original count and U-047 added `src/components/ui/Sheet.test.tsx` — it keeps getting copy-pasted precisely because the "inherit it by importing the harness" rationale doesn't cover specs that don't import it): `src/hooks/__testutils__/renderHook.ts`, `src/components/ErrorBoundary.test.tsx`, `src/pages/expense-coding/ExpenseCodingCockpit.test.tsx`, `src/layout/BottomTabBar.test.tsx`, `src/components/ui/Sheet.test.tsx`. Hoist the one line to `vitest.setup.ts` and delete all inline copies. Out of U-036's 3-file scope (touches non-diff files; changes the flag for suites that don't currently set it). Surfaced by Pass-2 altitude review. Related trajectory note: if hook-test reuse widens, `deferred`/`drain` are general async-test primitives that would belong in a top-level `src/test/` util rather than `renderHook.ts` (leaving that file the render harness proper).
- [x] InlineContacts edit row: no Escape-key handler to cancel edit. Minor UX. — DONE (U-021, 2026-07-14): added `handleEditKeyDown(e, contact)` — **Escape** → `cancelEdit()` (always, regardless of `saving`); **Enter** → `handleUpdate(contact)` only when `!saving` (mirrors the Save button's `disabled={saving}` guard, so no double-save; Enter is a no-op mid-save). Wired `onKeyDown` onto all 5 edit-row inputs; `preventDefault()` on both keys. Add-new-contact row, delete-guard (U-010), and all `saving` guards untouched; normal typing unaffected (only Enter/Escape intercepted). tsc + build + 110/110 vitest green (incl. 5 cacheCleanup specs). Codex (gpt-5.5, xhigh) PASS, zero findings; Pass-2 skipped-trivial.
- [ ] **215 `@typescript-eslint/no-explicit-any` errors** across `src/pages/*` — not bugs themselves but a risk amplifier; real type-mismatches survive type-check. Sweep over time.
- [ ] **[U-047 follow-up] `ActionModal` in `ReviewTimeline.tsx` is now a worse duplicate of `Sheet`.** [src/components/ReviewTimeline.tsx:337-365](src/components/ReviewTimeline.tsx). Hand-rolls `role="dialog"` + `aria-modal` + backdrop + `stopPropagation` with inline styles, and has **no focus trap, no Escape, no focus restore, no `aria-labelledby`**. U-047 made `Sheet` strictly better on every one of those axes, so this should either adopt `Sheet` or be deleted in favour of it. Pre-existing (predates U-047), surfaced by its Pass-2 reuse review. Blast radius = the review-timeline action flow, so it wants its own unit + regression pass.
- [ ] **[U-047 follow-up] Escape closes EVERY open sheet, not just the topmost.** [src/components/ui/Sheet.tsx](src/components/ui/Sheet.tsx). Each open `Sheet` registers its own `document` keydown listener, so if two are ever open simultaneously one Escape fires both `onDismiss` handlers. **Pre-existing** (the listener is unchanged by U-047, whose spec required preserving Esc as-is) and **currently unreachable** — no Sheet nests inside another, and all five consumers mount closed and toggle `open`; a picker's backdrop + focus trap block reaching a second trigger. Confirmed by Codex Pass-1 (P3) and consciously deferred. Fix when the first nested/simultaneous sheet lands: track a module-level open-sheet stack and only let the topmost handle Escape.
- [ ] **[U-047 follow-up] `SheetHeader` title should probably be an `<h2>`, not a `<div>`.** [src/components/ui/SheetHeader.tsx:26](src/components/ui/SheetHeader.tsx). Held out of U-047 because it changes the accessibility tree (= behavior) and Pass 2 is behavior-preserving. **The obvious objection is not real:** `index.css:1-8` has a global `*, *::before, *::after { margin: 0; padding: 0 }` reset, and `.sheet-header-title` already pins font-size/weight with a class selector, so an `h2` renders **pixel-identical** — verified by Pass-2 altitude review. It's an improvement for the two NON-sheet consumers too (`TimeLogForm`/`EditEntryScreen` currently render zero headings). Real question is house consistency: sibling `NavHeader` uses the same div-title pattern while older `PageHeader`/`DetailView` use real headings — decide the convention once and apply it to all of them.
- [ ] **[U-047 follow-up] `aria-labelledby` is claimed unconditionally but delivery isn't guaranteed.** [src/components/ui/Sheet.tsx](src/components/ui/Sheet.tsx). `Sheet` always renders `aria-labelledby={titleId}`, but the context only *offers* the id — a `Sheet` rendered without a `SheetHeader` would get a dangling IDREF and therefore **no accessible name**, the same shape of false claim U-046/U-047 exist to kill. Harmless today (all five consumers render `SheetHeader`). Proportionate fix is a tripwire test in the `menuConfig.test.ts` spirit, NOT `hasTitle` state or folding `title` into `Sheet`'s props — both cost more than the risk.
- [ ] **[U-047 follow-up] Evaluate native `<dialog>` + `showModal()` to replace the hand-rolled focus machinery.** [src/components/ui/Sheet.tsx](src/components/ui/Sheet.tsx). The platform gives trigger-restore-on-close (the entire `triggerRef` mechanism), the focus trap, Escape, `aria-modal`, and `::backdrop` for free — both of U-047's most heavily-commented mechanisms (render-phase trigger capture, StrictMode replay guard) exist *only* because we re-implement the platform. Bonus: top-layer rendering is immune to transformed ancestors, which would **retire the "MoreDrawer must stay outside `.app-tabbar`" landmine** documented in CLAUDE.md and pinned by a DOM-tree test in `BottomTabBar.test.tsx`. Baseline is fine (Safari 15.4+, below this PWA's floor). Migration touches the rise/fade animation, backdrop dismissal, and every pinning test — its own unit. Surfaced by U-047 Pass-2 altitude review.
- [ ] **[U-047 follow-up] Extract a shared component-mount test harness (sibling to U-036's `renderHook`).** The `container = createElement("div")` → `appendChild(document.body)` → `createRoot` → `afterEach` unmount+remove block is now triplicated verbatim: [src/layout/BottomTabBar.test.tsx](src/layout/BottomTabBar.test.tsx), [src/pages/expense-coding/ExpenseCodingCockpit.test.tsx](src/pages/expense-coding/ExpenseCodingCockpit.test.tsx), and the new `src/components/ui/Sheet.test.tsx`. Note U-036's `src/hooks/__testutils__/renderHook.ts` is **hook-only and deliberately does not append to `document.body`** — focus assertions need a connected tree, so component specs correctly can't reuse it. Wants a `mount()` returning `{container, root}` in a new `src/__testutils__/` (the `hooks/` dir is the wrong home for a component harness). Same shape as U-036 → its own test-only unit.
- [ ] **[U-088 follow-up] Extract shared `makeUser`/`makeModule` nav test fixtures.** U-088 added `src/layout/AppSidebar.test.tsx`, which carries a **3rd byte-for-byte copy** of the `makeUser`/`makeModule` `CurrentUser`/`CurrentUserModule` builders — the other two are [src/layout/BottomTabBar.test.tsx](src/layout/BottomTabBar.test.tsx) and [src/layout/menuConfig.test.ts](src/layout/menuConfig.test.ts) (verified identical). Any change to the `CurrentUser` shape means editing all three. No shared fixture module exists yet (`src/hooks/__testutils__/renderHook.ts` is hook-render only). Concrete fix: a `src/layout/__testutils__/navFixtures.ts` (or the broader `src/__testutils__/` proposed by the U-047 mount-harness follow-up) exporting `makeUser`/`makeModule`; migrate all three layout specs. Deferred: test-only, tangential to U-088's active-state unification, and best folded in with the sibling U-047 mount-harness + U-036 `IS_REACT_ACT_ENVIRONMENT` extractions (this new spec adds a copy to **both** of those lists too — `IS_REACT_ACT_ENVIRONMENT` now 6 sites; mount-harness block now quadruplicated). Surfaced by U-088 Pass-2 reuse+simplification review.
- [ ] **[U-005] Expense Coding Cockpit — per-keystroke full-list re-render (only if typing lag appears).** Editor `selections` state lives in the parent, so each keystroke in an expanded row re-runs `rows.map`, re-rendering all N `QueueRowCard`s (unpaginated queue → ~441 at the backlog). Imperceptible at ~1–2/day + a one-time backlog (collapsed cards are trivial, no DOM mutations), so NOT actioned. If typing lag is ever observed, the fix is to memo `QueueRowCard` + stabilize its `selection`/callback props, or localize the editor's transient state to the expanded card and lift only on Confirm (mind the "selections entry == dirty" invariant). Also unpaginated `/expense-coding/queue` — add server paging if the queue grows large.

## /docs — Web section (U-085) follow-ups

Shipped U-085 (2026-07-18): DERIVED build-time route table — `scripts/gen-web-docs-manifest.mjs` AST-introspects `routes.tsx` + `menuConfig.ts` → `src/docs/web/web-manifest.json` → `WebDocs.tsx`; `prebuild` hook regenerates on every build; `webManifest.test.ts` freshness tripwire. Deferred from its Pass-2 `/simplify` (all behavior-preserving, out of the web-routes-only unit scope):

- [ ] **[U-085 follow-up] Shared docs-freshness helper + type.** `relativeAge`/`isStale`/`STALE_AFTER_DAYS` are copied verbatim in both `src/pages/docs/sections/WebDocs.tsx` and `IOSDocs.tsx`; `types.ts` aliases `WebFreshness = IosFreshness`. Extract a shared `src/pages/docs/freshness.ts` (the date helpers) + a shared `DocsFreshness` type that both `IosFreshness`/`WebFreshness` alias, and migrate BOTH sections. Deferred because extracting for a single consumer isn't a win and migrating the working iOS section is outside U-085's web-only scope — do it when the API/MCP/Scheduler sections land (3rd+ consumer makes the shared home pay off). Same call applies to the `?raw`+`JSON.parse`+fallback loader duplicated across `webManifest.ts`/`iosManifest.ts` (a generic `parseRawManifest<T>(raw, fallback)`).
- [ ] **[U-085 follow-up] Rename the `nav_reachable` manifest key.** The count is honest ("page routes carrying a nav_group" = 34) but the KEY name overstates (reads like "nav landing targets", which is ~10). The WebDocs *label* was already corrected to "In nav group" (presentation-only, no output change). Renaming the JSON key (+ `WebCounts` type + the generator's stdout string + a truer companion metric = routes whose full path exactly equals a `MENU_ENTRIES.route`) changes manifest bytes, so it's a follow-up that regenerates. Deferred to keep the Pass-2 cleanup output byte-identical.
- [ ] **[U-085 follow-up] Share the route-tree walker between tests.** `routePaths()` in `src/docs/web/webManifest.test.ts` duplicates the file-local one in `src/routes.test.tsx` (identical minus a `.sort()`). Lift into a shared test util both import when a third caller appears. Test-only, low value.

---
